
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let showUponStr:String = "IM\u{9000}出登\u{5f55}失"
fileprivate let const_temporaryMessage:[Character] = ["败","，","c","o","d","e",":"]

/*: , desc: :*/
fileprivate let showCellKey:String = ", desc:balance as pant resource"

/*: "IM登录失败，code: :*/
fileprivate let kEvenMessage:[Character] = ["I","M","登","\u{5f55}","失","败"]
fileprivate let kTarTheMessage:String = "\u{ff0c}"
fileprivate let main_platStr:String = "volume effcode:"

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let mainDecideString:[Character] = ["I","M","获"]
fileprivate let userVersionUrl:[Character] = ["\u{53d6}","未","读","\u{6d88}","息","总","\u{6570}","失","\u{8d25}","，","c","o","d","e",":"]

/*: "crushBadge" :*/
fileprivate let user_easternValue:String = "documentus"

/*: "badNumber" :*/
fileprivate let app_paintValue:[UInt8] = [0xbe,0xbd,0xb8,0x92,0xa9,0xb1,0xbe,0xb9,0xae]

private func housingTab(texture num: UInt8) -> UInt8 {
    return num ^ 220
}

/*: "plaza" :*/
fileprivate let user_quitUponString:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let const_decreaseValue:[Character] = ["r","o","o","m","N","u","m"]

/*: "num" :*/
fileprivate let user_distinctionId:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let const_willWayString:String = "day pop nothingonlyO"

/*: "LIVE-CHAT" :*/
fileprivate let show_beComprehensiveUrl:String = "LIVE-altogether may while"

/*: "extra" :*/
fileprivate let constPressurePhaseMessage:String = "eoriginalra"

/*: "msgType" :*/
fileprivate let k_customCountId:[Character] = ["m","s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let show_agoExistingFormat:String = "newPkanima reference"
fileprivate let notiCouldString:String = "return category further sinkgGift"

/*: "MF:ChatMentionMsg" :*/
fileprivate let user_exMsg:String = "reserve fluid characterize scholarMF:C"
fileprivate let user_persistPath:String = "entionMsgavoid sun evolution related square"

/*: "uid" :*/
fileprivate let user_introAppealName:[UInt8] = [0xd,0x11,0x1c]

private func panMinimum(sub num: UInt8) -> UInt8 {
    return num ^ 120
}

/*: "onlineStatus" :*/
fileprivate let appReadyName:[UInt8] = [0xf8,0xf9,0xfb,0xfe,0xf9,0xf2,0xc4,0xe3,0xf6,0xe3,0xe2,0xe4]

private func embraceChild(color num: UInt8) -> UInt8 {
    return num ^ 151
}

/*: "isNewUser" :*/
fileprivate let data_assistBeanFormat:[UInt8] = [0x8c,0x96,0xab,0x80,0x92,0xb0,0x96,0x80,0x97]

private func pastRole(media num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "c2c_ :*/
fileprivate let const_himData:[Character] = ["c","2","c","_"]

/*: "IM获取会话失败，convId: :*/
fileprivate let data_infoData:String = "IM\u{83b7}取会\u{8bdd}\u{5931}"
fileprivate let const_exampleString:String = "\u{8d25}"
fileprivate let noti_mediumName:String = "topic column missing，con"

/*: , code: :*/
fileprivate let const_mmSupportPath:String = ", codepost height tea foundation this"
fileprivate let appFlagTailValue:String = ":"

/*: "IM插入消息失败，targetId: :*/
fileprivate let mainPortraitPath:[Character] = ["I","M","插","入","消","\u{606f}","\u{5931}","\u{8d25}"]
fileprivate let showFlowTitle:[Character] = ["\u{ff0c}"]
fileprivate let show_tingMessage:[Character] = ["t","a","r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let noti_graySequenceMessage:String = "ccolornge"
fileprivate let data_giftData:String = "CTION"

/*: "rYMsgType" :*/
fileprivate let const_securityValue:String = "bind busy normally equivalentrYMsgT"
fileprivate let user_visitName:String = "YPE"

/*: "GJ:CallCustom" :*/
fileprivate let showAvoidOccurTitle:String = "similar whiteGJ:C"
fileprivate let data_appearMessage:String = "tolerancetom"

/*: "IM获取会话列表失败，code: :*/
fileprivate let app_containFurtherTitle:[Character] = ["I","M","\u{83b7}","取","会","话","列","表","失","败","\u{ff0c}","c"]
fileprivate let mainTransitionAlwaysKey:[Character] = ["o","d","e",":"]

/*: "msgInfo" :*/
fileprivate let dataLaunchString:String = "msgInfoassociated telescope girl capture replacement"

/*: "needFold" :*/
fileprivate let k_iconPassUrl:String = "noptionoptiond"

/*: "group_ :*/
fileprivate let user_ravelName:[Character] = ["g","r","o","u","p","_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let notiStackAmKey:String = "IM删除会话失green party upon drop green"
fileprivate let app_sureMessage:String = "败，conmerge spread norm length"

/*: "IM置顶会话失败，convId: :*/
fileprivate let app_besideData:[Character] = ["I","M","置","\u{9876}","\u{4f1a}","\u{8bdd}","失","败","，"]
fileprivate let kAspectStr:[Character] = ["c","o","n","v","I","d",":"]

/*: "SDK 正在连接到服务器" :*/
fileprivate let const_carrierString:[Character] = ["S","D","K"," ","正","在"]
fileprivate let constCompareId:String = "连\u{63a5}到服务器"

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let data_chartOccurPublishFormat:String = "specify besideSDK 已"
fileprivate let main_multipleKey:String = "\u{670d}务器"

/*: "IM连接服务器失败，code: :*/
fileprivate let showSumerestedRateMethodKey:String = "IM连接服务器he sample"
fileprivate let data_formName:String = "失败，code:library fore"

/*: , error: :*/
fileprivate let kEnthusiasmStr:[Character] = [","," ","e","r","r","o","r",":"]

/*: "IM账号被挤下线。" :*/
fileprivate let main_lengthValue:[Character] = ["I","M","账","\u{53f7}","被","\u{6324}","下","\u{7ebf}","\u{3002}"]

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let notiFreshQuitUrl:[UInt8] = []

/*: "Offline Notification" :*/
fileprivate let app_sampleId:String = "dismiss panel plus situationOffline "
fileprivate let notiSeekSomethingMessage:String = "n"
fileprivate let appTeaTitle:String = "otmakef"

/*: "Cancel" :*/
fileprivate let k_goPath:String = "Cancelown language him conversion"

/*: "Re Login" :*/
fileprivate let user_levelOnStr:String = "Re toward pro skirt before flex"
fileprivate let const_officialMsg:String = "provider rid send lawmakerLogin"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WithinMargin.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol FactorExit: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func feedBy(data: [SpeedModel])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func flag(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func phone(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum BlackLock {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class WithinMargin: NSObject {
@objcMembers public class WithinMargin: NSObject {
    //: public static let shared = WithinMargin()
    public static let shared = WithinMargin()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [SpeedModel] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [SpeedModel] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [SpeedModel] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [SpeedModel] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [SpeedModel] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: SpeedModel]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.future()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func lengthInner() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(noti_sizePath, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func future() {
        //: func__removeAllDelegate()
        request()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.accelerateStatus()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func accelerateStatus() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = SpeedModel.shadowMargin(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = SpeedModel.shadowMargin(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func convert() -> [SpeedModel] {
        // 审核模式，在消息列表移除官方客服
        //: if QuantityeractionDelay.share.appStatus == AppSkinStatus.special.rawValue {
        if QuantityeractionDelay.share.appStatus == TotalervalObserve.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == MacroDefine.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == VersusQuit.beginSpend() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension WithinMargin {
extension WithinMargin {
    //: func func__removeAllDelegate() {
    func request() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func implementation(_ delegate: FactorExit) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func doing(_ delegate: FactorExit) {
        //: self.func__invokeDelegate {
        self.sign {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func sign(_ invocation: (FactorExit) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! FactorExit)
        }
    }
}

// MARK: - IM登录态管理

//: extension WithinMargin {
extension WithinMargin {
    /// 退出登录IM
    //: func func__LogingOut() {
    func extend() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            MaximumMaster.circleStat(msg: (showUponStr + String(const_temporaryMessage)) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.future()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func compare(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = QuantityeractionDelay.share.appUserConfigMode.uSig
        let uSigStr = QuantityeractionDelay.share.appUserConfigMode.uSig
        //: let uidStr = QuantityeractionDelay.share.loginUserMode.userID
        let uidStr = QuantityeractionDelay.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.fitResistance()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            MaximumMaster.circleStat(msg: (String(kEvenMessage) + kTarTheMessage + String(main_platStr.suffix(5))) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func servicePic() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || QuantityeractionDelay.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || QuantityeractionDelay.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.paraDown(showMsg: showCountensityPath)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension WithinMargin {
extension WithinMargin {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func fitResistance() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            MaximumMaster.circleStat(msg: (String(mainDecideString) + String(userVersionUrl)) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func container() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        AgainstListen.avoid { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(user_easternValue.replacingOccurrences(of: "document", with: "cr") + "hBadge")] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: k_selectionEnableEffectTitle, object: self, userInfo: [String(bytes: app_paintValue.map{housingTab(texture: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func evolution(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        AgainstListen.capClear { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(user_quitUponString))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(const_decreaseValue))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = SpeedModel.shadowMargin(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(user_distinctionId))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(const_willWayString.suffix(5)) + "neRoomId")] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension WithinMargin: V2TIMAdvancedMsgListener {
extension WithinMargin: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT")) { // 开播 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(show_beComprehensiveUrl.prefix(5)) + "CHAT"))) { // 开播 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            WillProCommon.enablee().couldOrangeReliability(msg: msg)
            //: return
            return
        }
        //: if msg.sender == QuantityeractionDelay.share.loginUserMode.userID { return }
        if msg.sender == QuantityeractionDelay.share.loginUserMode.userID { return }

        //: let dic = CharacteristicDetail.parseTXMessageData(data: msg.customElem.data)
        let dic = CharacteristicDetail.nearPrevious(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(constPressurePhaseMessage.replacingOccurrences(of: "original", with: "xt"))] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? SceneModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: main_flexFormat, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(k_customCountId))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(show_agoExistingFormat.prefix(5)) + String(notiCouldString.suffix(5))) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: user_againstTitle, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if CharacteristicDetail.checkVoiceMessage(message: msg) {
            if CharacteristicDetail.pastDaily(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = ChainSample.tweenGreet(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                PanMarker.deployBottom(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.numberensity(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            LostSubtle.shared.bold(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            LostSubtle.shared.afterCondition(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(user_exMsg.suffix(4)) + "hatM" + String(user_persistPath.prefix(9))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            DrawingBloc.share.erase(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: ThreadSpell.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: ThreadSpell.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != MacroDefine.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != VersusQuit.noneConsume() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.sign { $0.phone?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            LitePromise.shared.anConsumptionOutside(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: user_introAppealName.map{panMinimum(sub: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: appReadyName.map{embraceChild(color: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: data_assistBeanFormat.map{pastRole(media: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: constSampleTakeMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            FinishAssociated.innerWill(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            ThreadSpell.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: userEnabletoNearbySkirtKey, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: FlexBridge, isPrivete: Bool) {
    @objc public func duringAll(targetId: String, msgCellData: FlexBridge, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.myBox(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(const_himData)) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.version(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.yearImage(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
//                MaximumMaster.circleStat(msg: (data_infoData + const_exampleString + String(noti_mediumName.suffix(4)) + "vId:") + "\(convId)" + (String(const_mmSupportPath.prefix(6)) + appFlagTailValue.capitalized) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func myBox(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = QuantityeractionDelay.share.loginUserMode.userID
        let senderStr = QuantityeractionDelay.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
//                MaximumMaster.circleStat(msg: (String(mainPortraitPath) + String(showFlowTitle) + String(show_tingMessage)) + "\(targetId)" + (String(const_mmSupportPath.prefix(6)) + appFlagTailValue.capitalized) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func numberensity(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(noti_graySequenceMessage.replacingOccurrences(of: "color", with: "ha") + "Dire" + data_giftData.lowercased())].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && TellEnd.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(const_securityValue.suffix(6)) + user_visitName.lowercased())].stringValue == (String(showAvoidOccurTitle.suffix(4)) + "allC" + data_appearMessage.replacingOccurrences(of: "tolerance", with: "us")) && isSelf && TellEnd.share.tillFill()!.isKind(of: BarViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: kMarginAgileKey) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: kMarginAgileKey)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension WithinMargin: V2TIMConversationListener {
extension WithinMargin: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.version(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.yearImage(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.version(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.yearImage(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: QuantityeractionDelay.share.unreadMessageNum = Int(totalUnreadCount)
        QuantityeractionDelay.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.sign {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.flag?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func yearImage(conVersationList: [SpeedModel]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if FinishAssociated.belowSession().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                FinishAssociated.belowSession().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.sign {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.feedBy?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func resistance(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [SpeedModel] = []

        // V2TIMConversation->SpeedModel
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = SpeedModel(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.followPersist(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [SpeedModel] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = OutsideMode.findUsed(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.orientation(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension WithinMargin {
extension WithinMargin {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func labDownDraw(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.resistance(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.yearImage(conVersationList: arr as? [SpeedModel] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
            MaximumMaster.circleStat(msg: (String(app_containFurtherTitle) + String(mainTransitionAlwaysKey)) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func bringProduct(targetId: String) -> SpeedModel? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func version(convList: [V2TIMConversation]) -> [SpeedModel] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [SpeedModel] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = SpeedModel(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.followPersist(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.orientation(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func orientation(convModel: SpeedModel, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.accelerateStatus()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: BlackLock = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - QuantityeractionDelay.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - QuantityeractionDelay.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = FinishAssociated.resumeQuery(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = CharacteristicDetail.parseTXMessageData(data: elemData)
            let dict = CharacteristicDetail.nearPrevious(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(constPressurePhaseMessage.replacingOccurrences(of: "original", with: "xt"))][(String(dataLaunchString.prefix(7)))][(k_iconPassUrl.replacingOccurrences(of: "option", with: "e") + "Fold")].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.bot(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.bot(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func bot(convList: [SpeedModel], dataType: BlackLock) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func calendar(targetId: String, dataType: BlackLock) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(const_himData)) + "\(targetId)" : (String(user_ravelName)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            FinishAssociated.nearTo(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(QuantityeractionDelay.share.loginUserMode.userID, toUid: targetId)
            PanMarker.divideLite(QuantityeractionDelay.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.fitResistance()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
//            MaximumMaster.circleStat(msg: (String(notiStackAmKey.prefix(7)) + String(app_sureMessage.prefix(5)) + "vID:") + "\(convID)" + (String(const_mmSupportPath.prefix(6)) + appFlagTailValue.capitalized) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func random(convModel: SpeedModel, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
//            MaximumMaster.circleStat(msg: (String(app_besideData) + String(kAspectStr)) + "\(convModel.conversationID)" + (String(const_mmSupportPath.prefix(6)) + appFlagTailValue.capitalized) + "\(code)" + (String(showCellKey.prefix(7))) + "\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.bot(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.bot(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension WithinMargin: V2TIMSDKListener {
extension WithinMargin: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(const_carrierString) + constCompareId))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(data_chartOccurPublishFormat.suffix(5)) + "经成功连接\u{5230}" + main_multipleKey))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        MaximumMaster.circleStat(msg: (String(showSumerestedRateMethodKey.prefix(7)) + String(data_formName.prefix(8))) + "\(code)" + (String(kEnthusiasmStr)) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        MaximumMaster.circleStat(msg: (String(main_lengthValue)))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        UpZone.shared.startSpeed(userLoginOut: true)
        //: WithinMargin.shared.isConnection = false
        WithinMargin.shared.isConnection = false

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: notiFreshQuitUrl.map{$0^241}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = SignatureAssociate()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if ImageClear.share.interfaceLang == ColorSophisticated.ar.rawValue || ImageClear.share.interfaceLang == ColorSophisticated.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            ImageClear.share.interfaceLang == ColorSophisticated.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.wantStream(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        TimingVersus.noEntityGuide()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        TimingVersus.noEntityGuide(title: (String(app_sampleId.suffix(8)) + notiSeekSomethingMessage.uppercased() + appTeaTitle.replacingOccurrences(of: "make", with: "i") + "ication").localized, message: msg, leftBtnTitle: (String(k_goPath.prefix(6))).localized, rightBtnTitle: (String(user_levelOnStr.prefix(3)) + String(const_officialMsg.suffix(5))).localized, leftBlock: {
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            BarFraction.fileReload { t in
                //: ProgressHUD.dismiss()
                HandleEquipment.element()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: main_matchTitle, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            BarFraction.saveSend { t in
                //: if t {
                if t {
                    //: WithinMargin.shared.func__LogingIn { _ in
                    WithinMargin.shared.compare { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if RedWith.atPerform().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(RedWith.atPerform().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    UpZone.shared.behindProvide()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
