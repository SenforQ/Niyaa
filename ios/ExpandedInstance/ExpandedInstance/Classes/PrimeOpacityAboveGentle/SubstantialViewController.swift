
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_weightWrapFormat:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Load..." :*/
fileprivate let appCalculateTitle:String = "Load...gesture only"

/*: "Skip" :*/
fileprivate let noti_mValue:String = "permission falseSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let const_attachMsg:[UInt8] = [0x3a,0x6,0x6,0x2,0x26,0x1d,0x1d,0x1e,0x5c,0x3c,0x13,0x6,0x1b,0x4,0x17,0x26,0x1d,0x38,0x1,0x5a,0x55,0x0,0x17,0x11,0x1a,0x13,0x0,0x15,0x17,0x55,0x5b]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let userSignalData:[UInt8] = [0x6c,0x72,0x75,0x7f,0x74,0x6c,0x35,0x74,0x75,0x4b,0x7a,0x7c,0x7e,0x48,0x73,0x74,0x6c,0x3d,0x3d,0x74,0x75,0x4b,0x7a,0x7c,0x7e,0x48,0x73,0x74,0x6c,0x33,0x32,0x20]

/*: "jsEvent(onPageShow):  :*/
fileprivate let user_speedValue:[Character] = ["j","s","E"]
fileprivate let dataCosStr:String = "heart processvent(o"
fileprivate let showSeaCertainData:String = "man protocol needShow): "

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let constDarkName:[UInt8] = [0xdb,0xcd,0xd2,0xc8,0xd3,0xdb,0x92,0xd3,0xd2,0xb4,0xc5,0xcb,0xc9,0xac,0xcd,0xc8,0xc9,0x8a,0x8a,0xd3,0xd2,0xb4,0xc5,0xcb,0xc9,0xac,0xcd,0xc8,0xc9,0x8c,0x8d,0x9f]

fileprivate func tenseOnly(complaint num: UInt8) -> UInt8 {
    let value = Int(num) + 156
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageHide):  :*/
fileprivate let user_arrayMicId:String = "jsEvenfirst heavy deploy any monster"
fileprivate let kConversionUrl:String = "exactly fade contact affectt(onPa"
fileprivate let noti_appropriateKey:String = "orange button density action heapde): "

/*: "title" :*/
fileprivate let constSoundWholeString:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let show_contextMaximumFormat:String = "canGoactual speed terms comply subject"

/*: "PHPSESSID= :*/
fileprivate let main_wordFormat:[Character] = ["P","H","P","S","E","S","S","I"]
fileprivate let noti_gestureData:String = "gray suspend indeed secure averageD="

/*: ";uid= :*/
fileprivate let user_actorUrl:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let main_regionStr:[Character] = ["C","o","o","k","i","e"]

/*: "nav_back_black_nor" :*/
fileprivate let main_commonRavelTitle:String = "nav_bpublish body upon"
fileprivate let main_agoName:String = "blclusterk"
fileprivate let k_altogetherUrl:[Character] = ["_","n","o","r"]

/*: "ico_intimate_list_sm" :*/
fileprivate let dataEvaluationName:String = "ico_inbar accept integration zone invisible"
fileprivate let user_telescopeAmMessage:[Character] = ["l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let mainStrokeUrl:[UInt8] = [0x5f,0x54,0x58,0x4e,0x56,0x5e,0x55,0x4f,0x15,0x5c,0x5e,0x4f,0x7e,0x57,0x5e,0x56,0x5e,0x55,0x4f,0x48,0x79,0x42,0x6f,0x5a,0x5c,0x75,0x5a,0x56,0x5e,0x13,0x1c,0x59,0x54,0x5f,0x42,0x1c,0x12,0x60,0xb,0x66,0x15,0x48,0x4f,0x42,0x57,0x5e,0x15,0x59,0x5a,0x58,0x50,0x5c,0x49,0x54,0x4e,0x55,0x5f,0x6,0x1c,0x49,0x5c,0x59,0x5a,0x13,0xb,0x17,0xb,0x17,0xb,0x17,0xb,0x12,0x1c]

/*: "Alert" :*/
fileprivate let mainTwoInsideStr:String = "birthday protection minute retain quantityAlert"

/*: "Cancle" :*/
fileprivate let main_tensionRegionFormat:String = "Canclspell graphic hello depth"
fileprivate let const_isolatePopularPath:String = "reply"

/*: "Done" :*/
fileprivate let user_segmentFormat:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let data_productionUrl:[Character] = ["j","s"," ","c","a"]
fileprivate let k_validValue:[Character] = ["l","l"," ","m","e","t"]
fileprivate let dataPersonalData:String = "listen sovereign collection offhod na"

/*: , message =  :*/
fileprivate let user_beyondRecoverFailMsg:String = "min number, mess"
fileprivate let appEvaluationId:String = "permanent chooseage = "

/*: "http://" :*/
fileprivate let appChoiceMessage:String = "http://price environment"

/*: "https://" :*/
fileprivate let showIndeedString:String = "maximumtps"
fileprivate let mainNameConsumerId:[Character] = [":","/","/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let noti_furtherCharacterUrl:[UInt8] = [0x7a,0x6d,0x7e,0x2c,0x6f,0x63,0x63,0x67,0x65,0x69,0x42,0x6d,0x61,0x69,0x7f,0x2c,0x31,0x2c,0x68,0x63,0x6f,0x79,0x61,0x69,0x62,0x78,0x22,0x6f,0x63,0x63,0x67,0x65,0x69,0x22,0x7f,0x7c,0x60,0x65,0x78,0x24,0x2b,0x37,0x2c,0x2b,0x25,0x22,0x61,0x6d,0x7c,0x24,0x6a,0x79,0x62,0x6f,0x78,0x65,0x63,0x62,0x24,0x6f,0x63,0x63,0x67,0x65,0x69,0x25,0x2c,0x77,0x2c,0x7e,0x69,0x78,0x79,0x7e,0x62,0x2c,0x6f,0x63,0x63,0x67,0x65,0x69,0x22,0x7f,0x7c,0x60,0x65,0x78,0x24,0x2b,0x31,0x2b,0x25,0x57,0x3c,0x51,0x2c,0x71,0x2c,0x25,0x37,0x6]

private func loseRequest(bound num: UInt8) -> UInt8 {
    return num ^ 12
}

/*: "uid= :*/
fileprivate let kTensionMsg:String = "fog deadline difficult associateuid="

/*: ;domain= :*/
fileprivate let dataBetweenMessage:String = "control consequence excited reject;dom"

/*: ;path=/" :*/
fileprivate let noti_sectionDarkStr:[Character] = [";","p","a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let main_significantMessage:String = "dbeum"
fileprivate let k_beanKey:String = "ootemporaryi"
fileprivate let notiContainerUrl:String = "e=\'"

/*: ;domin= :*/
fileprivate let show_distinctiveMmUrl:String = "intensity sub gravity gate size;domin="

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let userForceTitle:[UInt8] = [0xc3,0xcc,0x8a,0x82,0xc9,0xc5,0xc5,0xc1,0xc3,0xcf,0xe4,0xcb,0xc7,0xcf,0xd9,0x84,0xc3,0xc4,0xce,0xcf,0xd2,0xe5,0xcc,0x82,0x8d,0xfa,0xe2,0xfa,0xf9,0xef,0xf9,0xf9,0xe3,0xee,0x8d,0x83,0x8a,0x97,0x97,0x8a,0x87,0x9b,0x83,0x8a,0xd1,0x8a,0xce,0xc5,0xc9,0xdf,0xc7,0xcf,0xc4,0xde,0x84,0xc9,0xc5,0xc5,0xc1,0xc3,0xcf,0x97,0x8d]

private func providerBefore(unknown num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "h5Domain =  :*/
fileprivate let user_recommendationId:String = "clearly pass output during baseh5Dom"

/*: "getAllCookies =  :*/
fileprivate let showPlayerString:String = "GET"
fileprivate let const_storageKey:[Character] = ["A","l","l","C","o","o","k","i","e","s"," ","="," "]

/*: "PHPSESSID" :*/
fileprivate let show_cigarCrushPath:[Character] = ["P","H","P"]
fileprivate let appRocketPath:String = "effEeffeffID"

/*: "uid" :*/
fileprivate let appLevelStr:String = "uiindex"

/*: "appCookies =  :*/
fileprivate let userTaskGravityId:String = "make administrativeappCook"
fileprivate let notiTelescopeDesignSumerruptTitle:String = "ies = temp enough recto visit directly"

/*: "UID" :*/
fileprivate let k_consumerPadUrl:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let k_publisherMessage:String = "nastripig"
fileprivate let const_observerId:[Character] = ["a","t","o","r","."]
fileprivate let main_tapJuniorData:String = "userAabout display like"

/*: "xunwu_iphone" :*/
fileprivate let main_takeKey:String = "accuracyunw"
fileprivate let dataContextData:String = "honenough"

/*: " xunwu_device_id/ :*/
fileprivate let main_interactionTitle:String = " xunwuuniversal shade second target fast"
fileprivate let data_bottomId:String = "reply farmer sharedid/"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let mainFromId:[Character] = ["x","u","n","w","u","_","i","p","h","o","n","e"," ","x","u"]
fileprivate let dataThreeStr:String = "nwu_vprovider sustainable"

/*:  xunwu_package/ :*/
fileprivate let k_shMsg:String = " xunwu_pclub month hold hi"
fileprivate let k_bubbleUrl:[Character] = ["a"]
fileprivate let const_happyStandardId:String = "bottom clip skip nearckage/"

/*:  xunwu_bundleId/ :*/
fileprivate let showPackString:[Character] = [" ","x","u","n","w","u","_","b","u","n","d","l","e"]
fileprivate let appHardName:String = "move sum sovereignId/"

/*:   webkit/1" :*/
fileprivate let k_liteMsg:String = "home target help publish net  web"
fileprivate let kStartData:String = "who"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SubstantialViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class SubstantialViewController: ReductionText {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: ImageTranslate!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: PlatformVolume?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: PlatformVolume?, webConfig: ImageTranslate) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_weightWrapFormat.reversed(), encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        agentOutsideHeadFractionBalance()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        shareDiscuss()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(appCalculateTitle.prefix(7)))
        //: if config.isHideBackBtn {
        if config.isHideBackBtn {
            //: hideNavi = true
            hideNavi = true
        }
        //: addNotification()
        replyDivide()
        //: func__checkConfig()
        fitMode()
        //: func__setUpWebView()
        beyondThree()
        //: func__registerObserver()
        sinkFragment()
        //: func__beginStartRequest()
        curriculum()
        //: func_checkAppleIAP()
        bringStrength()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: TellEnd.share.func__pushToWebVC(webViewType: .DiscountPopup)
                TellEnd.share.tierProcessing(webViewType: .DiscountPopup)
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: notiBoldName, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = []
        webConfig.mediaTypesRequiringUserActionForPlayback = []

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            afterHard(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = true
        w.allowsBackForwardNavigationGestures = true
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.multiSince(), .font: UIFont.alongsideOk(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(noti_mValue.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension SubstantialViewController {
    /// 添加通知
    //: private func addNotification() {
    private func replyDivide() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(drawLikeSumerrupt),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: k_computeThroughUrl,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(agentOutsideHeadFractionBalance),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func drawLikeSumerrupt() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: const_attachMsg.map{$0^114}, encoding: .utf8)!) { data, error in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func agentOutsideHeadFractionBalance() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: userSignalData.map{$0^27}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(user_speedValue) + String(dataCosStr.suffix(6)) + "nPage" + String(showSeaCertainData.suffix(7))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func shareDiscuss() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: constDarkName.map{tenseOnly(complaint: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(user_arrayMicId.prefix(6)) + String(kConversionUrl.suffix(6)) + "geHi" + String(noti_appropriateKey.suffix(5))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension SubstantialViewController {
    //: func func__registerObserver() {
    func sinkFragment() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(constSoundWholeString)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(show_contextMaximumFormat.prefix(5)) + "Back"))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(consequenceSkirt), name: k_listenFormat, object: nil)
    }

    //: func func__setUpWebView() {
    func beyondThree() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        verbExist()
        //: func__addBridgeMethod()
        deployUnique()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.boardImplement()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            HandleEquipment.sinceFollow(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(dataDoingRapidMsg)
            }
        }
    }

    //: func func__beginStartRequest() {
    func curriculum() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.old()
            }
            //: } else {
        } else {
            //: func__startRequest()
            old()
        }
    }

    //: func func__startRequest() {
    func old() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = QuantityeractionDelay.share.loginUid
        let uid = QuantityeractionDelay.share.loginUid
        //: let ssid = QuantityeractionDelay.share.loginSessionId
        let ssid = QuantityeractionDelay.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (String(main_wordFormat) + String(noti_gestureData.suffix(2))) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(user_actorUrl)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(main_regionStr)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        colonial()
        //: func__initRightButton()
        independentBroad()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func bringStrength() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            PinMake.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.rewardCanvas(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            PinMake.shared.embraceSetAnswer()
        }
    }

    //: func func__initLeftButton() {
    func directionSum() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.transferAndJoinSmall(name: (String(main_commonRavelTitle.prefix(5)) + "ack_" + main_agoName.replacingOccurrences(of: "cluster", with: "ac") + String(k_altogetherUrl))), style: .plain, target: self, action: #selector(strategyWith(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func independentBroad() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(PlatformVolume.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.transferAndJoinSmall(name: (String(dataEvaluationName.prefix(6)) + "timate_" + String(user_telescopeAmMessage))), style: .plain, target: self, action: #selector(feedbackSufficient))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func strategyWith(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        mightSpeaker()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.moreHide() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: SubstantialViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! SubstantialViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! SubstantialViewController).agentOutsideHeadFractionBalance()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func fitMode() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func sequenceDown(sucess: Bool) {
        //: ProgressHUD.dismiss()
        HandleEquipment.element()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func colonial() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: mainStrokeUrl.map{$0^59}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func sineTine(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: TellEnd.share.func__pushToWebVC(urlStr: url)
            TellEnd.share.atRein(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func characteristicDown(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func consequenceSkirt() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: k_listenFormat, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func feedbackSufficient() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = PlatformSendView(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId))
        //: view.show()
        view.overFast()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension SubstantialViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            HandleEquipment.sinceFollow(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            sequenceDown(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        directionSum()
        //: func__clearJSBgColor()
        colonial()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            sequenceDown(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            sequenceDown(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(mainTwoInsideStr.suffix(5))), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(main_tensionRegionFormat.prefix(5)) + const_isolatePopularPath.replacingOccurrences(of: "reply", with: "e")), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(user_segmentFormat)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension SubstantialViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(data_productionUrl) + String(k_validValue) + String(dataPersonalData.suffix(6)) + "me = ") + "\(message.name)" + (String(user_beyondRecoverFailMsg.suffix(6)) + String(appEvaluationId.suffix(6))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.accuseAgeRestore(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func deployUnique() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in GlobalOccasion.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func mightSpeaker() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in GlobalOccasion.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension SubstantialViewController {
    //: func func__addUserScript() {
    func projectPinion() {
        //: var htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
        var htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(appChoiceMessage.prefix(7)))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(appChoiceMessage.prefix(7))), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((showIndeedString.replacingOccurrences(of: "maximum", with: "ht") + String(mainNameConsumerId))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (showIndeedString.replacingOccurrences(of: "maximum", with: "ht") + String(mainNameConsumerId)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = QuantityeractionDelay.share.loginUserMode.userID
        let tempUid: String = QuantityeractionDelay.share.loginUserMode.userID
        //: let tempSessionID: String = QuantityeractionDelay.share.loginSessionId
        let tempSessionID: String = QuantityeractionDelay.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: noti_furtherCharacterUrl.map{loseRequest(bound: $0)}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kTensionMsg.suffix(4))) + "\(tempUid)" + (String(dataBetweenMessage.suffix(4)) + "ain=") + "\(htmlDomain)" + (String(noti_sectionDarkStr))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((main_significantMessage.replacingOccurrences(of: "be", with: "oc") + "ent.c" + k_beanKey.replacingOccurrences(of: "temporary", with: "k") + notiContainerUrl) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (String(main_wordFormat) + String(noti_gestureData.suffix(2))) + "\(tempSessionID)" + (String(show_distinctiveMmUrl.suffix(7))) + "\(htmlDomain)" + (String(noti_sectionDarkStr))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((main_significantMessage.replacingOccurrences(of: "be", with: "oc") + "ent.c" + k_beanKey.replacingOccurrences(of: "temporary", with: "k") + notiContainerUrl) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: userForceTitle.map{providerBefore(unknown: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(kTensionMsg.suffix(4))) + "\(tempUid)" + (String(dataBetweenMessage.suffix(4)) + "ain=") + "\(htmlDomain)" + (String(noti_sectionDarkStr))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((main_significantMessage.replacingOccurrences(of: "be", with: "oc") + "ent.c" + k_beanKey.replacingOccurrences(of: "temporary", with: "k") + notiContainerUrl) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(kTensionMsg.suffix(4))) + "\(tempSessionID)" + (String(dataBetweenMessage.suffix(4)) + "ain=") + "\(htmlDomain)" + (String(noti_sectionDarkStr))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((main_significantMessage.replacingOccurrences(of: "be", with: "oc") + "ent.c" + k_beanKey.replacingOccurrences(of: "temporary", with: "k") + notiContainerUrl) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func afterHard(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
            let htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(user_recommendationId.suffix(5)) + "ain = ") + "\(htmlDomain)")
            //: let userID: String = QuantityeractionDelay.share.loginUserMode.userID
            let userID: String = QuantityeractionDelay.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (showPlayerString.lowercased() + String(const_storageKey)) + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (String(show_cigarCrushPath) + appRocketPath.replacingOccurrences(of: "eff", with: "S")) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (appLevelStr.replacingOccurrences(of: "index", with: "d")), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.curriculum()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.duringGroup(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func duringGroup(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
                let htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(user_recommendationId.suffix(5)) + "ain = ") + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: ImageClear.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(userTaskGravityId.suffix(7)) + String(notiTelescopeDesignSumerruptTitle.prefix(6))) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(k_consumerPadUrl)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (appLevelStr.replacingOccurrences(of: "index", with: "d"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (String(show_cigarCrushPath) + appRocketPath.replacingOccurrences(of: "eff", with: "S")), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (String(show_cigarCrushPath) + appRocketPath.replacingOccurrences(of: "eff", with: "S"))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.curriculum()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func verbExist() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((k_publisherMessage.replacingOccurrences(of: "strip", with: "v") + String(const_observerId) + String(main_tapJuniorData.prefix(5)) + "gent")) { [weak self] result, err in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (k_publisherMessage.replacingOccurrences(of: "strip", with: "v") + String(const_observerId) + String(main_tapJuniorData.prefix(5)) + "gent"))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((main_takeKey.replacingOccurrences(of: "accuracy", with: "x") + "u_ip" + dataContextData.replacingOccurrences(of: "enough", with: "e"))) {
                //: let str = getUserAgentString()
                let str = monthCombineTask()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.queryedExpected { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(main_interactionTitle.prefix(6)) + "_device_" + String(data_bottomId.suffix(3))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func monthCombineTask() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (String(mainFromId) + String(dataThreeStr.prefix(5)) + "ersion/") + "\(userManagerKeyUrl)" + (String(k_shMsg.prefix(8)) + String(k_bubbleUrl) + String(const_happyStandardId.suffix(6))) + "\(appMarkerResultValue)" + (String(showPackString) + String(appHardName.suffix(3))) + "\(appFirstFormat)" + (String(k_liteMsg.suffix(5)) + "kit/" + kStartData.replacingOccurrences(of: "who", with: "1"))
    }
}
