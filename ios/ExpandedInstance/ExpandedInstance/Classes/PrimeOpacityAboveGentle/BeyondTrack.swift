
//: Declare String Begin

/*: "bg_message_top" :*/
fileprivate let showImageString:String = "bg_mesignore be"

/*: "777777" :*/
fileprivate let user_belowEraseId:String = "777777"

/*: "Messages" :*/
fileprivate let dataTypicalKey:[Character] = ["M","e","s","s","a","g","e","s"]

/*: "Who like me" :*/
fileprivate let notiPathName:[Character] = ["W","h","o"," ","l"]
fileprivate let appNowFamilyUrl:[Character] = ["i","k","e"," ","m","e"]

/*: "Call" :*/
fileprivate let appNativePath:[Character] = ["C","a","l","l"]

/*: "#FF2348" :*/
fileprivate let noti_actorLessMsg:String = "#FF2348bubble processor"

/*: "Current network unavailable" :*/
fileprivate let constSumPath:[Character] = ["C","u","r","r","e","n"]
fileprivate let constCharacterMsg:String = "t cloud warn dense property"
fileprivate let kPlatData:String = "nehistoryor"
fileprivate let appUniqueMmReinKey:String = "aiupperble"

/*: "icon_yidu_pre" :*/
fileprivate let appShareStr:String = "icfore"
fileprivate let showMyMsg:[Character] = ["r","e"]

/*: "transform.rotation" :*/
fileprivate let noti_successfulMsg:String = "TRANS"
fileprivate let data_skirtString:[Character] = ["f","o","r","m",".","r","o","t","a","t","i","o","n"]

/*: "transform.scale" :*/
fileprivate let user_haveBlindValue:String = "tcanvasnsfo"
fileprivate let mainFrameMsg:String = "rm.scaleput floor"

/*: "zoom&shake" :*/
fileprivate let showProductKey:String = "zoom&scomplaint wait stack new"
fileprivate let noti_scenarioMessage:String = "HAKE"

/*: "yyyy-MM-dd" :*/
fileprivate let notiAverageValue:String = "cycle labyyyy-M"

/*: "Allow %@ to send you notifications?" :*/
fileprivate let main_mountMentionMsg:[UInt8] = [0xf5,0xd8,0xd8,0xdb,0xc3,0x94,0x91,0xf4,0x94,0xc0,0xdb,0x94,0xc7,0xd1,0xda,0xd0,0x94,0xcd,0xdb,0xc1,0x94,0xda,0xdb,0xc0,0xdd,0xd2,0xdd,0xd7,0xd5,0xc0,0xdd,0xdb,0xda,0xc7,0x8b]

private func giftAutomatic(can num: UInt8) -> UInt8 {
    return num ^ 180
}

/*: "Cancel" :*/
fileprivate let data_yaWhoMsg:String = "spark destroy cheap news activeCancel"

/*: "Settings" :*/
fileprivate let constMediumPath:[Character] = ["S","e","t","t","i","n","g","s"]

/*: "badNumber" :*/
fileprivate let noti_characteristicStr:[Character] = ["b","a","d","N","u","m","b","e","r"]

/*: "isConnection" :*/
fileprivate let userOldString:String = "provision connectisCon"

/*: "networkStatus" :*/
fileprivate let app_combinedOptionFormat:[Character] = ["n","e","t","w","o"]
fileprivate let mainCityFormat:String = "maintain telescoperkStatus"

/*: "unreadMessageNum" :*/
fileprivate let appModelId:String = "uwhoea"
fileprivate let noti_translatePath:String = "event out minisageNum"

/*: "Do you want to mark all messages as read?" :*/
fileprivate let noti_amUrl:[UInt8] = [0xac,0x87,0xc8,0x91,0x87,0x9d,0xc8,0x9f,0x89,0x86,0x9c,0xc8,0x9c,0x87,0xc8,0x85,0x89,0x9a,0x83,0xc8,0x89,0x84,0x84,0xc8,0x85,0x8d,0x9b,0x9b,0x89,0x8f,0x8d,0x9b,0xc8,0x89,0x9b,0xc8,0x9a,0x8d,0x89,0x8c,0xd7]

private func sectionNumb(capture num: UInt8) -> UInt8 {
    return num ^ 232
}

/*: "消息列表一键已读失败：code: :*/
fileprivate let mainSubsequentWeekString:String = "消\u{606f}列表一"
fileprivate let noti_similarData:String = "code:dark public"

/*: , desc: :*/
fileprivate let data_saltTitle:String = ", "
fileprivate let mainTellValue:String = "desc:movement drag he plate nature"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BeyondTrack.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/20.
//

//: import UIKit
import UIKit

//: class TalkingMessageViewController: TalkingBaseViewController {
class BeyondTrack: ScaleViewController {
    //: var isNoChangeTop = false
    var isNoChangeTop = false
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var seleteIndex = 0
    private var seleteIndex = 0

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: setIsShowCleanAmimat()
        nativeOfLibrary()
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: createUI()
        associatedAcross()
        //: bindInteraction()
        question()
        //: func__turnOnSystemNotification()
        rewardDoing()
    }

    // MARK: - Lazy load

    // 背景
    //: private lazy var colorImageView: UIImageView = {
    private lazy var colorImageView: UIImageView = {
        //: let colorV = UIImageView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: 100+StatusBarNavigationBarHeight))
        let colorV = UIImageView(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: 100 + app_whenAppearanceData))
        //: colorV.image = UIImage.BundleImageNamed(name: "bg_message_top")
        colorV.image = UIImage.transferAndJoinSmall(name: (String(showImageString.prefix(6)) + "sage_top"))
        //: return colorV
        return colorV
        //: }()
    }()

    //: private lazy var noticeView: TalkingNoticeTipView = {
    private lazy var noticeView: IdentityView = {
        //: let V = TalkingNoticeTipView()
        let V = IdentityView()
        //: V.isHidden = true
        V.isHidden = true
        //: return V
        return V
        //: }()
    }()

    //: private lazy var slideVC: QYSlideNavigationViewController = {
    private lazy var slideVC: ExclusiveViewController = {
        //: let vc = QYSlideNavigationViewController()
        let vc = ExclusiveViewController()
        //: vc.topDistance = 0
        vc.topDistance = 0
        //: vc.controllerItems = controllerItems
        vc.controllerItems = controllerItems
        //: vc.titleItems = titleItems
        vc.titleItems = titleItems
        //: vc.rightSpace = 60
        vc.rightSpace = 60
        //: vc.titleViewBackgroundColor = .clear
        vc.titleViewBackgroundColor = .clear
        //: vc.titleFont = .pingfangFont(type: .Medium, fontSize: 16)
        vc.titleFont = .alongsideOk(type: .Medium, fontSize: 16)
        //: vc.selectTitleFont = .pingfangFont(type: .Medium, fontSize: 18)
        vc.selectTitleFont = .alongsideOk(type: .Medium, fontSize: 18)
        //: vc.normalColor = UIColor(hex: "777777")!
        vc.normalColor = UIColor(hex: (user_belowEraseId.capitalized))!
        //: vc.selectedColor = UIColor.appTitleColor()
        vc.selectedColor = UIColor.thinOf()
        //: vc.hasBttomTabBar = true
        vc.hasBttomTabBar = true
        //: vc.delegate = self
        vc.delegate = self
        //: vc.scrollEnabled = false
        vc.scrollEnabled = false
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var controllerItems: [TalkingBaseViewController] = {
    private lazy var controllerItems: [ScaleViewController] = {
        //: var arr = [self.chatListVC, TalkingWhoLikeViewController()]
        var arr = [self.chatListVC, PageTask()]
        //: if QuantityeractionDelay.share.loginUserMode.callTabSwitch == 1 {
        if QuantityeractionDelay.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert(TalkingVideoCallVC(), at: arr.count-1)
            arr.insert(DeployLabel(), at: arr.count - 1)
        }
        //: return arr
        return arr

        //: }()
    }()

    //: private lazy var chatListVC: TalkingChatListViewController = {
    private lazy var chatListVC: MovementSkirt = {
        //: let vc = TalkingChatListViewController()
        let vc = MovementSkirt()
        //: return vc
        return vc
        //: }()
    }()

    //: private lazy var titleItems: [String] = {
    private lazy var titleItems: [String] = {
        //: var arr = ["Messages".localized, "Who like me".localized]
        var arr = [(String(dataTypicalKey)).localized, (String(notiPathName) + String(appNowFamilyUrl)).localized]
        //: if QuantityeractionDelay.share.loginUserMode.callTabSwitch == 1 {
        if QuantityeractionDelay.share.loginUserMode.callTabSwitch == 1 {
            //: arr.insert("Call".localized, at: arr.count-1)
            arr.insert((String(appNativePath)).localized, at: arr.count - 1)
        }
        //: return arr
        return arr
        //: }()
    }()

    // 断网提醒
    //: private lazy var netTipLabel: UILabel = {
    private lazy var netTipLabel: UILabel = {
        //: let tipView = UILabel.init(frame: CGRect(x: 0, y: StatusBarNavigationBarHeight, width: ScreenWidth, height: 30))
        let tipView = UILabel(frame: CGRect(x: 0, y: app_whenAppearanceData, width: dataTabUrl, height: 30))
        //: tipView.font = UIFont.pingfangRugularFont(fontSize: 14)
        tipView.font = UIFont.botLocal(fontSize: 14)
        //: tipView.textColor = .white
        tipView.textColor = .white
        //: tipView.backgroundColor = UIColor.init(hex: "#FF2348")?.withAlphaComponent(0.8)
        tipView.backgroundColor = UIColor(hex: (String(noti_actorLessMsg.prefix(7))))?.withAlphaComponent(0.8)
        //: tipView.textAlignment = .center
        tipView.textAlignment = .center
        //: tipView.text = "Current network unavailable".localized
        tipView.text = (String(constSumPath) + String(constCharacterMsg.prefix(2)) + kPlatData.replacingOccurrences(of: "history", with: "tw") + "k unav" + appUniqueMmReinKey.replacingOccurrences(of: "upper", with: "la")).localized
        //: tipView.isHidden = true
        tipView.isHidden = true
        //: return tipView
        return tipView
        //: }()
    }()

    // message红点角标
    //: private lazy var msgNumLabel: BadgeLab = {
    private lazy var msgNumLabel: ReloadImplementation = {
        //: let numbLabel = BadgeLab()
        let numbLabel = ReloadImplementation()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // whoLikeMe红点角标
    //: private lazy var badgLB: BadgeLab = {
    private lazy var badgLB: ReloadImplementation = {
        //: let numbLabel = BadgeLab()
        let numbLabel = ReloadImplementation()
        //: numbLabel.isHidden = true
        numbLabel.isHidden = true
        //: return numbLabel
        return numbLabel
        //: }()
    }()

    // 一键清除
    //: private lazy var cleanBtn: UIButton = {
    private lazy var cleanBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.backgroundColor = .clear
        btn.backgroundColor = .clear
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .normal)
        btn.setImage(UIImage.transferAndJoinSmall(name: (appShareStr.replacingOccurrences(of: "fore", with: "on") + "_yidu_p" + String(showMyMsg))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_yidu_pre"), for: .highlighted)
        btn.setImage(UIImage.transferAndJoinSmall(name: (appShareStr.replacingOccurrences(of: "fore", with: "on") + "_yidu_p" + String(showMyMsg))), for: .highlighted)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - CurrentChart

//: extension TalkingMessageViewController: QYSlideNavigationDelegate {
extension BeyondTrack: CurrentChart {
    //: func selectedViewController(_ viewController: UIViewController, index: Int) {
    func data(_ viewController: UIViewController, index: Int) {
        //: self.seleteIndex = Int(index)
        self.seleteIndex = Int(index)
        //: if viewController.isKind(of: TalkingChatListViewController.self) {
        if viewController.isKind(of: MovementSkirt.self) {
            //: self.cleanBtn.isHidden = false
            self.cleanBtn.isHidden = false
            //: setIsShowCleanAmimat()
            nativeOfLibrary()

            //: } else if viewController.isKind(of: TalkingWhoLikeViewController.self) {
        } else if viewController.isKind(of: PageTask.self) {
            //: self.cleanBtn.isHidden = true
            self.cleanBtn.isHidden = true
            //: uploadRecord.uploadRecordEvent(eventID: ClickWLikeMeTabNoP)
            k_playShadowValue.behindImplement(eventID: main_headInputMsg)
        }
    }
}

// MARK: - Event

//: extension TalkingMessageViewController {
extension BeyondTrack {
    //: func setIsTopAll() {
    func heroLocation() {
        /// 默认选中all
        //: if seleteIndex == 0 {
        if seleteIndex == 0 {
            //: let vc = slideVC.controllerItems.first as? TalkingChatListViewController
            let vc = slideVC.controllerItems.first as? MovementSkirt
            //: vc?.resetToTopItemView()
            vc?.towardDivide()
            // 刷新在线状态
            //: vc?.refreshOnlineStatus(isFirst: true)
            vc?.duringFoundInput(isFirst: true)
        }
    }

    /// 无网络提示
    //: func func__checkNetTipState() {
    func anButt() {
        //: if QuantityeractionDelay.share.networkStatus != .Unavailable && WithinMargin.shared.isConnection {
        if QuantityeractionDelay.share.networkStatus != .Unavailable, WithinMargin.shared.isConnection {
            //: netTipLabel.isHidden = true
            netTipLabel.isHidden = true
            //: } else {
        } else {
            //: netTipLabel.isHidden = false
            netTipLabel.isHidden = false
        }
    }

    /// message红点
    //: func func__messagebadgLBState(num: Int) {
    func destroyPolicy(num: Int) {
        //: if num > 0 {
        if num > 0 {
            //: msgNumLabel.isHidden = false
            msgNumLabel.isHidden = false
            //: if num > 99 {
            if num > 99 {
                //: msgNumLabel.text = "99+"
                msgNumLabel.text = "99+"
                //: } else {
            } else {
                //: msgNumLabel.text = String(num)
                msgNumLabel.text = String(num)
            }
            //: } else {
        } else {
            //: msgNumLabel.isHidden = true
            msgNumLabel.isHidden = true
        }
    }

    /// 一键清除动画
    //: func setIsShowCleanAmimat() {
    func nativeOfLibrary() {
        //: let unreadMsgCount = QuantityeractionDelay.share.unreadMessageNum
        let unreadMsgCount = QuantityeractionDelay.share.unreadMessageNum
        //: let isClean: Bool = UserDefaults.standard.object(forKey: isShowCleanAnimatCacheKey) as? Bool ?? false
        let isClean: Bool = UserDefaults.standard.object(forKey: user_squareMsg) as? Bool ?? false
        //: if unreadMsgCount >= 10 && !isClean {
        if unreadMsgCount >= 10, !isClean {
            //: self.cleanBtnBtnAnimat()
            self.targetDoingce()
        }
    }

    //: func cleanBtnBtnAnimat() {
    func targetDoingce() {
        //: let degreesToRadians = 180.0 * .pi
        let degreesToRadians = 180.0 * .pi
        //: let angle1 = 0 / degreesToRadians
        let angle1 = 0 / degreesToRadians
        //: let angle2 = 100 / degreesToRadians
        let angle2 = 100 / degreesToRadians
        //: let angle3 = -100 / degreesToRadians
        let angle3 = -100 / degreesToRadians
        //: let angle4 = 100 / degreesToRadians
        let angle4 = 100 / degreesToRadians
        //: let angle5 = -100 / degreesToRadians
        let angle5 = -100 / degreesToRadians
        //: let angle6 = 0 / degreesToRadians
        let angle6 = 0 / degreesToRadians

        //: let du1: NSNumber = 0
        let du1: NSNumber = 0
        //: let du2: NSNumber = 0.13
        let du2: NSNumber = 0.13
        //: let du3: NSNumber = 0.3
        let du3: NSNumber = 0.3
        //: let du4: NSNumber = 0.47
        let du4: NSNumber = 0.47
        //: let du5: NSNumber = 0.8
        let du5: NSNumber = 0.8
        //: let du6: NSNumber = 1.0
        let du6: NSNumber = 1.0

        // 旋转
        //: let roate = CAKeyframeAnimation.init(keyPath: "transform.rotation")
        let roate = CAKeyframeAnimation(keyPath: (noti_successfulMsg.lowercased() + String(data_skirtString)))
        //: roate.values = [(angle1), (angle2), (angle3), (angle4), (angle5), (angle6)]
        roate.values = [angle1, angle2, angle3, angle4, angle5, angle6]
        //: roate.keyTimes = [(du1), (du2), (du3), (du4), (du5), (du6)]
        roate.keyTimes = [du1, du2, du3, du4, du5, du6]

        // 缩放
        //: let zoom = CAKeyframeAnimation.init(keyPath: "transform.scale")
        let zoom = CAKeyframeAnimation(keyPath: (user_haveBlindValue.replacingOccurrences(of: "canvas", with: "ra") + String(mainFrameMsg.prefix(8))))
        //: zoom.values = [(1.0), (1.2), (1.0)]
        zoom.values = [1.0, 1.2, 1.0]
        //: zoom.keyTimes = [(0), (0.5), (1.0)]
        zoom.keyTimes = [0, 0.5, 1.0]

        //
        //: let groupAnnimation = CAAnimationGroup.init()
        let groupAnnimation = CAAnimationGroup()
        //: groupAnnimation.autoreverses = false
        groupAnnimation.autoreverses = false
        //: groupAnnimation.animations = [roate, zoom]
        groupAnnimation.animations = [roate, zoom]
        //: groupAnnimation.duration = 1.0
        groupAnnimation.duration = 1.0
        //: groupAnnimation.repeatCount = 2
        groupAnnimation.repeatCount = 2
        //: groupAnnimation.isRemovedOnCompletion = false
        groupAnnimation.isRemovedOnCompletion = false
        //: groupAnnimation.fillMode = .forwards
        groupAnnimation.fillMode = .forwards
        //: cleanBtn.layer.add(groupAnnimation, forKey: "zoom&shake")
        cleanBtn.layer.add(groupAnnimation, forKey: (String(showProductKey.prefix(6)) + noti_scenarioMessage.lowercased()))
    }

    /// 检查系统通知是否开启
    //: private func func__turnOnSystemNotification() {
    private func rewardDoing() {
        //: TalkingPermissionTool.checkPushNotification { isEnable in
        StrengthMomentum.searchMouse { isEnable in

            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: if isEnable {
                if isEnable {
                    //: self.noticeView.isHidden = true
                    self.noticeView.isHidden = true
                    //: self.func__hideNotificationTipView(hide: true)
                    self.haveInAgainst(hide: true)
                    //: }else{
                } else {
                    /// 页面顶部提示
                    //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
                    let today = NSDate.messageLarge(date: Date(), dateFormat: (String(notiAverageValue.suffix(6)) + "M-dd"))
                    //: if let closeDateStr = Defaults.string(forKey: TalkingMessageTabCloseNotificationsKey), today == closeDateStr {
                    if let closeDateStr = app_pressureId.string(forKey: k_builderOutputName), today == closeDateStr {
                        //: self.func__hideNotificationTipView(hide: true)
                        self.haveInAgainst(hide: true)
                        //: }else{
                    } else {
                        //: self.func__hideNotificationTipView(hide: false)
                        self.haveInAgainst(hide: false)
                    }
                    /// 弹框逻辑
                    //: let isShow = Defaults.bool(forKey: TalkingMessageTabTurnOnNotificationsKey)
                    let isShow = app_pressureId.bool(forKey: noti_whiteFormat)
                    //: if !isShow {
                    if !isShow {
                        //: Defaults.set(true, forKey: TalkingMessageTabTurnOnNotificationsKey)
                        app_pressureId.set(true, forKey: noti_whiteFormat)
                        //: TalkingAlertShow.alert(title: nil,
                        TimingVersus.noEntityGuide(title: nil,
                                                       //: message: "Allow %@ to send you notifications?".localizedArguments(AppName),
                                                       message: String(bytes: main_mountMentionMsg.map{giftAutomatic(can: $0)}, encoding: .utf8)!.underConnection(notiCaptureMsg),
                                                       //: leftBtnTitle: "Cancel".localized,
                                                       leftBtnTitle: (String(data_yaWhoMsg.suffix(6))).localized,
                                                       //: rightBtnTitle: "Settings".localized) {
                                                       rightBtnTitle: (String(constMediumPath)).localized)
                        {
                            //: TalkingAlertShow.hideAlert()
                            TimingVersus.stripNow()
                            //: return
                            //: } rightBlock: {
                        } rightBlock: {
                            //: if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                            if let settingsUrl = URL(string: UIApplication.openSettingsURLString) {
                                //: UIApplication.shared.open(settingsUrl)
                                UIApplication.shared.open(settingsUrl)
                            }
                        }
                    }
                }
            }
        }
    }

    //: private func func__hideNotificationTipView(hide: Bool) {
    private func haveInAgainst(hide: Bool) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: if hide{
            if hide {
                //: self.noticeView.isHidden = true
                self.noticeView.isHidden = true
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight)
                    make.top.equalToSuperview().offset(dataDoingRapidMsg)
                }
                //: self.slideVC.viewHeight = ScreenHeight - StatusBarHeight - TabBarViewHeight
                self.slideVC.viewHeight = const_rebuildWithinId - dataDoingRapidMsg - user_chainFormat
                //: }else{
            } else {
                //: self.noticeView.isHidden = false
                self.noticeView.isHidden = false
                //: self.slideVC.view.snp.updateConstraints { make in
                self.slideVC.view.snp.updateConstraints { make in
                    //: make.top.equalToSuperview().offset(StatusBarHeight + self.noticeView.viewHeight)
                    make.top.equalToSuperview().offset(dataDoingRapidMsg + self.noticeView.viewHeight)
                }
                //: self.slideVC.viewHeight = ScreenHeight - self.noticeView.bottom - TabBarViewHeight
                self.slideVC.viewHeight = const_rebuildWithinId - self.noticeView.bottom - user_chainFormat
            }
        }
    }
}

// MARK: - 通知

//: extension TalkingMessageViewController {
extension BeyondTrack {
    /// who like me 红点通知
    //: @objc func showbadgNumberNotif(notification: NSNotification) {
    @objc func thinTimingSkip(notification: NSNotification) {
        //: let userinfo = notification.userInfo as![String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let badNumber: Int  = userinfo["badNumber"] as! Int
        let badNumber: Int = userinfo[(String(noti_characteristicStr))] as! Int
        //: badgLB.isHidden = false
        badgLB.isHidden = false
        //: if badNumber > 99 {
        if badNumber > 99 {
            //: badgLB.text = "99+"
            badgLB.text = "99+"
            //: } else if badNumber<=0 {
        } else if badNumber <= 0 {
            //: badgLB.isHidden = true
            badgLB.isHidden = true
            //: badgLB.text = "0"
            badgLB.text = "0"
            //: } else {
        } else {
            //: badgLB.text = String(badNumber)
            badgLB.text = String(badNumber)
        }
    }
}

// MARK: - Layout

//: extension TalkingMessageViewController {
extension BeyondTrack {
    /// UI
    //: private func createUI() {
    private func associatedAcross() {
        //: view.backgroundColor = .white
        view.backgroundColor = .white

        //: view.addSubview(colorImageView)
        view.addSubview(colorImageView)
        //: view.addSubview(noticeView)
        view.addSubview(noticeView)
        //: self.addChild(slideVC)
        self.addChild(slideVC)

        //: self.view.addSubview(slideVC.view)
        self.view.addSubview(slideVC.view)
        //: slideVC.view.backgroundColor = UIColor.clear
        slideVC.view.backgroundColor = UIColor.clear
        //: slideVC.view.snp.makeConstraints { make in
        slideVC.view.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(StatusBarHeight)
            make.top.equalToSuperview().offset(dataDoingRapidMsg)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.bottom.equalToSuperview()
            make.bottom.equalToSuperview()
        }

        //: for btn in slideVC.allBtn {
        for btn in slideVC.allBtn {
            //: let curenbrn: UIButton = btn
            let curenbrn: UIButton = btn
            //: if curenbrn.titleLabel!.text == "Who like me".localized {
            if curenbrn.titleLabel!.text == (String(notiPathName) + String(appNowFamilyUrl)).localized {
                //: (curenbrn as AnyObject).addSubview(badgLB)
                (curenbrn as AnyObject).addSubview(badgLB)
                //: badgLB.snp.makeConstraints { make in
                badgLB.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
                //: badgLB.isHidden = true
                badgLB.isHidden = true
                //: } else if (curenbrn.titleLabel!.text == "Messages".localized) {
            } else if curenbrn.titleLabel!.text == (String(dataTypicalKey)).localized {
                //: (curenbrn as AnyObject).addSubview(msgNumLabel)
                (curenbrn as AnyObject).addSubview(msgNumLabel)
                //: msgNumLabel.snp.makeConstraints { make in
                msgNumLabel.snp.makeConstraints { make in
                    //: make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    make.leading.equalTo(curenbrn.titleLabel!.snp.trailing).offset(-5)
                    //: make.top.equalTo(curenbrn).offset(7)
                    make.top.equalTo(curenbrn).offset(7)
                    //: make.height.equalTo(16)
                    make.height.equalTo(16)
                    //: make.width.greaterThanOrEqualTo(16)
                    make.width.greaterThanOrEqualTo(16)
                }
            }
        }

        //: view.addSubview(cleanBtn)
        view.addSubview(cleanBtn)
        //: cleanBtn.snp.makeConstraints { make in
        cleanBtn.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top)
            make.top.equalTo(slideVC.view.snp.top)
            //: make.trailing.equalTo(-5)
            make.trailing.equalTo(-5)
            //: make.size.equalTo(CGSize(width: 44, height: 44))
            make.size.equalTo(CGSize(width: 44, height: 44))
        }

        //: view.addSubview(netTipLabel)
        view.addSubview(netTipLabel)
        //: netTipLabel.snp.makeConstraints { make in
        netTipLabel.snp.makeConstraints { make in
            //: make.top.equalTo(slideVC.view.snp.top).offset(44)
            make.top.equalTo(slideVC.view.snp.top).offset(44)
            //: make.leading.trailing.equalToSuperview()
            make.leading.trailing.equalToSuperview()
            //: make.height.equalTo(30)
            make.height.equalTo(30)
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func question() {
        //: WithinMargin.shared.rx
        WithinMargin.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(userOldString.suffix(5)) + "nection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.anButt()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: QuantityeractionDelay.share.rx.observeWeakly(Int.self, "networkStatus")
        QuantityeractionDelay.share.rx.observeWeakly(Int.self, (String(app_combinedOptionFormat) + String(mainCityFormat.suffix(8))))
            //: .subscribe(onNext: { [weak self]  (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__checkNetTipState()
                self.anButt()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // 展示全部红点
        //: QuantityeractionDelay.share.rx.observeWeakly(Int.self, "unreadMessageNum")
        QuantityeractionDelay.share.rx.observeWeakly(Int.self, (appModelId.replacingOccurrences(of: "who", with: "nr") + "dMes" + String(noti_translatePath.suffix(7))))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let valueInt = value ?? 0
                let valueInt = value ?? 0
                //: self.func__messagebadgLBState(num: valueInt)
                self.destroyPolicy(num: valueInt)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: cleanBtn.rx.controlEvent(.touchUpInside)
        cleanBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: {
            .subscribe(onNext: {
                //: let config = ShowAlertConfig()
                let config = SignatureAssociate()
                //: config.alignment = .center
                config.alignment = .center

                //: TalkingAlertShow.customAlert(message: "Do you want to mark all messages as read?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, leftBlock: {
                TimingVersus.writtenSufficient(message: String(bytes: noti_amUrl.map{sectionNumb(capture: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(data_yaWhoMsg.suffix(6))).localized, rightBtnTitle: "OK".localized, leftBlock: {
                    //: TalkingAlertShow.hideAlert()
                    TimingVersus.stripNow()

                    //: }, rightBlock: {
                }, rightBlock: {
                    //: V2TIMManager.sharedInstance().markAllMessage {
                    V2TIMManager.sharedInstance().markAllMessage {
                        //: UserDefaults.standard.set(true, forKey: isShowCleanAnimatCacheKey)
                        UserDefaults.standard.set(true, forKey: user_squareMsg)
                        //: } fail: { code, desc in
                    } fail: { code, desc in
                        //: UploadLogTool.writeLog(msg: "消息列表一键已读失败：code:\(code), desc:\(String(describing: desc))")
//                        MaximumMaster.circleStat(msg: (mainSubsequentWeekString + "键已读失败\u{ff1a}" + String(noti_similarData.prefix(5))) + "\(code)" + (data_saltTitle.capitalized + String(mainTellValue.prefix(5))) + "\(String(describing: desc))")
                    }
                    //: }, config: config)
                }, config: config)

                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        // who like me 红点通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(showbadgNumberNotif(notification:)),
                                               selector: #selector(thinTimingSkip(notification:)),
                                               //: name: WHOLIKEME_BADGENUMBER_NOTIF,
                                               name: k_selectionEnableEffectTitle,
                                               //: object: nil)
                                               object: nil)

        //: noticeView.closeBtnBlock = { [weak self] ()  -> Void in
        noticeView.closeBtnBlock = { [weak self] () in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: let today = NSDate.getTimeString(date: Date(), dateFormat: "yyyy-MM-dd")
            let today = NSDate.messageLarge(date: Date(), dateFormat: (String(notiAverageValue.suffix(6)) + "M-dd"))
            //: Defaults.set(today, forKey: TalkingMessageTabCloseNotificationsKey)
            app_pressureId.set(today, forKey: k_builderOutputName)
            //: self.func__hideNotificationTipView(hide: true)
            self.haveInAgainst(hide: true)
        }

        //: NotificationCenter.default.rx.notification( UIApplication.willEnterForegroundNotification)
        NotificationCenter.default.rx.notification(UIApplication.willEnterForegroundNotification)
            //: .subscribe(onNext: { [weak self] (notification) in
            .subscribe(onNext: { [weak self] notification in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.func__turnOnSystemNotification()
                self.rewardDoing()
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
