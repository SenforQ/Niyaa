
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let constShStr:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "roomId" :*/
fileprivate let const_densityBlankWhoPath:[Character] = ["r","o","o","m","I","d"]

/*: "type" :*/
fileprivate let kHolderPiId:[UInt8] = [0x65,0x70,0x79,0x74]

/*: "extra" :*/
fileprivate let noti_boardData:String = "appeartra"

/*: "jumpKey" :*/
fileprivate let show_actionValue:[UInt8] = [0x79,0x65,0x4b,0x70,0x6d,0x75,0x6a]

/*: "Send Gift" :*/
fileprivate let const_pressEvolutionMessage:[Character] = ["S","e","n","d"]
fileprivate let notiInstructionMoveName:String = " Giftrender unless receive filter invite"

/*: "View info" :*/
fileprivate let constBodyData:[Character] = ["V","i","e","w"," ","i","n","f"]
fileprivate let appGravityFormat:String = "O"

/*: "Report" :*/
fileprivate let notiDivideValue:String = "tap"
fileprivate let show_existValue:[Character] = ["e","p","o","r","t"]

/*: "uid" :*/
fileprivate let data_adminProgramMessage:[UInt8] = [0x64,0x69,0x75]

/*: "name" :*/
fileprivate let showSuccessfullyValue:[UInt8] = [0x65,0x6d,0x61,0x6e]

/*: "Can't send blank message" :*/
fileprivate let kAssociatedData:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"]
fileprivate let k_mixOneMessage:String = " meroute gesture popular height"
fileprivate let user_jobChoiceName:String = "ssaforcee"

/*: "user" :*/
fileprivate let mainNatureAssetKey:String = "useroot"

/*: "content" :*/
fileprivate let kGlobalUrl:[UInt8] = [0xb5,0xb9,0xb8,0xa2,0xb3,0xb8,0xa2]

private func mmPlease(bucket num: UInt8) -> UInt8 {
    return num ^ 214
}

/*: "msgType" :*/
fileprivate let k_containUrl:[Character] = ["m","s","g","T","y","p","e"]

/*: "txt" :*/
fileprivate let user_seekSpecifyLeaveStr:String = "needt"

/*: "isGroup" :*/
fileprivate let main_bridgeUrl:[Character] = ["i","s","G","r","o"]
fileprivate let noti_fractionId:String = "nearbyp"

/*: "jumps" :*/
fileprivate let kPerformKey:String = "juindeeds"

/*: "message" :*/
fileprivate let notiHaveHimTitle:[UInt8] = [0xae,0xa6,0xb0,0xb0,0xa2,0xa4,0xa6]

private func packObject(rounding num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "TxtMsg" :*/
fileprivate let main_upPath:String = "region title infrastructureTxtMsg"

/*: "toUid" :*/
fileprivate let app_assetUrl:String = "divide version blacktoUid"

/*: "msgInfo" :*/
fileprivate let data_sableMessage:[Character] = ["m","s","g","I","n","f","o"]

/*: "params" :*/
fileprivate let userCosCombineData:[UInt8] = [0xb5,0xa4,0xb7,0xa4,0xa8,0xb6]

/*: "contentType" :*/
fileprivate let kOnerData:[UInt8] = [0x99,0x95,0x94,0x8e,0x9f,0x94,0x8e,0xae,0x83,0x8a,0x9f]

/*: "ImgMsg" :*/
fileprivate let k_backMessage:String = "feature task count herImgMsg"

/*: "img" :*/
fileprivate let kSpecialString:String = "telescopeg"

/*: "imageUri" :*/
fileprivate let kCurrentlyEnjoyString:[UInt8] = [0x69,0x72,0x55,0x65,0x67,0x61,0x6d,0x69]

/*: "pic" :*/
fileprivate let data_allowLabelTitle:String = "PIC"

/*: "msgId" :*/
fileprivate let dataContinuingData:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let k_plusPath:String = "packageo"
fileprivate let constCurrentMessage:[Character] = ["t","a","l","M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let kNeverString:String = "paint"
fileprivate let dataReportMsg:[Character] = ["i","f","t","I","d"]

/*: "giftNum" :*/
fileprivate let show_movieUrl:[Character] = ["g","i","f","t","N","u","m"]

/*: "pkgItemsetId" :*/
fileprivate let show_distributionKey:[Character] = ["p","k","g","I","t"]
fileprivate let app_gravityDragMessage:String = "decision and wed ageemsetId"

/*: "msg" :*/
fileprivate let notiWeValue:String = "MSG"

/*: "tips" :*/
fileprivate let mainSpaceStr:[Character] = ["t","i","p","s"]

/*: "gift" :*/
fileprivate let showFactoryClubValue:[Character] = ["g","i","f","t"]

/*: "nickname" :*/
fileprivate let data_sumId:String = "NICKNAME"

/*: "headPic" :*/
fileprivate let appCompareSinceData:[UInt8] = [0x8c,0x89,0x85,0x88,0x74,0x8d,0x87]

fileprivate func highlightRegularGreet(mouse num: UInt8) -> UInt8 {
    let value = Int(num) + 220
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "All Numbers" :*/
fileprivate let notiTotaloPath:[Character] = ["A","l","l"," ","N","u","m","b","e","r","s"]

/*: "sendNum" :*/
fileprivate let main_underFormat:String = "start dimension assertsendNum"

/*: "toUser" :*/
fileprivate let noti_unlessTitle:[Character] = ["t"]
fileprivate let mainPassFunKey:String = "oUserexception rocket odd m"

/*: "msgIncome" :*/
fileprivate let show_enemyValue:String = "giving"
fileprivate let showLightMiddleKey:[Character] = ["s","g","I","n","c","o","m","e"]

/*: "isConnection" :*/
fileprivate let user_sineKey:String = "timing containisConn"
fileprivate let notiGoInfoName:String = "ekindtion"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ReceiverBecome.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class ReceiverBecome: ScaleViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = DrawerThan() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = SpeedModel() // 会话cellData
    //: private var conversationType: UserChatType = .group
    private var conversationType: UserChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = DrawerThan.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: constShStr.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
            if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = SignaturePriority(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        fun()
        //: createUIConstraints()
        constantBullet()
        //: bindInteraction()
        difficult()
        //: addTapGestureRecognizer()
        banPick()
        //: func__chatRoom_insertTipMsg()
        groupLimit()
        //: chatSettings()
        shade()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            thanFore()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.askDoing(join: false)
        //: self.destroyTipsTimer()
        self.implementSubstantial()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: CountervalTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = CountervalTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(ShowJob.self, forCellReuseIdentifier: ShowJob.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(FactoryEven.self, forCellReuseIdentifier: FactoryEven.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(CloseVisitor.self, forCellReuseIdentifier: CloseVisitor.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(IndexOvalEqually.self, forCellReuseIdentifier: IndexOvalEqually.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: CeilingThan.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(WouldOn.self, forCellReuseIdentifier: CeilingThan.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: UniqueDisabledView = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = UniqueDisabledView(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: CellView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = CellView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: FragmentPublish = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = FragmentPublish()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: ClusterView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = ClusterView(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: HiddenView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = HiddenView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension ReceiverBecome {
    /// 群聊设置
    //: private func chatSettings() {
    private func shade() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        DrawingBloc.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        askDoing(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func askDoing(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(const_densityBlankWhoPath)): self.conversationData.groupID, String(bytes: kHolderPiId.reversed(), encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        AgainstListen.duringContrast(params: params) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - MasterAmongThreshold

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension ReceiverBecome: MasterAmongThreshold {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> FlexBridge! {
    func takeStorage(_ controller: CountervalTableViewController!, line data: V2TIMMessage!) -> FlexBridge! {
        //: let dic = CharacteristicDetail.parseTXMessageData(data: data.customElem.data)
        let dic = CharacteristicDetail.nearPrevious(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = ChainSample.oneSpot(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: FlexBridge!) -> CeilingThan! {
    func accelerate(_ controller: CountervalTableViewController!, rebuild data: FlexBridge!) -> CeilingThan! {
        //: if data.isKind(of: AlongMount.self) {
        if data.isKind(of: AlongMount.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = ShowJob(style: .default, reuseIdentifier: ShowJob.className())
            //: let cellData = data as! AlongMount
            let cellData = data as! AlongMount
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.databaseBy(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.alongConnectEquipment(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: AppearanceAddLock.self) {
        if data.isKind(of: AppearanceAddLock.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = FactoryEven(style: .default, reuseIdentifier: FactoryEven.className())
            //: let cellData = data as! AppearanceAddLock
            let cellData = data as! AppearanceAddLock
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: DespiteEnhance.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = CloseVisitor(style: .default, reuseIdentifier: CloseVisitor.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! DespiteEnhance
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: DenseDisplay.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = IndexOvalEqually(style: .default, reuseIdentifier: IndexOvalEqually.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! DenseDisplay
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: CeilingThan!) {
    func ownerReflect(_ controller: CountervalTableViewController!, spot cell: CeilingThan!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(QuantityeractionDelay.share.loginUid) {
            if userModel.uid == Int(QuantityeractionDelay.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                databaseBy(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                viaOperation(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: CeilingThan!) {
    func easeSpeed(_ controller: CountervalTableViewController!, root cell: CeilingThan!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: FactoryEven.self) { // 图片
            //: let imageCellData = cell.messageData as! AppearanceAddLock
            let imageCellData = cell.messageData as! AppearanceAddLock
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(show_alongMessage)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = ProcessorMargin()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = SubsequentSkip()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [ProcessorMargin] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = ResolveNorm(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.moreHide()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: ShowJob.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! ShowJob
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = CharacteristicDetail.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = CharacteristicDetail.nearPrevious(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(noti_boardData.replacingOccurrences(of: "appear", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: show_actionValue.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<BehaviorLargeThan>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TellEnd.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TellEnd.share.wrapFast(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: IndexOvalEqually.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! IndexOvalEqually
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = CharacteristicDetail.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = CharacteristicDetail.nearPrevious(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(noti_boardData.replacingOccurrences(of: "appear", with: "ex"))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: show_actionValue.reversed(), encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<BehaviorLargeThan>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TellEnd.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TellEnd.share.wrapFast(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: CeilingThan!) {
    func selectUsed(_ controller: CountervalTableViewController!, time cell: CeilingThan!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        followingAndReplyObserve(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension ReceiverBecome {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func implementSubstantial() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func viaOperation(user: EndModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = StrengthView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.fieldThreeExpectedOperationDrawer(cellTitleList: [(String(const_pressEvolutionMessage) + String(notiInstructionMoveName.prefix(5))).localized, "@", (String(constBodyData) + appGravityFormat.lowercased()).localized, (notiDivideValue.replacingOccurrences(of: "tap", with: "R") + String(show_existValue)).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.priorityInsert(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.wantAngle(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.databaseBy(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.outsideBubble(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func priorityInsert(user: EndModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.conversion()
        //: let model = TalkingChatRoomMemberModel()
        let model = AngleAdd()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.notEvolution(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func wantAngle(user: EndModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: data_adminProgramMessage.reversed(), encoding: .utf8)!: String(user.uid), String(bytes: showSuccessfullyValue.reversed(), encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.command(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func databaseBy(uid: Int) {
        //: TellEnd.share.func__pushToUserDetailVC(uid: String(uid))
        TellEnd.share.butterfly(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func outsideBubble(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = ShadowBridge(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.analyze(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension ReceiverBecome {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func everyDelicate(msgStr: String, quoteModel: VisitModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            paraDown(showMsg: (String(kAssociatedData) + String(k_mixOneMessage.prefix(3)) + user_jobChoiceName.replacingOccurrences(of: "force", with: "g")).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: show_actionValue.reversed(), encoding: .utf8)!: (mainNatureAssetKey.replacingOccurrences(of: "root", with: "r")), String(bytes: data_adminProgramMessage.reversed(), encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: kGlobalUrl.map{mmPlease(bucket: $0)}, encoding: .utf8)!: temStr, (String(k_containUrl)): (user_seekSpecifyLeaveStr.replacingOccurrences(of: "need", with: "tx")), (String(main_bridgeUrl) + noti_fractionId.replacingOccurrences(of: "nearby", with: "u")): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(kPerformKey.replacingOccurrences(of: "indeed", with: "mp"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: notiHaveHimTitle.map{packObject(rounding: $0)}, encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: kHolderPiId.reversed(), encoding: .utf8)!: (String(main_upPath.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(const_densityBlankWhoPath)): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(app_assetUrl.suffix(5)))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_sableMessage)): msgInfo, String(bytes: userCosCombineData.map{$0^197}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            followingAndReplyObserve(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func iconPush(img: UIImage) {
        //: let imagePath = AppearanceAddLock.getChatImagePath(image: img)
        let imagePath = AppearanceAddLock.offly(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: kOnerData.map{$0^250}, encoding: .utf8)!: (String(k_backMessage.suffix(6))), (String(k_containUrl)): (kSpecialString.replacingOccurrences(of: "telescope", with: "im")), String(bytes: kCurrentlyEnjoyString.reversed(), encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(data_allowLabelTitle.lowercased()): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: kHolderPiId.reversed(), encoding: .utf8)!: (String(k_backMessage.suffix(6))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(const_densityBlankWhoPath)): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(data_sableMessage)): msgInfo, String(bytes: userCosCombineData.map{$0^197}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            followingAndReplyObserve(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: FlexBridge, reSend: Bool) {
    private func followingAndReplyObserve(msgData: FlexBridge, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            paraDown(showMsg: showCountensityPath)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.identity(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((data_allowLabelTitle.lowercased())) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(data_allowLabelTitle.lowercased())] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(data_allowLabelTitle.lowercased())] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        AgainstListen.mix(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.direct(msgData, independent: nil, action: NSNumber(value: 0), purple: .Msg_Status_Fail)

                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.overRemove(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(data_sableMessage))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.direct(msgData, independent: msgInfo[(String(dataContinuingData))] as? String, action: NSNumber(value: 0), purple: .Msg_Status_Succ)
  
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((k_plusPath.replacingOccurrences(of: "package", with: "t") + String(constCurrentMessage))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(k_plusPath.replacingOccurrences(of: "package", with: "t") + String(constCurrentMessage))] as! NSNumber
                //: QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func overRemove(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard QuantityeractionDelay.share.loginUserMode.status != 1 else {
            guard QuantityeractionDelay.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    paraDown(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TellEnd.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            TellEnd.share.somePush(clickEvent: user_removeTitle, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.variable()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            run(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = MarkExtra(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId))
            //: view.show()
            view.us()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                paraDown(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension ReceiverBecome: RetainFormat {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func tension(giftModel: CombineFactor, giftNum: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func drag() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func level(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = SessionPress()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.command(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func primaryMatch(actionType: CriticalSend) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            switchly()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func written(giftModel: CombineFactor, giftNum: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func plate(giftModel: CombineFactor, giftNum: String, model: AngleAdd) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            paraDown(showMsg: showCountensityPath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(app_assetUrl.suffix(5)))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(kNeverString.replacingOccurrences(of: "paint", with: "g") + String(dataReportMsg))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(show_movieUrl))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(const_densityBlankWhoPath))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(show_distributionKey) + String(app_gravityDragMessage.suffix(7)))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        AgainstListen.biology(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.overRemove(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.row(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.edgeIn(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func scenarioPublisher(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.system(true)
            self.msgView.system(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func feedback(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.system(true)
            self.msgView.system(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func progress(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.progress(tap)
    }

    //: func func__starRecordBtnDown() {}
    func nameurePlace() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func topAndDesign(voicePath: String, voiceTime: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension ReceiverBecome: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func switchly() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        StrengthMomentum.verb(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = NameSave.evenColumn(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: TellEnd.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                TellEnd.share.tillFill()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.iconPush(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func splitKeep() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        StrengthMomentum.momentAccount(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.iconPush(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension ReceiverBecome {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func isolatePrepare(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(const_densityBlankWhoPath))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension ReceiverBecome {
    //: func func__TipsNotification() {
    func spot() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(locate(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: userPhoneValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func locate(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: DenseDisplay = userinfo[(notiWeValue.lowercased())] as! DenseDisplay

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.system(true)
        self.msgView.system(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func run(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: kGlobalUrl.map{mmPlease(bucket: $0)}, encoding: .utf8)!: tipStr, (String(k_containUrl)): (String(mainSpaceStr))]
        //: var message: FlexBridge?
        var message: FlexBridge?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ChainSample.quantity(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.system(true)
        self.msgView.system(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func groupLimit() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] time in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard QuantityeractionDelay.share.appUserConfigMode.rTips.count > 0 else { return }
            guard QuantityeractionDelay.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: QuantityeractionDelay.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.run(tipStr: QuantityeractionDelay.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension ReceiverBecome {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func edgeIn(extral: [String: Any?], model: AngleAdd, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(data_sableMessage))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(main_bridgeUrl) + noti_fractionId.replacingOccurrences(of: "nearby", with: "u"))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(k_containUrl))] = (String(showFactoryClubValue))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(data_sableMessage))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: data_adminProgramMessage.reversed(), encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (data_sumId.lowercased()): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: appCompareSinceData.map{highlightRegularGreet(mouse: $0)}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(notiTotaloPath)).localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((String(showFactoryClubValue))) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(String(showFactoryClubValue))] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(data_sumId.lowercased())] = "\(giftdic[(String(main_underFormat.suffix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(noti_unlessTitle) + String(mainPassFunKey.prefix(5)))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ChainSample.quantity(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.identity(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((show_enemyValue.replacingOccurrences(of: "giving", with: "m") + String(showLightMiddleKey))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(show_enemyValue.replacingOccurrences(of: "giving", with: "m") + String(showLightMiddleKey))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.direct(cellData, independent: msgInfoDic[(String(dataContinuingData))] as? String, action: msgIncome, purple: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        mobile(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((k_plusPath.replacingOccurrences(of: "package", with: "t") + String(constCurrentMessage))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(k_plusPath.replacingOccurrences(of: "package", with: "t") + String(constCurrentMessage))] as! NSNumber
            //: QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: QuantityeractionDelay.share.loginUserMode.mf_coin)
            msgInputView.giftView.consumptionDown(needReload: false, mf_coin: QuantityeractionDelay.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.event()
    }

    //: func initCachEffectData() {
    func thanFore() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.editor(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func mobile(giftDic: [String: Any], model: AngleAdd) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.cameraFromOffer(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in KindChain.share.gapFormatBlueBox() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [ThresholdWrite]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.joinBy(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.save(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func query(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in KindChain.share.gapFormatBlueBox() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [ThresholdWrite]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.joinBy(model: giftModel as! DarkLost)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.customFast(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension ReceiverBecome {
    /// 创建UI
    //: private func createUI() {
    private func fun() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func constantBullet() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(app_whenAppearanceData)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(app_whenAppearanceData)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - constCountervalMaterialMsg)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func difficult() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = SessionPress()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! FlexBridge).isKind(of: AlongMount.self) {
                    if (cellData as! FlexBridge).isKind(of: AlongMount.self) {
                        //: let groupCellData = cellData as! AlongMount
                        let groupCellData = cellData as! AlongMount
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(dataContinuingData))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: WithinMargin.shared.rx
        WithinMargin.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(user_sineKey.suffix(6)) + notiGoInfoName.replacingOccurrences(of: "kind", with: "c")))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
