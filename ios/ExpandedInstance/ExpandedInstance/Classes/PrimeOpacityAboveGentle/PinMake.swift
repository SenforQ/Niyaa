
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let kCrystalMessage:[UInt8] = [0xb2,0xa3,0xa3,0xbf,0xb6,0xac,0xba,0xb2,0xa3,0xac,0xa3,0xb2,0xaa,0xac,0xa0,0xa6,0xb0,0xb0,0xb6,0xb6,0xb7,0xac,0xbd,0xbc,0xa7,0xba,0xb5,0xba,0xb0,0xb2,0xa7,0xba,0xbc,0xbd]

private func impactButt(movement num: UInt8) -> UInt8 {
    return num ^ 243
}

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let notiSpecKey:[UInt8] = [0x0,0x11,0x11,0xd,0x4,0x1e,0x8,0x0,0x11,0x1e,0x12,0x14,0x3,0x12,0x2,0x13,0x8,0x3,0x4,0x1e,0x12,0x14,0x2,0x2,0x4,0x4,0x5,0x1e,0xf,0xe,0x15,0x8,0x7,0x8,0x2,0x0,0x15,0x8,0xe,0xf]

private func announcementTask(window num: UInt8) -> UInt8 {
    return num ^ 65
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let app_userStr:String = "mf/reexplore general"
fileprivate let appLackUrl:String = "key reference todaye/crea"
fileprivate let appCoverFormat:String = "afilter"

/*: "productId" :*/
fileprivate let user_profileMessage:[Character] = ["p","r","o","d","u","c","t","I","d"]

/*: "source" :*/
fileprivate let const_pinionKey:String = "sourgivinge"

/*: "IAP充值创建订单失败: :*/
fileprivate let data_locationOwnString:[Character] = ["I","A","P","\u{5145}"]
fileprivate let mainSaveMessage:[Character] = ["值","创","建","\u{8ba2}","\u{5355}","失","败",":"]

/*: "orderNum" :*/
fileprivate let appSimilarPath:String = "owarnde"

/*: "mf/recharge/applePayNotify" :*/
fileprivate let data_anotherMpTitle:[Character] = ["m","f","/","r","e","c","h","a","r"]
fileprivate let kTunMessage:String = "ge/apquit automatic"
fileprivate let noti_enthusiasmTitle:String = "each fault meetNotify"

/*: "IAP充值校验失败: :*/
fileprivate let kPrimaryId:String = "IAP\u{5145}\u{503c}"
fileprivate let const_efficiencyTitle:[Character] = ["校","\u{9a8c}","失","败",":"]

/*: "transactionId" :*/
fileprivate let app_usedKey:String = "tranestimated"

/*: "reportMoney" :*/
fileprivate let constMaleKey:String = "reindeed"
fileprivate let k_passName:[Character] = ["e","y"]

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let show_examineTitle:String = "super resign recordmf/Aut"
fileprivate let notiStageName:String = "AppleCreweight explicit"
fileprivate let mainLiveJuniorPath:[Character] = ["a"]
fileprivate let const_topicName:[Character] = ["t","e","O","r","d","e","r"]

/*: "IAP订阅创建订单失败: :*/
fileprivate let notiAccessibleUrl:String = "IApara"
fileprivate let appAccuracyIonDeadlineName:String = "\u{8ba2}阅创建"

/*: "orderId" :*/
fileprivate let showRatePath:[UInt8] = [0x27,0x3a,0x2c,0x2d,0x3a,0x1,0x2c]

private func marginAir(dialog num: UInt8) -> UInt8 {
    return num ^ 72
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let noti_readerComposeTarMsg:String = "mf/Autoprint turn point plus reject"
fileprivate let k_duringCommitRewardStr:String = "Sub/Apfade beside"
fileprivate let k_randomEnemyId:String = "floor sibling moment poolySuccess"

/*: "IAP订阅校验失败: :*/
fileprivate let kImmediateData:String = "enhance funIAP订"

/*: "App" :*/
fileprivate let appVeryId:[Character] = ["A","p","p"]

/*: "OrderTransactionInfo_ :*/
fileprivate let kEasyMessage:[Character] = ["O","r","d","e","r","T","r","a","n","s"]
fileprivate let const_itProjectionUrl:String = "infotio"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let showSheMsg:[UInt8] = [0x5f,0x65,0x62,0x69,0x72,0x63,0x73,0x62,0x75,0x53,0x5f,0x6f,0x66,0x6e,0x49,0x6e,0x6f,0x69,0x74,0x63,0x61,0x73,0x6e,0x61,0x72,0x54,0x72,0x65,0x64,0x72,0x4f]

/*: "verifyData" :*/
fileprivate let showDenySaveValue:[UInt8] = [0xed,0xfe,0xe9,0xf2,0xfd,0xe2,0xdf,0xfa,0xef,0xfa]

private func timeChain(cloud num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let constComponentBrushMsg:String = "IAP\u{67e5}询内"
fileprivate let notiToolName:String = " prostack guide draft"
fileprivate let noti_housingExerciseData:String = "radio"

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let mainMyTitle:String = "social zz two aIAP查询内"
fileprivate let show_readStr:String = ", profloat odd listen instead"
fileprivate let showKingBeServerUrl:String = "dgapct"
fileprivate let noti_dropSecondData:String = "nt为0.period processing ravel"

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let constIgnoreTitle:[Character] = ["I","A","P","\u{67e5}","\u{8be2}","内","购","商"]
fileprivate let showScreenStr:[Character] = ["品","失","\u{8d25}",","," "]
fileprivate let app_sharePath:[Character] = ["E","r","r","o","r",":"]

/*: "IAP-purchased: :*/
fileprivate let show_dailyName:[Character] = ["I","A","P","-","p","u","r","c"]
fileprivate let mainLockData:[Character] = ["h","a","s","e","d",":"]

/*: , 订单号: :*/
fileprivate let showSaltStr:[Character] = [","," ","\u{8ba2}","单","号",":"]

/*: , 商品Id: :*/
fileprivate let data_jackString:String = ", 商品Id:hundred pan till agent fluid"

/*: , 苹果transId: :*/
fileprivate let notiPriorValue:[Character] = [","," ","苹","果","t"]
fileprivate let app_verbPath:String = "ransId:document crush"

/*: "IAP-failed: :*/
fileprivate let noti_mutualMomentumMessage:[Character] = ["I","A","P","-","f"]
fileprivate let k_carrierValue:[Character] = ["a"]
fileprivate let mainEnthusiasmString:[Character] = ["i","l","e","d",":"]

/*: "IAP-deferred: :*/
fileprivate let user_mapString:String = "it assistantIAP-de"
fileprivate let data_generateKey:String = "second"

/*: "IAP未知交易类型: :*/
fileprivate let notiDepartmentId:String = "IAP\u{672a}知"
fileprivate let app_panDetailedString:String = "交易类型:"

/*: " 未知的交易类型" :*/
fileprivate let showDiskMsg:[Character] = [""," ","未","知","的"]
fileprivate let show_gapTitle:[Character] = ["交","易","类","型"]

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let notiHereSheValue:String = "IAP获取finish display"
fileprivate let app_analyzeStr:String = "\u{636e}失败,"
fileprivate let mainBlocPolicyMessage:[Character] = ["c","t","i","o","n","I","d",":"]

/*: , orderId: :*/
fileprivate let noti_numberValue:[Character] = [","," ","o","r","d"]
fileprivate let notiOurAmongFormat:[Character] = ["e","r","I","d",":"]

/*: , payType: :*/
fileprivate let user_shallData:String = ", payTting similar appearance skip"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PinMake.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let k_immediateName = NSNotification.Name(rawValue: String(bytes: kCrystalMessage.map{impactButt(movement: $0)}, encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let notiTempFormat = NSNotification.Name(rawValue: String(bytes: notiSpecKey.map{announcementTask(window: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let k_performString = 9

/// 支付类型
//: enum ApplePayType {
enum ArraySpecifySignificant {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum CrucialRound: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (CrucialRound, Double, ArraySpecifySignificant) -> Void

//: class AppleIAPManager: NSObject {
class PinMake: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: ArraySpecifySignificant = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = PinMake()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(groupDetail),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func groupDetail() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension PinMake {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func consumeAvailable(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PerCoordinator()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(app_userStr.prefix(5)) + "charg" + String(appLackUrl.suffix(6)) + "teAppleP" + appCoverFormat.replacingOccurrences(of: "filter", with: "y"))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(user_profileMessage))] = productId
        //: dict["source"] = source
        dict[(const_pinionKey.replacingOccurrences(of: "giving", with: "c"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                MaximumMaster.circleStat(msg: (String(data_locationOwnString) + String(mainSaveMessage)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(appSimilarPath.replacingOccurrences(of: "warn", with: "r") + "rNum")].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func isVisual(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PerCoordinator()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(data_anotherMpTitle) + String(kTunMessage.prefix(5)) + "plePay" + String(noti_enthusiasmTitle.suffix(6)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    MaximumMaster.circleStat(msg: (kPrimaryId + String(const_efficiencyTitle)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.decideExecute(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.coverWith()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_immediateName, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(constMaleKey.replacingOccurrences(of: "indeed", with: "po") + "rtMon" + String(k_passName))].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension PinMake {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func betweenRedFull(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PerCoordinator()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(show_examineTitle.suffix(6)) + "oSub/" + String(notiStageName.prefix(8)) + String(mainLiveJuniorPath) + String(const_topicName))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(String(user_profileMessage))] = productId
        //: dict["source"] = source
        dict[(const_pinionKey.replacingOccurrences(of: "giving", with: "c"))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                MaximumMaster.circleStat(msg: (notiAccessibleUrl.replacingOccurrences(of: "para", with: "P") + appAccuracyIonDeadlineName + "订单失败:") + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: showRatePath.map{marginAir(dialog: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func remarkLock(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PerCoordinator()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(noti_readerComposeTarMsg.prefix(7)) + String(k_duringCommitRewardStr.prefix(6)) + "plePa" + String(k_randomEnemyId.suffix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    MaximumMaster.circleStat(msg: (String(kImmediateData.suffix(4)) + "阅校验失败:") + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.decideExecute(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.reinOff()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: notiTempFormat, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(constMaleKey.replacingOccurrences(of: "indeed", with: "po") + "rtMon" + String(k_passName))].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension PinMake {
    /// 初始化数据
    //: private func iap_initData() {
    private func exceptSplit() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = quantityerval(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = quantityerval(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func quantityerval(payType: ArraySpecifySignificant) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = coverWith()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = reinOff()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func coverWith() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(appVeryId)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = app_pressureId.string(forKey: userFloorString) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(kEasyMessage) + const_itProjectionUrl.replacingOccurrences(of: "info", with: "ac") + "nInfo_") + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func reinOff() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(appVeryId)))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = app_pressureId.string(forKey: userFloorString) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: showSheMsg.reversed(), encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func splitLoan(_ transactionId: String, _ payType: ArraySpecifySignificant) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: showDenySaveValue.map{timeChain(cloud: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: showDenySaveValue.map{timeChain(cloud: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension PinMake {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func embraceSetAnswer() {
        //: iap_initData()
        exceptSplit()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            shouldActivity(dict[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            shouldActivity(dict[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func shouldActivity(_ transactionId: String?, _ payType: ArraySpecifySignificant) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        decideExecute(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension PinMake {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func eagerStorage(productId: String, payType: ArraySpecifySignificant, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        exceptSplit()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            consumeAvailable(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.situationAbove(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            betweenRedFull(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.situationAbove(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func situationAbove(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            MaximumMaster.circleStat(msg: (constComponentBrushMsg + "购商品失败," + String(notiToolName.prefix(4)) + "ductId" + noti_housingExerciseData.replacingOccurrences(of: "radio", with: ":")) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.resourceByCan()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func resourceByCan() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension PinMake: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            MaximumMaster.circleStat(msg: (String(mainMyTitle.suffix(6)) + "购商\u{54c1}\u{5931}败" + String(show_readStr.prefix(5)) + showKingBeServerUrl.replacingOccurrences(of: "gap", with: "u") + "s.cou" + String(noti_dropSecondData.prefix(5))))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_ request: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        MaximumMaster.circleStat(msg: (String(constIgnoreTitle) + String(showScreenStr) + String(app_sharePath)) + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_ request: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension PinMake: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    MaximumMaster.circleStat(msg: (String(show_dailyName) + String(mainLockData)) + "\(currentPayType)" + (String(showSaltStr)) + "\(String(describing: createOrderId))" + (String(data_jackString.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(notiPriorValue) + String(app_verbPath.prefix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    decideExecute(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MaximumMaster.circleStat(msg: (String(noti_mutualMomentumMessage) + String(k_carrierValue) + String(mainEnthusiasmString)) + "\(currentPayType)" + (String(showSaltStr)) + "\(String(describing: createOrderId))" + (String(data_jackString.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(notiPriorValue) + String(app_verbPath.prefix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: let vc = TalkingContactServiceView.init()
                let vc = StateCharacteristic()
                //: vc.show()
                vc.aboveFile()
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MaximumMaster.circleStat(msg: (String(user_mapString.suffix(6)) + "ferred" + data_generateKey.replacingOccurrences(of: "second", with: ":")) + "\(currentPayType)" + (String(showSaltStr)) + "\(String(describing: createOrderId))" + (String(data_jackString.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(notiPriorValue) + String(app_verbPath.prefix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                MaximumMaster.circleStat(msg: (notiDepartmentId + app_panDetailedString.capitalized) + "\(currentPayType)" + (String(showSaltStr)) + "\(String(describing: createOrderId))" + (String(data_jackString.prefix(7))) + "\(transaction.payment.productIdentifier)" + (String(notiPriorValue) + String(app_verbPath.prefix(7))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((String(showDiskMsg) + String(show_gapTitle)))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func decideExecute(_ transactionId: String, _ payType: ArraySpecifySignificant) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = splitLoan(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            MaximumMaster.circleStat(msg: (String(notiHereSheValue.prefix(5)) + "\u{672c}地收据数" + app_analyzeStr + " transa" + String(mainBlocPolicyMessage)) + "\(transactionId)" + (String(noti_numberValue) + String(notiOurAmongFormat)) + "\(String(describing: createOrderId))" + (String(user_shallData.prefix(6)) + "ype:") + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] == transactionId || $0[String(bytes: showRatePath.map{marginAir(dialog: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId"): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: showRatePath.map{marginAir(dialog: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: showDenySaveValue.map{timeChain(cloud: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.coverWith()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] == transactionId || $0[String(bytes: showRatePath.map{marginAir(dialog: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId"): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: showRatePath.map{marginAir(dialog: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: showDenySaveValue.map{timeChain(cloud: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.reinOff()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > k_performString {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            isVisual(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(app_usedKey.replacingOccurrences(of: "estimated", with: "sa") + "ctionId")] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            remarkLock(transactionId, params: paramsArr.first!)
        }
    }
}
