
//: Declare String Begin

/*: ; build: :*/
fileprivate let main_mentionString:[Character] = [";"," ","b","u","i","l","d",":"]

/*: ; iOS  :*/
fileprivate let kCeilingMessage:[Character] = [";"," ","i","O","S"," "]

/*: "audio" :*/
fileprivate let data_keyName:String = "audizone"

/*: .wav" :*/
fileprivate let user_descriptionPath:[Character] = [".","w","a","v"]

/*: "audio/wav" :*/
fileprivate let const_upperOddId:String = "listendi"

/*: .jpg" :*/
fileprivate let noti_backgroundStr:String = ".jpgdrop m speaker"

/*: "image/jpeg" :*/
fileprivate let dataOutputMessage:[Character] = ["i","m","a","g","e","/","j","p","e","g"]

/*: "://app." :*/
fileprivate let data_errBigName:[Character] = [":"]
fileprivate let noti_multiValue:String = "outer resident lose//app."

/*: "data" :*/
fileprivate let kComposeId:String = "relatedta"

/*: ":null" :*/
fileprivate let kAwayString:String = ":nullload beyond tab"

/*: "Request failed, Try again later" :*/
fileprivate let kGiveCertainName:[UInt8] = [0x3c,0xb,0x1f,0x1b,0xb,0x1d,0x1a,0x4e,0x8,0xf,0x7,0x2,0xb,0xa,0x42,0x4e,0x3a,0x1c,0x17,0x4e,0xf,0x9,0xf,0x7,0x0,0x4e,0x2,0xf,0x1a,0xb,0x1c]

/*: "json error" :*/
fileprivate let noti_freshTitle:[Character] = ["j","s"]
fileprivate let dataValueSuspendData:[Character] = ["o","n"," ","e","r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let data_recordLocateName:[Character] = ["p","l","a","t","f","o","r","m","=","i"]
fileprivate let user_gestureProcessFormat:String = "psocial"
fileprivate let mainFillData:[Character] = ["o","n","e","&","v","e","r","s","i","o","n","="]

/*: &packageId= :*/
fileprivate let main_personalValue:[Character] = ["&","p","a","c"]
fileprivate let noti_labName:[Character] = ["k","a","g","e","I","d","="]

/*: &bundleId= :*/
fileprivate let dataOutsideEasternStr:[Character] = ["&","b","u","n","d","l"]
fileprivate let notiMissingCouldKey:[Character] = ["e","I","d","="]

/*: &lang= :*/
fileprivate let appAnonymousPlaceName:String = "day rounding happy favorite two&lang="

/*: "/route/slowindex" :*/
fileprivate let notiStopCapStr:[Character] = ["/","r","o","u","t","e","/","s","l","o","w"]
fileprivate let show_makerMsg:String = "incrucialex"

/*: "/route/index" :*/
fileprivate let userAttachData:[Character] = ["/","r","o","u","t","e"]
fileprivate let noti_enjoyPlayMsg:String = "/indexif heavy quality against"

/*: "?path= :*/
fileprivate let user_enterUrl:[Character] = ["?","p","a","t","h","="]

/*: "&GJNonceStr= :*/
fileprivate let const_shapeUrl:[Character] = ["&","G","J","N","o","n","c","e","S","t","r","="]

/*: "&params= :*/
fileprivate let userFoundationMsg:String = "&paratypical we delay destroy continuing"
fileprivate let show_trainMsg:[Character] = ["m","s","="]

/*: "token" :*/
fileprivate let constProjectDeliveryId:String = "tonaturalen"

/*: "请求成功 :*/
fileprivate let app_veryRecordConstraintWouldKey:String = "shared求成功"

/*: "AAAAAAAA" :*/
fileprivate let noti_siblingPath:String = "invisibleinvisibleinvisibleinvisible"

/*: "下载完成 -  :*/
fileprivate let user_imageMsg:String = "下\u{8f7d}完成 - "

/*: "/Documents/" :*/
fileprivate let dataCommitFormat:String = "/Docthorough receive"

/*: ".mp4" :*/
fileprivate let app_consumeAdviceKey:String = "drop current component let mark.mp4"

/*: "移动地址: :*/
fileprivate let const_shapePlainYesCompositionKey:String = "移\u{52a8}地址:"

/*: "后台任务下载回来" :*/
fileprivate let showThresholdData:[Character] = ["后","台","任"]
fileprivate let noti_shotFormat:String = "务下载回relationship"

/*: "下载成功 : :*/
fileprivate let const_wordPath:String = "下载\u{6210}功 :"

/*: "Net Error" :*/
fileprivate let userCollectKey:[Character] = ["N","e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let const_helpJobString:String = "colonial失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  DownLibrary.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let showRoleMessage = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let notiAttachString = "\(notiCaptureMsg)/\(show_loadName) (\(appFirstFormat)" + (String(main_mentionString)) + "\(const_grainData)" + (String(kCeilingMessage)) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: ComprehensiveGrayLibrary?) -> Void

//: let GJ = TalkingRequestTool.init()
let appRowLabelUrl = DownLibrary()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class DownLibrary: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func mouse(model: PerCoordinator, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.programEnd() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.banCondition(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.bagPhaseRemoteOperateGoldPoint(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func banCondition(model: PerCoordinator, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.mechanismByApp(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.broadConstruct(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    analyzeBehavior(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.NetError.rawValue, errorMsg: showCountensityPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        paraDown(showMsg: showCountensityPath)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (data_keyName.replacingOccurrences(of: "zone", with: "o")) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.jumpSum())+\(appMarkerResultValue)" + (String(user_descriptionPath)), mimeType: (const_upperOddId.replacingOccurrences(of: "listen", with: "au") + "o/wav"))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(noti_backgroundStr.prefix(4))), mimeType: (String(dataOutputMessage)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    analyzeBehavior(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.NetError.rawValue, errorMsg: showCountensityPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        paraDown(showMsg: showCountensityPath)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func bagPhaseRemoteOperateGoldPoint(model: PerCoordinator, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.mechanismByApp(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.broadConstruct(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: mainAwayQuantityensityFormat ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = showRoleMessage }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    analyzeBehavior(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.NetError.rawValue, errorMsg: showCountensityPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        paraDown(showMsg: showCountensityPath)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = showRoleMessage }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    analyzeBehavior(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.NetError.rawValue, errorMsg: showCountensityPath))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        paraDown(showMsg: showCountensityPath)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func analyzeBehavior(model: PerCoordinator, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            ImageClear.share.hideBag(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if mainAwayQuantityensityFormat, model.requestServer.contains((String(data_errBigName) + String(noti_multiValue.suffix(6)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.someCommon(key: ImageClear.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (kComposeId.replacingOccurrences(of: "related", with: "da")) + "\"" + (String(kAwayString.prefix(5))), with: "" + "\"" + (kComposeId.replacingOccurrences(of: "related", with: "da")) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !mainAwayQuantityensityFormat {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(kAwayString.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<AtLimit>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == ArchitectureLoad.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, ComprehensiveGrayLibrary(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    paraDown(showMsg: responseModel.msg ?? String(bytes: kGiveCertainName.map{$0^110}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case ArchitectureLoad.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: main_matchTitle, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.NetError.rawValue, errorMsg: (String(noti_freshTitle) + String(dataValueSuspendData)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func mechanismByApp(model: PerCoordinator) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(data_recordLocateName) + user_gestureProcessFormat.replacingOccurrences(of: "social", with: "h") + String(mainFillData)) + "\(userManagerKeyUrl)" + (String(main_personalValue) + String(noti_labName)) + "\(appMarkerResultValue)" + (String(dataOutsideEasternStr) + String(notiMissingCouldKey)) + "\(appFirstFormat)" + (String(appAnonymousPlaceName.suffix(6))) + "\(ImageClear.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if mainAwayQuantityensityFormat, serverUrl.contains((String(data_errBigName) + String(noti_multiValue.suffix(6)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if ImageClear.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(notiStopCapStr) + show_makerMsg.replacingOccurrences(of: "crucial", with: "d")))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(userAttachData) + String(noti_enjoyPlayMsg.prefix(6)))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(user_enterUrl)) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(const_shapeUrl)) + "\(ImageClear.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.partyTo(key: ImageClear.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(userFoundationMsg.prefix(5)) + String(show_trainMsg)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func broadConstruct(model: PerCoordinator) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(notiAttachString)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (constProjectDeliveryId.replacingOccurrences(of: "natural", with: "k")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func deleteFramework(model: PerCoordinator) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.mechanismByApp(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { data, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (app_veryRecordConstraintWouldKey.replacingOccurrences(of: "shared", with: "请")) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (noti_siblingPath.replacingOccurrences(of: "invisible", with: "A") + "AAAA")) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (user_imageMsg) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(dataCommitFormat.prefix(4)) + "uments/") + NSDate.jumpSum() + (String(app_consumeAdviceKey.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (const_shapePlainYesCompositionKey) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (String(showThresholdData) + noti_shotFormat.replacingOccurrences(of: "relationship", with: "来")))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = AlongAt.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func exclusiveDistribution(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: showGroupFlexibleFlowName.occur(), with: show_whiteTitle)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (const_wordPath) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.Normal.rawValue, errorMsg: (String(userCollectKey)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.NetError.rawValue, errorMsg: (String(userCollectKey)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (const_helpJobString.replacingOccurrences(of: "colonial", with: "下载")) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func ta(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: showGroupFlexibleFlowName.occur(), with: show_whiteTitle)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (const_wordPath) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.Normal.rawValue, errorMsg: (String(userCollectKey)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, ComprehensiveGrayLibrary(errorCode: ArchitectureLoad.NetError.rawValue, errorMsg: (String(userCollectKey)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (const_helpJobString.replacingOccurrences(of: "colonial", with: "下载")) + "\(errMsg)")
            }
        }
    }
}
