
//: Declare String Begin

/*: "mf/moment/list" :*/
fileprivate let userFirstReparationFormat:[Character] = ["m","f","/","m","o","m","e","n","t","/","l"]
fileprivate let show_betweenName:[Character] = ["i","s","t"]

/*: "mf/moment/moments" :*/
fileprivate let constStorageKey:[Character] = ["m","f","/","m","o","m","e","n","t","/","m","o"]
fileprivate let k_audienceScopeSearchedPath:[Character] = ["m","e","n","t","s"]

/*: "mf/moment/like" :*/
fileprivate let userPassPath:[Character] = ["m","f","/","m","o","m","e","n","t","/","l","i"]
fileprivate let main_lackGameUrl:String = "KE"

/*: "mf/crush/send" :*/
fileprivate let dataPersonStr:String = "too enhance op halfmf/cru"
fileprivate let mainYieldFormat:String = "sh/sendcurrently increase"

/*: "mf/moment/view" :*/
fileprivate let user_attachPath:[Character] = ["m","f","/","m","o","m","e"]
fileprivate let const_bringMaxKey:String = "nt/viewraw lost initial advanced"

/*: "mf/momentReply/list" :*/
fileprivate let userTranslationPictureId:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p"]
fileprivate let k_createValue:String = "per markly/list"

/*: "mf/momentReply/save" :*/
fileprivate let k_deliveryName:[Character] = ["m","f","/","m","o","m","e","n","t","R","e","p","l","y","/"]
fileprivate let const_regularForceReductionUrl:String = "bounceave"

/*: "mf/moment/videoSign" :*/
fileprivate let dataLeaveTitle:String = "mf/momebass own title"
fileprivate let showEffectMsg:[Character] = ["n","t","/","v","i","d","e","o","S","i","g","n"]

/*: "dynamicPic.zip" :*/
fileprivate let userSplitId:String = "dyncheck"
fileprivate let dataEvolutionPath:[Character] = ["c",".","z","i","p"]

/*: "pic" :*/
fileprivate let mainSessionKey:[UInt8] = [0x85,0x7e,0x78]

fileprivate func missionBe(we num: UInt8) -> UInt8 {
    let value = Int(num) - 21
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let mainPostBlocValue:[Character] = ["c","o"]
fileprivate let constUniqueTitle:[Character] = ["n","t","e","n","t"]

/*: "mf/moment/save" :*/
fileprivate let const_reportMessage:String = "green specmf/mo"
fileprivate let notiFrontStr:String = "ment/choose second input"

/*: "Publish Successful, Go to My Dynamics to view" :*/
fileprivate let showAleFadeTimingMessage:[UInt8] = [0xa1,0x84,0x93,0x9d,0x98,0x82,0x99,0xd1,0xa2,0x84,0x92,0x92,0x94,0x82,0x82,0x97,0x84,0x9d,0xdd,0xd1,0xb6,0x9e,0xd1,0x85,0x9e,0xd1,0xbc,0x88,0xd1,0xb5,0x88,0x9f,0x90,0x9c,0x98,0x92,0x82,0xd1,0x85,0x9e,0xd1,0x87,0x98,0x94,0x86]

private func paraJump(filter num: UInt8) -> UInt8 {
    return num ^ 241
}

/*: "mf/moment/saveVideo" :*/
fileprivate let showDownName:String = "week cookiemf/m"
fileprivate let app_simultaneouslyTitle:String = "associated why check altogethersaveVi"
fileprivate let main_demonstrateCommitMessage:[Character] = ["d","e","o"]

/*: .png" :*/
fileprivate let dataSpeakerId:String = ".pngsubsequent impact fixed account"

/*: "dynamicPic" :*/
fileprivate let noti_upQuantityervalValue:[Character] = ["d","y","n","a","m","i","c","P","i"]
fileprivate let user_maximumPutId:String = "advice"

/*: "freeImage" :*/
fileprivate let show_creationString:String = "will adjust marginfreeI"

/*: "mf/momentReply/replylist" :*/
fileprivate let mainBringMsg:String = "res who restriction dealmf/m"
fileprivate let show_boxPublisherName:[Character] = ["R","e","p","l","y","/","r","e","p","l","y","l","i","s","t"]

/*: "mf/moment/delete" :*/
fileprivate let kOthersMsg:String = "mf/momdecrease lost detect admin vest"
fileprivate let const_previousMsg:String = "writing"
fileprivate let constHistoryFormat:String = "insertete"

/*: "mf/momentReply/delete" :*/
fileprivate let noti_rankString:String = "mf/momfemale please active estimated"
fileprivate let showPromptKey:String = "EPLY"

/*: "mf/moment/pin" :*/
fileprivate let kTierBlankFormat:String = "mf/mreturn tell document"
fileprivate let appTabStr:String = "omesovereign"

/*: "mf/moment/remindNum" :*/
fileprivate let mainFastAssertName:String = "enough marker alwaysmf/mo"
fileprivate let constImplementationFollowingKey:String = "/remindNuinterested volume union"
fileprivate let appPopRegionUrl:String = "crystal"

/*: "mf/moment/remindList" :*/
fileprivate let main_actualLowerKey:String = "mf/malways list reject enthusiasm"
fileprivate let dataDrawString:String = "t/remias week mouse"

/*: "page" :*/
fileprivate let showSubjectString:[UInt8] = [0x9b,0x8c,0x92,0x90]

fileprivate func writingProcessor(today num: UInt8) -> UInt8 {
    let value = Int(num) - 43
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cos/getCredential" :*/
fileprivate let dataShallProductMightId:String = "cos/gfurther actual required"
fileprivate let user_formWhatPath:[Character] = ["e","t","C","r","e","d"]
fileprivate let userRecentPath:String = "edespitetial"

/*: "type" :*/
fileprivate let mainSuccessfulData:[Character] = ["t","y","p","e"]

/*: "user/reportUserInfo" :*/
fileprivate let dataDonMoreData:[Character] = ["u","s","e","r","/","r","e","p","o","r","t","U","s","e","r","I","n","f","o"]

/*: "uid" :*/
fileprivate let main_pushId:[Character] = ["u","i","d"]

/*: "deviceId" :*/
fileprivate let showDenyMessage:String = "simultaneouslyvi"

/*: "deviceName" :*/
fileprivate let k_presentString:String = "adevi"

/*: "iphone" :*/
fileprivate let kToFormat:String = "iphattentionne"

/*: "fileUrl" :*/
fileprivate let app_aheadMessage:[Character] = ["f","i"]
fileprivate let userIndependentMessage:String = "leUrlpresent choose"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TellCharacter.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/19.
//

//: import Adjust
import Adjust
//: import UIKit
import UIKit

//: class TalkingMomentRequestTool: NSObject {
class TellCharacter: NSObject {
    //: let _fm = FileManager.default
    let _fm = FileManager.default

    /// 动态列表接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func pure(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/list"
        reqModel.requestPath = (String(userFirstReparationFormat) + String(show_betweenName))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 用户动态接口
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_UserMomentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func basicEarly(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/moments"
        reqModel.requestPath = (String(constStorageKey) + String(k_audienceScopeSearchedPath))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 点赞
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentLike(params: [String: Any], completion: @escaping FinishBlock) {
    class func air(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/like"
        reqModel.requestPath = (String(userPassPath) + main_lackGameUrl.lowercased())
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 谁心动我 - 发送心动
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CrushSend(params: [String: Any], completion: @escaping FinishBlock) {
    class func video(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/crush/send"
        reqModel.requestPath = (String(dataPersonStr.suffix(6)) + String(mainYieldFormat.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态详情
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_MomentDetail(params: [String: Any], completion: @escaping FinishBlock) {
    class func labelPad(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/view"
        reqModel.requestPath = (String(user_attachPath) + String(const_bringMaxKey.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentList(params: [String: Any], completion: @escaping FinishBlock) {
    class func route(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/momentReply/list"
        reqModel.requestPath = (String(userTranslationPictureId) + String(k_createValue.suffix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 发表评论、回复
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_SendComment(params: [String: Any], completion: @escaping FinishBlock) {
    class func minimum(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/momentReply/save"
        reqModel.requestPath = (String(k_deliveryName) + const_regularForceReductionUrl.replacingOccurrences(of: "bounce", with: "s"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取视频上传sign
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_VideoSign(params: [String: Any], completion: @escaping FinishBlock) {
    class func scale(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/videoSign"
        reqModel.requestPath = (String(dataLeaveTitle.prefix(7)) + String(showEffectMsg))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - photos: 图片
    ///   - content: 内容
    ///   - completion: 回调
    //: func releasePhotosMoment(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
    func net(photos: [UIImage], content: String, completion: @escaping FinishBlock) {
        // 处理图片
        //: deleteFile()
        acceptExercise()
        //: for (index, photo) in photos.enumerated() {
        for (index, photo) in photos.enumerated() {
            //: getImagePath(index: index, image: photo)
            against(index: index, image: photo)
        }
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let doc_path = (documentDictPath as NSString).appendingPathComponent("dynamicPic.zip")
        let doc_path = (documentDictPath as NSString).appendingPathComponent((userSplitId.replacingOccurrences(of: "check", with: "a") + "micPi" + String(dataEvolutionPath)))
        //: SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        SSZipArchive.createZipFile(atPath: doc_path, withContentsOfDirectory: filePath)
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: let data = NSData(contentsOfFile: doc_path)
            let data = NSData(contentsOfFile: doc_path)
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                // 发布
                //: var params: [String: Any] = ["pic": data as Any]
                var params: [String: Any] = [String(bytes: mainSessionKey.map{missionBe(we: $0)}, encoding: .utf8)!: data as Any]
                //: if content.count > 0 {
                if content.count > 0 {
                    //: params["content"] = content
                    params[(String(mainPostBlocValue) + String(constUniqueTitle))] = content
                }
                //: let reqModel = TalkingRequestModel()
                let reqModel = PerCoordinator()
                //: reqModel.requestPath = "mf/moment/save"
                reqModel.requestPath = (String(const_reportMessage.suffix(5)) + String(notiFrontStr.prefix(5)) + "save")
                //: reqModel.requestType = .POST
                reqModel.requestType = .POST
                //: reqModel.params = params
                reqModel.params = params
                //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
                appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Publish Successful, Go to My Dynamics to view".localized)
                        self.outsideFeature(showMsg: String(bytes: showAleFadeTimingMessage.map{paraJump(filter: $0)}, encoding: .utf8)!.localized)
                        //: self.deleteFile()
                        self.acceptExercise()
                    }
                    //: completion(succeed, result, errorModel)
                    completion(succeed, result, errorModel)
                }
            }
        }
    }

    /// 动态发布
    /// - Parameters:
    ///   - completion: 回调
    //: class func releaseVideoMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func exclusive(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/saveVideo"
        reqModel.requestPath = (String(showDownName.suffix(4)) + "oment/" + String(app_simultaneouslyTitle.suffix(6)) + String(main_demonstrateCommitMessage))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: private func getImagePath(index: Int, image: UIImage) {
    private func against(index: Int, image: UIImage) {
        //: let data = image.compressedImageData()
        let data = image.basicSink()
        //: let nameString = "\(index).png"
        let nameString = "\(index)" + (String(dataSpeakerId.prefix(4)))
        //: let picPath = (filePath as NSString).appendingPathComponent("dynamicPic")
        let picPath = (filePath as NSString).appendingPathComponent((String(noti_upQuantityervalValue) + user_maximumPutId.replacingOccurrences(of: "advice", with: "c")))
        //: if !_fm.fileExists(atPath: picPath) {
        if !_fm.fileExists(atPath: picPath) {
            //: try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
            try? _fm.createDirectory(atPath: picPath, withIntermediateDirectories: true)
        }
        //: let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        let imagePath = (picPath as NSString).appendingPathComponent(nameString)
        //: _fm.createFile(atPath: imagePath, contents: data)
        _fm.createFile(atPath: imagePath, contents: data)
    }

    //: private func deleteFile() {
    private func acceptExercise() {
        //: if _fm.fileExists(atPath: filePath) {
        if _fm.fileExists(atPath: filePath) {
            //: try? _fm.removeItem(atPath: filePath)
            try? _fm.removeItem(atPath: filePath)
        }
    }

    //: private lazy var filePath: String = {
    private lazy var filePath: String = {
        //: let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDictPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let imagePath = (documentDictPath as NSString).appendingPathComponent("freeImage")
        let imagePath = (documentDictPath as NSString).appendingPathComponent((String(show_creationString.suffix(5)) + "mage"))
        //: return imagePath
        return imagePath
        //: }()
    }()

    /// 子评论列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_CommentDetailList(params: [String: Any], completion: @escaping FinishBlock) {
    class func scenario(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/momentReply/replylist"
        reqModel.requestPath = (String(mainBringMsg.suffix(4)) + "oment" + String(show_boxPublisherName))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除动态
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func mentionRaw(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/delete"
        reqModel.requestPath = (String(kOthersMsg.prefix(6)) + "ent/" + const_previousMsg.replacingOccurrences(of: "writing", with: "d") + constHistoryFormat.replacingOccurrences(of: "insert", with: "el"))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 删除评论
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_DeleteMomentReply(params: [String: Any], completion: @escaping FinishBlock) {
    class func typical(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/momentReply/delete"
        reqModel.requestPath = (String(noti_rankString.prefix(6)) + "entR" + showPromptKey.lowercased() + "/delete")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 置顶操作
    /// - Parameters:
    /// status=1表示置顶，0=取消置顶
    ///   - completion: 回调
    //: class func req_TopMoment(params: [String: Any], completion: @escaping FinishBlock) {
    class func everyPurple(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/pin"
        reqModel.requestPath = (String(kTierBlankFormat.prefix(4)) + appTabStr.replacingOccurrences(of: "sovereign", with: "n") + "t/pin")
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户未读动态总数
    /// - Parameter completion: 回调
    //: class func req_getRemindNum(completion: @escaping FinishBlock) {
    class func platformEffect(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/remindNum"
        reqModel.requestPath = (String(mainFastAssertName.suffix(5)) + "ment" + String(constImplementationFollowingKey.prefix(9)) + appPopRegionUrl.replacingOccurrences(of: "crystal", with: "m"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取当前未读动态信息列表
    /// - Parameters:
    ///   - page: 分页
    ///   - completion: 回调
    //: class func req_getRemindList(page: Int, completion: @escaping (_ data: [TalkingRemindListModel]) -> Void) {
    class func linkAcrossBody(page: Int, completion: @escaping (_ data: [WindowResult]) -> Void) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/moment/remindList"
        reqModel.requestPath = (String(main_actualLowerKey.prefix(4)) + "omen" + String(dataDrawString.prefix(6)) + "ndList")
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["page": page]
        reqModel.params = [String(bytes: showSubjectString.map{writingProcessor(today: $0)}, encoding: .utf8)!: page]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: var dataArr: [TalkingRemindListModel] = []
            var dataArr: [WindowResult] = []
            //: guard let arr = result as? [[String: Any]] else {
            guard let arr = result as? [[String: Any]] else {
                //: completion(dataArr)
                completion(dataArr)
                //: return
                return
            }
            //: if let datas = Array<TalkingRemindListModel>.deserialize(from: arr as? Array) as? [TalkingRemindListModel] {
            if let datas = Array<WindowResult>.deserialize(from: arr as? Array) as? [WindowResult] {
                //: dataArr.append(contentsOf: datas)
                dataArr.append(contentsOf: datas)
            }
            //: completion(dataArr)
            completion(dataArr)
        }
    }

    /// COS  视频存储
    /// - Parameters:
    ///   - type: 1=动态，2=视频封面，3=私信视频
    ///   - completion: 回调
    //: class func req_getCredential(type: Int, completion: @escaping FinishBlock) {
    class func projection(type: Int, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "cos/getCredential"
        reqModel.requestPath = (String(dataShallProductMightId.prefix(5)) + String(user_formWhatPath) + userRecentPath.replacingOccurrences(of: "despite", with: "n"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["type": type]
        reqModel.params = [(String(mainSuccessfulData)): type]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 上传cos日志存储路径
    /// - Parameters:
    ///   - fileUrl: 日志路径
    ///   - completion: 回调
    //: class func req_uploadLogPath(fileUrl: String, completion: @escaping FinishBlock) {
    class func bridgeBasic(fileUrl: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = PerCoordinator()
        //: reqModel.requestServer = QuantityeractionDelay.share.appConfigMode.reportDomain
        reqModel.requestServer = QuantityeractionDelay.share.appConfigMode.reportDomain
        //: reqModel.requestPath = "user/reportUserInfo"
        reqModel.requestPath = (String(dataDonMoreData))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var params = [String: Any]()
        var params = [String: Any]()
        //: params["uid"] = QuantityeractionDelay.share.loginUserMode.userID
        params[(String(main_pushId))] = QuantityeractionDelay.share.loginUserMode.userID
        //: params["deviceId"] = Adjust.adid() ?? ""
        params[(showDenyMessage.replacingOccurrences(of: "simultaneously", with: "de") + "ceId")] = Adjust.adid() ?? ""
        //: params["deviceName"] = "iphone"
        params[(k_presentString.replacingOccurrences(of: "ad", with: "d") + "ceName")] = (kToFormat.replacingOccurrences(of: "attention", with: "o"))
        //: params["type"] = 1
        params[(String(mainSuccessfulData))] = 1
        //: params["fileUrl"] = fileUrl
        params[(String(app_aheadMessage) + String(userIndependentMessage.prefix(5)))] = fileUrl
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }
}
