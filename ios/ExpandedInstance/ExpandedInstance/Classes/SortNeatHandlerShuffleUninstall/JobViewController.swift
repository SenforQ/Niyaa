
//: Declare String Begin

/*: "cmd" :*/
fileprivate let user_tabId:[UInt8] = [0x44,0x4a,0x43]

private func equivalentMonth(re num: UInt8) -> UInt8 {
    return num ^ 39
}

/*: "cancelCall" :*/
fileprivate let constInsertString:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "data" :*/
fileprivate let show_reinTitle:[UInt8] = [0xc8,0xcd,0xd8,0xcd]

private func disappearExplore(price num: UInt8) -> UInt8 {
    return num ^ 172
}

/*: "logId" :*/
fileprivate let app_furtherId:String = "heavy all through minimizelogId"

/*: "#322870" :*/
fileprivate let showMmFormat:String = "#322870"

/*: "Tap to start video chatting" :*/
fileprivate let app_leyMsg:[Character] = ["T","a","p"," ","t","o"," ","s","t","a","r","t"," ","v","i"]
fileprivate let dataAppearanceTitle:String = "volume menu quote retireddeo cha"
fileprivate let user_quickUrl:String = "ttifieldg"

/*: "icon_close_nor" :*/
fileprivate let constFirePath:String = "icon_cadvanced domain catch"
fileprivate let notiOriginPassUrl:[Character] = ["o","r"]

/*: "icon_views_number" :*/
fileprivate let data_piAssistMessage:String = "icon_vper tense nor jack down"
fileprivate let show_interruptUrl:String = "ienable"
fileprivate let mainRateName:String = "ws_numultiple light"

/*: "%@ chatting" :*/
fileprivate let main_evenValue:String = "%@ chawho catch"
fileprivate let data_literalStaffMessage:String = "tticigar"

/*: "icon_lounge" :*/
fileprivate let showCurPath:String = "icwarnn"

/*: "icon_rank_coin" :*/
fileprivate let appHaveTailedActorKey:String = "which question var schedule brickicon_rank"
fileprivate let appCommonMsg:String = "_coingirl coordinate with revenue"

/*: "vip_btn_top" :*/
fileprivate let user_perValidPath:String = "detailed remaining listenervip_b"

/*: "btn_nearby_settings_nor" :*/
fileprivate let showUntilFormat:String = "previous factorbtn_ne"
fileprivate let k_assistantKey:String = "cos key comment equivalent unknown_settin"
fileprivate let mainTransformStructureName:String = "replace chart process nearby thangs_nor"

/*: "%@ Coins/min" :*/
fileprivate let mainPriorityValue:String = "union owner each like%@ Coi"

/*: "img_match_connecting" :*/
fileprivate let kCriticalPath:[Character] = ["i","m","g","_","m","a","t","c","h","_","c","o","n","n"]
fileprivate let user_boxName:[Character] = ["e","c","t","i","n","g"]

/*: "#9F80FF" :*/
fileprivate let show_answerId:String = "#9F80FFupon factory broad answer"

/*: "get json error" :*/
fileprivate let app_cigarFunBotKey:[Character] = ["g","e","t"," ","j","s","o","n"," "]
fileprivate let main_reduceString:String = "errvolumer"

/*: "matchId" :*/
fileprivate let noti_floorId:String = "matchIdteam accounting spot ruling outer"

/*: "errno" :*/
fileprivate let noti_attributeNormMsg:[UInt8] = [0x6f,0x6e,0x72,0x72,0x65]

/*: "VIPFee" :*/
fileprivate let constPersonMessage:[Character] = ["V","I","P","F","e","e"]

/*: "Matching now" :*/
fileprivate let user_pauseName:String = "Matclimit mode display woman subject"
fileprivate let userFragmentMsg:String = "turn add operatehing now"

/*: "userTouch" :*/
fileprivate let user_sovereignString:[Character] = ["u","s"]
fileprivate let kVestName:String = "erTouchthrough min heart"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let user_aboutUrl:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i","d","e","o","I","n"]
fileprivate let constCreationAgainTitle:String = "administrative"

/*: "fromType" :*/
fileprivate let dataWrittenTitle:[UInt8] = [0xe3,0xf7,0xea,0xe8,0xd1,0xfc,0xf5,0xe0]

private func yesPerson(immediate num: UInt8) -> UInt8 {
    return num ^ 133
}

/*: "You've been barred from receiving calls" :*/
fileprivate let k_revenueUrl:[UInt8] = [0x8e,0xb8,0xa2,0xf0,0xa1,0xb2,0xf7,0xb5,0xb2,0xb2,0xb9,0xf7,0xb5,0xb6,0xa5,0xa5,0xb2,0xb3,0xf7,0xb1,0xa5,0xb8,0xba,0xf7,0xa5,0xb2,0xb4,0xb2,0xbe,0xa1,0xbe,0xb9,0xb0,0xf7,0xb4,0xb6,0xbb,0xbb,0xa4]

private func farShake(explain num: UInt8) -> UInt8 {
    return num ^ 215
}

/*: "source" :*/
fileprivate let showGameReceiveMsg:[UInt8] = [0x65,0x63,0x72,0x75,0x6f,0x73]

/*: "type" :*/
fileprivate let notiEraseEhUrl:[UInt8] = [0xa1,0xac,0xa5,0xb0]

private func plainPull(sequence num: UInt8) -> UInt8 {
    return num ^ 213
}

/*: "uid" :*/
fileprivate let user_verticalFormat:[UInt8] = [0x4,0x18,0x15]

/*: "fromFreeCall" :*/
fileprivate let notiRetMessage:[Character] = ["f","r","o","m","F","r","e"]
fileprivate let user_greetUrl:[Character] = ["e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let const_searcherFormat:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let dataPinFemaleString:[Character] = ["o","n","V","i","d","e","o","M","a"]
fileprivate let dataHundredFlowFormat:String = "tcforce"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let user_weightCoreContinuingData:[UInt8] = [0x67,0x6e,0x69,0x6d,0x6f,0x63,0x6e,0x69,0x20,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x76,0x69,0x65,0x63,0x65,0x52,0x20,0x5d,0x6f,0x65,0x64,0x69,0x76,0x20,0x63,0x69,0x74,0x61,0x6d,0x6f,0x74,0x75,0x41,0x5b]

/*: "isRandomVCPush" :*/
fileprivate let data_accessTitle:[Character] = ["i","s","R","a","n","d","o","m","V","C","P","u","s"]
fileprivate let noti_platProductionFormat:[Character] = ["h"]

/*: "refuseCall" :*/
fileprivate let dataButtTenseValue:String = "rrecoveru"

/*: "onCancelCall" :*/
fileprivate let const_cycleUrl:[Character] = ["o","n","C","a","n","c","e","l","C"]
fileprivate let data_rawFormat:String = "ALL"

/*: "retryAfter" :*/
fileprivate let main_oldKey:String = "temporarytry"
fileprivate let data_jumpFileFormat:[Character] = ["A","f","t","e","r"]

/*: "onRequestCall" :*/
fileprivate let show_selectKey:[Character] = ["o","n","R"]
fileprivate let notiPartStr:String = "eqattachst"

/*: "failType" :*/
fileprivate let notiAddSeaString:String = "fashade"

/*: "callWaitingTime" :*/
fileprivate let kPropertyUrl:String = "callWaprivilege res"
fileprivate let noti_wrapPath:String = "gTimlistener schedule stat butt"
fileprivate let showToleranceId:String = "beside"

/*: "toUid" :*/
fileprivate let mainHiBackName:[Character] = ["t","o","U","i","d"]

/*: "Free 1 min" :*/
fileprivate let mainSensorData:String = "Free 1club behavior"

/*: "%@/min" :*/
fileprivate let notiOurString:String = "%@/minmend related"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  JobViewController.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class JobViewController: ScaleViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.manualForParticleOutput()
            }
        }
        //: self.resetCamera()
        self.shareOrBe()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        plus()
        //: setupSubViewsConstraint()
        deadline()
        //: bindInteraction()
        averageSlide()
        //: videoManager.startPreview(isMatch: true)
        videoManager.columnReport(isMatch: true)
        //: setPlayer(isRandon: false)
        month(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            replyMini()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            UpZone.shared.commonCrop(info: [String(bytes: user_tabId.map{equivalentMonth(re: $0)}, encoding: .utf8)!: (String(constInsertString)), String(bytes: show_reinTitle.map{disappearExplore(price: $0)}, encoding: .utf8)!: [(String(app_furtherId.suffix(5))): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        cable()
        //: if TalkingSocketManager.shared.agileImageSkewy == false {
        if UpZone.shared.agileImageSkewy == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            UpZone.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            UpZone.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            UpZone.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            UpZone.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AroundSubtle = {
        //: let m = TalkingVideoChatManager()
        let m = AroundSubtle()
        //: let info = TalkingVideoChatModel()
        let info = DismissModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .thinOf()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(replyMini), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: dataTabUrl + 1, height: const_rebuildWithinId + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (showMmFormat.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(app_leyMsg) + String(dataAppearanceTitle.suffix(7)) + user_quickUrl.replacingOccurrences(of: "field", with: "n")).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.transferAndJoinSmall(name: (String(constFirePath.prefix(6)) + "lose_n" + String(notiOriginPassUrl))), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(rebuildDownMenu), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: ArrayButton = {
        //: let btn = TalkingButton()
        let btn = ArrayButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.transferAndJoinSmall(name: (String(data_piAssistMessage.prefix(6)) + show_interruptUrl.replacingOccurrences(of: "enable", with: "e") + String(mainRateName.prefix(5)) + "mber")), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.transferAndJoinSmall(name: (String(data_piAssistMessage.prefix(6)) + show_interruptUrl.replacingOccurrences(of: "enable", with: "e") + String(mainRateName.prefix(5)) + "mber")), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.alongsideOk(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(main_evenValue.prefix(6)) + data_literalStaffMessage.replacingOccurrences(of: "cigar", with: "ng")).underConnection(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.transferAndJoinSmall(name: (showCurPath.replacingOccurrences(of: "warn", with: "o") + "_lounge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.transferAndJoinSmall(name: (String(appHaveTailedActorKey.suffix(9)) + String(appCommonMsg.prefix(5))))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.alongsideOk(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.transferAndJoinSmall(name: (String(user_perValidPath.suffix(5)) + "tn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: ArrayButton = {
        //: let btn = TalkingButton()
        let btn = ArrayButton()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(stepCrystal), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.transferAndJoinSmall(name: (String(showUntilFormat.suffix(6)) + "arby" + String(k_assistantKey.suffix(7)) + String(mainTransformStructureName.suffix(6)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.transferAndJoinSmall(name: (String(appHaveTailedActorKey.suffix(9)) + String(appCommonMsg.prefix(5))))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .outVideo()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(mainPriorityValue.suffix(6)) + "ns/min").underConnection(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.alongsideOk(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: EquivalentLimit = //: return EquivalentLimit(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()

    //: private lazy var connectingImg: UIImageView = {
    private lazy var connectingImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "img_match_connecting")
        imgV.image = UIImage.transferAndJoinSmall(name: (String(kCriticalPath) + String(user_boxName)))
        //: let view = UIActivityIndicatorView.init()
        let view = UIActivityIndicatorView()
        //: view.color = UIColor(hex: "#9F80FF")
        view.color = UIColor(hex: (String(show_answerId.prefix(7))))
        //: imgV.addSubview(view)
        imgV.addSubview(view)
        //: view.snp.makeConstraints { make in
        view.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-12)
            make.bottom.equalToSuperview().offset(-12)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(20)
            make.size.equalTo(20)
        }
        //: view.startAnimating()
        view.startAnimating()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: return imgV
        return imgV
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension JobViewController {
    //: private func setPlayer(isRandon: Bool) {
    private func month(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = PrepareSpecify.default.accessibleUp(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(app_cigarFunBotKey) + main_reduceString.replacingOccurrences(of: "volume", with: "o")))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func behindDrawingExpression(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(noti_floorId.prefix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: user_tabId.map{equivalentMonth(re: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: noti_attributeNormMsg.reversed(), encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ChartMoveBehavior.accounting(params: params) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.anyIndicator()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(noti_floorId.prefix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func simultaneouslySend() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ChartMoveBehavior.norNumberegration { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(constPersonMessage))].intValue
            //: self.refreshVideoCallPrice()
            self.manualForParticleOutput()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension JobViewController {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func stepCrystal() {
        //: TellEnd.share.func__pushToSubscribeAlert()
        TellEnd.share.doRange()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func rebuildDownMenu() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        quickByFresh()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        k_playShadowValue.behindImplement(eventID: app_backPlateValue)

        //: if QuantityeractionDelay.share.appConfigMode.displayMatchV4PopUp, QuantityeractionDelay.share.loginUserMode.freeCallTimes > 0,
        if QuantityeractionDelay.share.appConfigMode.displayMatchV4PopUp, QuantityeractionDelay.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.start()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        moreHide()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func replyMini() {
        //: guard TalkingSocketManager.shared.agileImageSkewy == false else {
        guard UpZone.shared.agileImageSkewy == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.paraDown(showMsg: data_colorCanCeilingMsg)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        disclose()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(user_pauseName.prefix(4)) + String(userFragmentMsg.suffix(8))).localized
        //: setPlayer(isRandon: true)
        month(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if QuantityeractionDelay.share.loginUserMode.randomVideoType == 2 && QuantityeractionDelay.share.loginUserMode.robotVideoNum > 0 {
        if QuantityeractionDelay.share.loginUserMode.randomVideoType == 2, QuantityeractionDelay.share.loginUserMode.robotVideoNum > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            k_playShadowValue.behindImplement(eventID: noti_fragmentUrl)
            //: req_getVideoInfo()
            beyondFlag()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            k_playShadowValue.behindImplement(eventID: showIconUrl)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            UpZone.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            UpZone.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            UpZone.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            UpZone.shared.errorDelegate = self
            //: restart_randomVideo()
            offActual()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func offActual(cmd: String = (String(user_sovereignString) + String(kVestName.prefix(7))), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        cable()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        behindDrawingExpression(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func anyIndicator() {
        // 停止定时器
        //: destroyWaitingTimer()
        cable()
        //: showCloseBtn()
        quickByFresh()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(app_leyMsg) + String(dataAppearanceTitle.suffix(7)) + user_quickUrl.replacingOccurrences(of: "field", with: "n")).localized
        //: setPlayer(isRandon: false)
        month(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.agileImageSkewy == false {
        if UpZone.shared.agileImageSkewy == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            UpZone.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            UpZone.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            UpZone.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            UpZone.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        manualForParticleOutput()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func cable() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func beyondFlag() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = PerCoordinator()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(user_aboutUrl) + constCreationAgainTitle.replacingOccurrences(of: "administrative", with: "fo"))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: dataWrittenTitle.map{yesPerson(immediate: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appRowLabelUrl.mouse(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.anyIndicator()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: QuantityeractionDelay.share.loginUserMode.randomVideoType = 1
                    QuantityeractionDelay.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.replyMini()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.paraDown(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: self.connectingImg.isHidden = false
            self.connectingImg.isHidden = false
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = CurrencyModel.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = PerformRetain()
                //: player.setMute(bEnable: true)
                player.graph(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.callVideoUrl, view: UIView.init())
                player.like(url: model.callVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                GateDuring.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                StrengthMomentum.pauseAbout { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.towardDiscover()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func towardDiscover() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        k_playShadowValue.behindImplement(eventID: k_rapidId)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard GateDuring.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.paraDown(showMsg: String(bytes: k_revenueUrl.map{farShake(explain: $0)}, encoding: .utf8)!.localized)
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: self.connectingImg.isHidden = true
            self.connectingImg.isHidden = true
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            GateDuring.shared.untilDense(type: 1) { succeed, result, errorModel in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.anyIndicator()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = WindowNeedMultiple()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.moreHide()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.anyIndicator()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func disclose() {
        //: if QuantityeractionDelay.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if QuantityeractionDelay.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(QuantityeractionDelay.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(QuantityeractionDelay.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func quickByFresh() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func shareOrBe() {
        //: guard TalkingSocketManager.shared.agileImageSkewy == false else {
        guard UpZone.shared.agileImageSkewy == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.columnReport()
        }
    }

    /// 匹配通话结束时
    //: @objc private func closeTool() {
    @objc private func reparation() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
            //: self.start_randomVideo()
            self.replyMini()
        }
    }
}

// MARK: - IndicatorDirection

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension JobViewController: IndicatorDirection {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func faceBlock(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = TempVideo.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(noti_floorId.prefix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: showGameReceiveMsg.reversed(), encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: notiEraseEhUrl.map{plainPull(sequence: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: user_verticalFormat.map{$0^113}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = FilterInfrastructure.randow
        //: if QuantityeractionDelay.share.loginUserMode.freeCallTimes > 0 && QuantityeractionDelay.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if QuantityeractionDelay.share.loginUserMode.freeCallTimes > 0 && QuantityeractionDelay.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(notiRetMessage) + String(user_greetUrl)))
            //: TalkingSocketManager.shared.isFreeCall = true
            UpZone.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: user_tabId.map{equivalentMonth(re: $0)}, encoding: .utf8)!: (String(const_searcherFormat)), String(bytes: show_reinTitle.map{disappearExplore(price: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        UpZone.shared.commonCrop(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            UpZone.shared.commonCrop(info: [String(bytes: user_tabId.map{equivalentMonth(re: $0)}, encoding: .utf8)!: (String(constInsertString)), String(bytes: show_reinTitle.map{disappearExplore(price: $0)}, encoding: .utf8)!: [(String(app_furtherId.suffix(5))): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.offActual(cmd: (String(dataPinFemaleString) + dataHundredFlowFormat.replacingOccurrences(of: "force", with: "h")))
            //: }, repeats: false)
        }, repeats: false)
        //: connectingImg.isHidden = false
        connectingImg.isHidden = false
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func beNet(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(app_furtherId.suffix(5)))].intValue
    }
}

// MARK: - MasterRange【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension JobViewController: MasterRange {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func plusBy(data: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func minimize(data: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func nonePriority(msg: String, data: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func appropriateProperty(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            AlongAt.crushBy(body: String(bytes: user_weightCoreContinuingData.reversed(), encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = TempVideo.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            dismissAcross(toUid: String(uid))
        }
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: stop_randomVideo()
        anyIndicator()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.everyAlways()
        //: var newData = data
        var newData = data
        //: newData["isRandomVCPush"] = true
        newData[(String(data_accessTitle) + String(noti_platProductionFormat))] = true
        //: QuantityeractionDelay.share.start1v1TalkCall(info: newData, situation: .Calling)
        QuantityeractionDelay.share.extensive(info: newData, situation: .Calling)
    }
}

// MARK: - SingleExtra【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension JobViewController: SingleExtra {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func shadowBrush(data: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        offActual(cmd: (dataButtTenseValue.replacingOccurrences(of: "recover", with: "ef") + "seCall"))
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func closePainter(data: [String: Any]) {}
}

// MARK: - StatusKit

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension JobViewController: StatusKit {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func frequencyComment(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: connectingImg.isHidden = true
        connectingImg.isHidden = true
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(const_cycleUrl) + data_rawFormat.lowercased()) { // 取消通话异常
            //: stop_randomVideo()
            anyIndicator()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(dataPinFemaleString) + dataHundredFlowFormat.replacingOccurrences(of: "force", with: "h")), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            anyIndicator()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(noti_floorId.prefix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.paraDown(showMsg: msg)
            //: uploadFailTime(type: 2)
            signatureIdentity(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(dataPinFemaleString) + dataHundredFlowFormat.replacingOccurrences(of: "force", with: "h")), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(main_oldKey.replacingOccurrences(of: "temporary", with: "re") + String(data_jumpFileFormat))].intValue), block: { [weak self] timer in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(noti_floorId.prefix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.offActual(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(show_selectKey) + notiPartStr.replacingOccurrences(of: "attach", with: "ue") + "Call"), errorNo == ObtainExtendScreen.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            anyIndicator()
            //: guard QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue else { return }
            //: TellEnd.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            TellEnd.share.somePush(clickEvent: showCapMessage, sufficient: false)
            //: uploadFailTime(type: 1)
            signatureIdentity(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        offActual(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension JobViewController {
    //: func uploadFailTime(type: Int) {
    func signatureIdentity(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(notiAddSeaString.replacingOccurrences(of: "shade", with: "il") + "Type")] = type
        //: parm["callWaitingTime"] = time
        parm[(String(kPropertyUrl.prefix(6)) + "itin" + String(noti_wrapPath.prefix(4)) + showToleranceId.replacingOccurrences(of: "beside", with: "e"))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        k_playShadowValue.signal(eventID: data_publishData, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func dismissAcross(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(String(kPropertyUrl.prefix(6)) + "itin" + String(noti_wrapPath.prefix(4)) + showToleranceId.replacingOccurrences(of: "beside", with: "e"))] = time
        //: parm["toUid"] = toUid
        parm[(String(mainHiBackName))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        k_playShadowValue.signal(eventID: main_prepareOrangeUrl, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension JobViewController {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func manualForParticleOutput() {
        //: guard QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !QuantityeractionDelay.share.loginUserMode.loungePlus
        vipIcon.isHidden = !QuantityeractionDelay.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = FilterInfrastructure.randow
        //: if QuantityeractionDelay.share.loginUserMode.freeCallTimes > 0 && QuantityeractionDelay.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if QuantityeractionDelay.share.loginUserMode.freeCallTimes > 0, QuantityeractionDelay.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if QuantityeractionDelay.share.appUserConfigMode.callBtnStyle == 1 {
            if QuantityeractionDelay.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(mainSensorData.prefix(6)) + " min").localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = QuantityeractionDelay.share.loginUserMode.loungePlus ? vipFee:fee
            let price = QuantityeractionDelay.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(notiOurString.prefix(6))).underConnection(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(notiOurString.prefix(6))).underConnection(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.wantStream(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = QuantityeractionDelay.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = QuantityeractionDelay.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = QuantityeractionDelay.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = QuantityeractionDelay.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !QuantityeractionDelay.share.loginUserMode.loungePlus
            priceStackView.isHidden = !QuantityeractionDelay.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func plus() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
        //: view.addSubview(connectingImg)
        view.addSubview(connectingImg)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func deadline() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(dataDoingRapidMsg + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }

        //: connectingImg.snp.makeConstraints { make in
        connectingImg.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
            make.size.equalTo(CGSize(width: actualWidth(w: 262), height: actualWidth(w: 102)))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func averageSlide() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(manualForParticleOutput),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: noti_secondUrl,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(simultaneouslySend),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: app_progressMsg,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeTool), name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(reparation), name: show_playAwayTitle, object: nil)
    }
}
