
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let data_signatureStr:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let userDelayUrl:[UInt8] = [0x26,0x3a,0x37]

private func shortSecure(bounce num: UInt8) -> UInt8 {
    return num ^ 83
}

/*: "headPic" :*/
fileprivate let kShadowKey:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let show_generalStr:[Character] = ["n","i","c","k","n"]
fileprivate let k_graphUrl:String = "adiscover"

/*: "birthday" :*/
fileprivate let kUpperData:String = "bforcerthd"
fileprivate let app_availableMsg:String = "AY"

/*: "videoUrl" :*/
fileprivate let mainSelecterName:String = "videoUhappy monster orientation"
fileprivate let constInstallPeopleTitle:String = "rshall"

/*: "cover" :*/
fileprivate let noti_tradeMsg:[Character] = ["c","o","v","e","r"]

/*: "location" :*/
fileprivate let kShapeTitle:String = "lopacktio"
fileprivate let kComputeValue:String = "N"

/*: "cmd" :*/
fileprivate let dataCeilingQueryTitle:[UInt8] = [0xfa,0xf4,0xfd]

/*: "refuseCall" :*/
fileprivate let app_currentlyUrl:[Character] = ["r","e","f","u","s"]
fileprivate let constMeetingBackgroundKey:String = "alongside check singleeCall"

/*: "data" :*/
fileprivate let dataDismissParticleUrl:[UInt8] = [0x3e,0x3b,0x2e,0x3b]

private func lineClearly(country num: UInt8) -> UInt8 {
    return num ^ 90
}

/*: "Call interruption" :*/
fileprivate let mainExactlyStr:[Character] = ["C","a","l","l"," ","i","n","t"]
fileprivate let notiAngleId:[Character] = ["e","r"]
fileprivate let data_buildFormat:[Character] = ["r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let main_borderAgileMsg:[Character] = ["s","t","o","p","C"]
fileprivate let data_lostStr:[Character] = ["a","l","l"]

/*: "logId" :*/
fileprivate let constBodyValue:[UInt8] = [0x6d,0x6e,0x66,0x48,0x65]

private func stripDiscount(player num: UInt8) -> UInt8 {
    return num ^ 1
}

/*: "cancelCall" :*/
fileprivate let const_availableData:String = "creservec"
fileprivate let user_arcMsg:String = "running"

/*: "No answer" :*/
fileprivate let main_personalBecomeString:[Character] = ["N","o"," ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let app_priorDirectlyData:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let userGenerateValue:[Character] = ["S","h","e"," ","h","a","s"," ","h","u"]
fileprivate let dataThumbString:[Character] = ["n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let mainMiddleUrl:[Character] = ["c","a"]
fileprivate let app_veryEffStr:String = "beyond then cornerncelSex"

/*: "He has hung up" :*/
fileprivate let const_intervalPath:String = "He has resource short blue orange"
fileprivate let noti_rulingStr:String = "question certainhung up"

/*: "endType" :*/
fileprivate let kMoveStrokeName:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let user_easeValue:String = "stopSpure successful"
fileprivate let main_additionLawmakerName:[Character] = ["e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let main_curveString:[UInt8] = [0x6,0x2b,0x30,0x32,0x23,0x23,0x26,0x20,0x26,0x22,0x2b,0x31,0xdd,0x24,0x2c,0x29,0x21,0xdd,0x20,0x2c,0x26,0x2b,0x30,0xe9,0xdd,0x20,0x1e,0x29,0x29,0xdd,0x25,0x1e,0x30,0xdd,0x22,0x2b,0x21,0x22,0x21]

fileprivate func roundingMarker(shall num: UInt8) -> UInt8 {
    let value = Int(num) + 67
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "The other hung up the phone" :*/
fileprivate let kBrownString:String = "flexible dynamics provisionThe "
fileprivate let const_secureData:String = "moment absolute staff hung "
fileprivate let dataColumnRouteThoroughString:[Character] = ["u"]
fileprivate let noti_thumbMsg:String = "bound upon clearly commit panp the"

/*: "type" :*/
fileprivate let app_existingKey:[Character] = ["t","y","p","e"]

/*: "jumpUrl" :*/
fileprivate let noti_linkFormat:String = "all mission exercise sequence capturejumpUrl"

/*: "Cancel" :*/
fileprivate let user_bucketString:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let user_tradeKey:[Character] = ["G","e","t"," ","g","o","l","d"," ","c","o","i","n","s"]

/*: "refuseSex" :*/
fileprivate let user_calculateMessage:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let showAreaGestureOnceStr:String = "accuse key dragonAcc"
fileprivate let data_infrastructureMentionStr:[Character] = ["e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let showMouseStatUrl:[Character] = ["a","c","c","e","p","t","C","a","l"]
fileprivate let const_formData:[Character] = ["l"]

/*: "Calls are cancelled" :*/
fileprivate let const_draftRatingKey:String = "appropriate lawmaker yesterday index momentCall"
fileprivate let appLimitData:[Character] = ["a","n","c","e","l","l","e","d"]

/*: "Continue matching with other users？" :*/
fileprivate let showGrandfatherName:[UInt8] = [0xa,0x36,0x35,0x3b,0x30,0x35,0x3c,0x2c,0xe7,0x34,0x28,0x3b,0x2a,0x2f,0x30,0x35,0x2e,0xe7,0x3e,0x30,0x3b,0x2f,0xe7,0x36,0x3b,0x2f,0x2c,0x39,0xe7,0x3c,0x3a,0x2c,0x39,0x3a,0xb6,0x83,0x66]

fileprivate func estimatedApplication(error num: UInt8) -> UInt8 {
    let value = Int(num) - 199
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Yes" :*/
fileprivate let kNativeProcedureValue:[Character] = ["Y","e","s"]

/*: "The call has ended" :*/
fileprivate let dataBehaviorLanguageMsg:String = "The caelement quick search lack"
fileprivate let kMightPath:String = "least thans end"
fileprivate let notiMergeGeneralData:String = "portd"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  StatusEven.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let app_saltEagerPath = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class StatusEven: ScaleViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: DismissModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SpotSkin? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: ObjectChain? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: ComposeFactoryCanvas? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        UpZone.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        UpZone.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        UpZone.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: data_signatureStr.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! WithoutImplement
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.everyMount()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            game()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            scope(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.switche()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        bulletMemory()
        //: setupSubViewsConstraint()
        selectContainer()
        //: initNotificationObserver()
        beyondCurrencyOdd()
        //: req_callGetUserInfo()
        fireSkirt()
        //: timeoutRules()
        sinceTick()
        //: setUpBGMPlayer()
        contrast()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.permissionPoint()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
            if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = SignaturePriority(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.agileImageSkewy = false
        UpZone.shared.agileImageSkewy = false
        //: TalkingSocketManager.shared.isCalling = false
        UpZone.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.scanThroughJob()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: FutureView = {
        //: let v = TalkingBGVideoView()
        let v = FutureView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.officialIn(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: SingleSensor = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = SingleSensor(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: EndView = {
        //: let v = TalkingVideoWindowView()
        let v = EndView()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = dataTabUrl - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if CoordinatorExtra.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: dataDoingRapidMsg + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.challengeModel()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: AroundSubtle = {
        //: let m = TalkingVideoChatManager()
        let m = AroundSubtle()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension StatusEven {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func fireSkirt() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AroundSubtle.levelEx(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: userDelayUrl.map{shortSecure(bounce: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(kShadowKey))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(String(show_generalStr) + k_graphUrl.replacingOccurrences(of: "discover", with: "me"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(kUpperData.replacingOccurrences(of: "force", with: "i") + app_availableMsg.lowercased())].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(mainSelecterName.prefix(6)) + constInstallPeopleTitle.replacingOccurrences(of: "shall", with: "l"))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(String(noti_tradeMsg))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(kShapeTitle.replacingOccurrences(of: "pack", with: "ca") + kComputeValue.lowercased())].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.aroundSize()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.design()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func design() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = QueryEven.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = QueryEven.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.secondObject(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.listenBack(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension StatusEven {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func game() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        StrengthMomentum.pauseAbout { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.paraDown(showMsg: k_implementMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(app_currentlyUrl) + String(constMeetingBackgroundKey.suffix(5))), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: userDelayUrl.map{shortSecure(bounce: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.remoteBy()
                //: return
                return
            }

            //: self.startPreview()
            self.permissionPoint()
            //: self.stopBGM()
            self.recommendOrientation()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SpotSkin.paint(type: .Response)
            //: self.startBGM()
            self.everyMount()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func permissionPoint() {
        //: self.videoManager.startPreview()
        self.videoManager.columnReport()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func scanThroughJob() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.recommendOrientation()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        SingleConnect.beyondTotal()
        //: TalkingSocketManager.shared.callerDelegate = nil
        UpZone.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        UpZone.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        UpZone.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func remoteBy(animated: Bool = true) {
        //: self.stopBGM()
        self.recommendOrientation()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func challengeModel() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.key()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.design()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func todayIn() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.dimensionBe()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.design()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.bubbleOf()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func bubbleOf() {
        //: guard QuantityeractionDelay.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = ComposeFactoryCanvas()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.cancelHold()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension StatusEven {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func beyondCurrencyOdd() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(existSignificant(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(beyondScene(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: noti_tableMessage,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(globalFront(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: noti_gestureTechniqueMsg,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        app_saltEagerPath.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func existSignificant(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            remoteBy()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func globalFront(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard UpZone.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        UpZone.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        UpZone.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        UpZone.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.balance()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func beyondScene(notification: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.design()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension StatusEven: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.paraDown(showMsg: (String(mainExactlyStr) + String(notiAngleId) + String(data_buildFormat)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(main_borderAgileMsg) + String(data_lostStr)), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (const_availableData.replacingOccurrences(of: "reserve", with: "an") + "elCal" + user_arcMsg.replacingOccurrences(of: "running", with: "l")), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(app_currentlyUrl) + String(constMeetingBackgroundKey.suffix(5))), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: userDelayUrl.map{shortSecure(bounce: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.publisher()
                //: TalkingSocketManager.shared.agileImageSkewyVC = nil
                UpZone.shared.agileImageSkewyVC = nil
            }
            //: popCurrentViewController()
            remoteBy()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension StatusEven {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func contrast() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? HandleDivide.Call : HandleDivide.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SpotSkin.paint(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func everyMount() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.sustainable()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func recommendOrientation() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.prepare()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension StatusEven {
    /// 超时定时器
    //: private func timeoutRules() {
    private func sinceTick() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? notiNaturalTitle : self.chatModel?.outAdvanced()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            restrictionOf()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.restrictionOf()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func restrictionOf() {
        //: guard !TalkingSocketManager.shared.agileImageSkewy else { return }
        guard !UpZone.shared.agileImageSkewy else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.paraDown(showMsg: (String(main_personalBecomeString)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (const_availableData.replacingOccurrences(of: "reserve", with: "an") + "elCal" + user_arcMsg.replacingOccurrences(of: "running", with: "l")), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(app_currentlyUrl) + String(constMeetingBackgroundKey.suffix(5))), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: userDelayUrl.map{shortSecure(bounce: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        UpZone.shared.commonCrop(info: socketDict)
        //: popCurrentViewController()
        remoteBy()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension StatusEven {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func computeBan(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(app_priorDirectlyData))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.accessibleOval(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.findFrame()
            //: createTalkRoom()
            todayIn()
            //: self.stopBGM()
            self.recommendOrientation()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func scope(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.key()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.officialIn(show: false)
        //: self.stopBGM()
        self.recommendOrientation()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.findFrame()
        //: self.createTalkRoom()
        self.todayIn()
    }
}

// MARK: - MasterRange【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension StatusEven: MasterRange {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func plusBy(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userGenerateValue) + String(dataThumbString)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(mainMiddleUrl) + String(app_veryEffStr.suffix(7)))].stringValue == CommonAmong.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_intervalPath.prefix(7)) + String(noti_rulingStr.suffix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.paraDown(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.publisher()
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }

        //: popCurrentViewController()
        remoteBy()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func appropriateProperty(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        scope(logId: json[String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func minimize(data: [String: Any]) {
        //: TalkingAlertShow.hideAlert()
        TimingVersus.stripNow()
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(kMoveStrokeName))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(userGenerateValue) + String(dataThumbString)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(user_easeValue.prefix(5)) + String(main_additionLawmakerName))].stringValue == CommonAmong.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(const_intervalPath.prefix(7)) + String(noti_rulingStr.suffix(7))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue {
            if QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: main_curveString.map{roundingMarker(shall: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(kBrownString.suffix(4)) + "other" + String(const_secureData.suffix(6)) + String(dataColumnRouteThoroughString) + String(noti_thumbMsg.suffix(5)) + " phone").localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.paraDown(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.publisher()
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.nextSatisfy()
        //: popCurrentViewController()
        remoteBy()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func nonePriority(msg: String, data: [String: Any]) {
        //: guard QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(String(app_existingKey))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(noti_linkFormat.suffix(7)))] as? String else { return }
            //: let httpsUrl = QuantityeractionDelay.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = QuantityeractionDelay.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ImageTranslate()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TellEnd.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            TellEnd.share.atRein(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TimingVersus.noEntityGuide(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (String(user_bucketString)).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(user_tradeKey)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                TimingVersus.stripNow()
                //: } rightBlock: {
            } rightBlock: {
                //: TellEnd.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                TellEnd.share.somePush(clickEvent: const_thirdOldValue, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func magnitude(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.curFee(giftArr: giftArr)
    }
}

// MARK: - SingleExtra【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension StatusEven: SingleExtra {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func shadowBrush(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userGenerateValue) + String(dataThumbString)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(user_calculateMessage))].stringValue == CommonAmong.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(const_intervalPath.prefix(7)) + String(noti_rulingStr.suffix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.paraDown(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.publisher()
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }

        //: popCurrentViewController()
        remoteBy()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func closePainter(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        scope(logId: json[String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - StatusKit【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension StatusEven: StatusKit {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func frequencyComment(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(app_currentlyUrl) + String(constMeetingBackgroundKey.suffix(5))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.paraDown(showMsg: (String(main_personalBecomeString)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.publisher()
                //: TalkingSocketManager.shared.agileImageSkewyVC = nil
                UpZone.shared.agileImageSkewyVC = nil
            }
            //: popCurrentViewController()
            remoteBy()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(showAreaGestureOnceStr.suffix(5)) + String(data_infrastructureMentionStr)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue else { return }
            //: TellEnd.share.func__jumpToWebRecharge(sufficient: false)
            TellEnd.share.somePush(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(showAreaGestureOnceStr.suffix(5)) + String(data_infrastructureMentionStr)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.paraDown(showMsg: msg)
            //: popCurrentViewController()
            remoteBy()
        }
    }
}

// MARK: - ReduceTitle【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension StatusEven: ReduceTitle {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func fragmentFactory() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(showMouseStatUrl) + String(const_formData)), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: userDelayUrl.map{shortSecure(bounce: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func theItem() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.paraDown(showMsg: (String(const_draftRatingKey.suffix(4)) + "s are c" + String(appLimitData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (const_availableData.replacingOccurrences(of: "reserve", with: "an") + "elCal" + user_arcMsg.replacingOccurrences(of: "running", with: "l")), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.paraDown(showMsg: (String(const_draftRatingKey.suffix(4)) + "s are c" + String(appLimitData)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(app_currentlyUrl) + String(constMeetingBackgroundKey.suffix(5))), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: userDelayUrl.map{shortSecure(bounce: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: if chatModel?.isRandomVCPush ?? false {
            if chatModel?.isRandomVCPush ?? false {
                //: TalkingAlertShow.alert(title: nil, message: "Continue matching with other users？".localized, leftBtnTitle: "No".localized, rightBtnTitle: "Yes".localized, leftBlock: {
                TimingVersus.noEntityGuide(title: nil, message: String(bytes: showGrandfatherName.map{estimatedApplication(error: $0)}, encoding: .utf8)!.localized, leftBtnTitle: "No".localized, rightBtnTitle: (String(kNativeProcedureValue)).localized, leftBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: false)
                    self.say(isNewRandow: false)
                    //: }, rightBlock: {
                }, rightBlock: {
                    //: self.randowCallIsEndTool(isNewRandow: true)
                    self.say(isNewRandow: true)
                    //: })
                })
                //: return
                return
                    //: } else {
            } else {
                //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
                self.paraDown(showMsg: (String(dataBehaviorLanguageMsg.prefix(6)) + "ll ha" + String(kMightPath.suffix(5)) + notiMergeGeneralData.replacingOccurrences(of: "port", with: "e")).localized)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(main_borderAgileMsg) + String(data_lostStr)), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            }
        }

        //: popCurrentViewController()
        remoteBy()
    }

    /// 随机匹配通话挂断时
    //: func randowCallIsEndTool(isNewRandow: Bool) {
    func say(isNewRandow: Bool) {
        //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
        self.paraDown(showMsg: (String(dataBehaviorLanguageMsg.prefix(6)) + "ll ha" + String(kMightPath.suffix(5)) + notiMergeGeneralData.replacingOccurrences(of: "port", with: "e")).localized)
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
        UpZone.shared.commonCrop(info: [String(bytes: dataCeilingQueryTitle.map{$0^153}, encoding: .utf8)!: (String(main_borderAgileMsg) + String(data_lostStr)), String(bytes: dataDismissParticleUrl.map{lineClearly(country: $0)}, encoding: .utf8)!: [String(bytes: constBodyValue.map{stripDiscount(player: $0)}, encoding: .utf8)!: self.chatModel?.logId]])

        //: if !isNewRandow{
        if !isNewRandow {
            //: if let targetVC = navigationController?.viewControllers.first(where: { $0 is RandomMatchVC }) {
            if let targetVC = navigationController?.viewControllers.first(where: { $0 is PlateSessionName }) {
                //: navigationController?.viewControllers = [targetVC]
                navigationController?.viewControllers = [targetVC]
                //: navigationController?.popToViewController(targetVC, animated: false)
                navigationController?.popToViewController(targetVC, animated: false)
            }
            //: }else {
        } else {
            //: popCurrentViewController()
            remoteBy()
            //: NotificationCenter.default.post(name: MATCHVIDEOCALL_END_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: show_playAwayTitle, object: nil)
        }
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func easyFromProcessing(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.pastYear(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func full(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.officialIn(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.towardWill(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.design()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func everySumegrity() {
        //: TalkingSocketManager.shared.agileImageSkewyVC = self
        UpZone.shared.agileImageSkewyVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = ObjectChain.followPing()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.menu()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.searcherEnableto()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.moreHide()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        remoteBy()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func duringMini() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension StatusEven {
    /// 添加视图
    //: private func setupSubviews() {
    private func bulletMemory() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.nameDiscover(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func selectContainer() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
