
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dataProjectionPerName:[UInt8] = [0xac,0xb1,0xac,0xb7,0x6b,0xa6,0xb2,0xa7,0xa8,0xb5,0x7d,0x6c,0x63,0xab,0xa4,0xb6,0x63,0xb1,0xb2,0xb7,0x63,0xa5,0xa8,0xa8,0xb1,0x63,0xac,0xb0,0xb3,0xaf,0xa8,0xb0,0xa8,0xb1,0xb7,0xa8,0xa7]

fileprivate func evolutionMulti(about num: UInt8) -> UInt8 {
    let value = Int(num) + 189
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "#128CFF" :*/
fileprivate let show_exploreAlwaysString:String = "receiver tine brown#128CFF"

/*: "Feedback" :*/
fileprivate let kAlongTitle:String = "Feedbacaccess editor res isolate found"
fileprivate let user_successfullyValue:String = "line"

/*: "#8370FF" :*/
fileprivate let app_skipId:[Character] = ["#","8","3","7","0"]
fileprivate let show_upTitle:String = "option"

/*: "Quick Greeting" :*/
fileprivate let showSunEqualPath:[Character] = ["Q","u","i","c","k"]
fileprivate let noti_segmentCityValue:[Character] = [" ","G","r","e","e","t","i","n","g"]

/*: "Beauty Photos" :*/
fileprivate let notiFutureMessage:[Character] = ["B","e","a","u","t","y"," ","P"]
fileprivate let showSupportTarCityAnonymousValue:String = "hclickos"

/*: "url" :*/
fileprivate let const_openlyStr:String = "uraltogether"

/*: "unlockGift" :*/
fileprivate let userFreshStr:String = "UNLO"

/*: "isBurn" :*/
fileprivate let user_protectionName:[UInt8] = [0xc6,0xd0,0x9f,0xd2,0xcf,0xcb]

fileprivate func analysisLoanExist(broadcast num: UInt8) -> UInt8 {
    let value = Int(num) - 93
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let k_deadlineMsg:String = "accept remarkgiftId"

/*: "rYMsgType" :*/
fileprivate let mainTechniquePopTitle:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:CallCustom" :*/
fileprivate let noti_propertyUrl:[Character] = ["G","J",":","C","a","l","l","C","u","s"]
fileprivate let mainFoundFormat:String = "totopic"

/*: "changeDirection" :*/
fileprivate let noti_addressReachFormat:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i","o","n"]

/*: "extra" :*/
fileprivate let mainRoundTensionValue:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let noti_aStyleStr:String = "msgInfocloud generate pressure"

/*: "toUid" :*/
fileprivate let kEnjoyUrl:String = "toUidtoward repeat may while below"

/*: "wantGift" :*/
fileprivate let dataLapData:String = "wantGibrick priority warning say"
fileprivate let k_openlyFormat:String = "fsatisfy"

/*: "jumpKey" :*/
fileprivate let k_betterOperationName:String = "jumpKeyscan rating replace spread way"

/*: "logType" :*/
fileprivate let user_targetOvalMessage:String = "logTypevertical cover"

/*: "content" :*/
fileprivate let userMovieTitle:[UInt8] = [0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "msgType" :*/
fileprivate let appOthersUrl:[UInt8] = [0x42,0x5c,0x48,0x7b,0x56,0x5f,0x4a]

private func remindObserve(even num: UInt8) -> UInt8 {
    return num ^ 47
}

/*: "txt" :*/
fileprivate let noti_childUrl:[Character] = ["t","x","t"]

/*: "message" :*/
fileprivate let const_givingKey:[UInt8] = [0x2f,0x27,0x31,0x31,0x23,0x25,0x27]

private func bagSpeaker(popular num: UInt8) -> UInt8 {
    return num ^ 66
}

/*: "replayId" :*/
fileprivate let appMissingTitle:[UInt8] = [0x4,0x13,0x6,0x1a,0x17,0xf,0x3f,0x12]

/*: "type" :*/
fileprivate let mainTalkStr:[Character] = ["t","y","p","e"]

/*: "TxtMsg" :*/
fileprivate let mainDetectUrl:[Character] = ["T","x","t","M","s","g"]

/*: "params" :*/
fileprivate let const_despiteValue:[UInt8] = [0x10,0x1,0x12,0x1,0xd,0x13]

private func mapCertain(appropriate num: UInt8) -> UInt8 {
    return num ^ 96
}

/*: "MultiQuickMsg" :*/
fileprivate let k_interactionFormat:[Character] = ["M","u","l","t","i","Q","u","i","c","k","M","s","g"]

/*: "QuickMsg" :*/
fileprivate let data_rankTitle:[Character] = ["Q","u","i"]
fileprivate let k_clusterHealthyMsg:String = "ckMsgauto path"

/*: "txtId" :*/
fileprivate let notiYearMessage:String = "search infrastructure marktxtId"

/*: "Voice hello download failed. Procedure" :*/
fileprivate let noti_createEvenMsg:[UInt8] = [0x9d,0xb6,0xb0,0xaa,0xac,0x67,0xaf,0xac,0xb3,0xb3,0xb6,0x67,0xab,0xb6,0xbe,0xb5,0xb3,0xb6,0xa8,0xab,0x67,0xad,0xa8,0xb0,0xb3,0xac,0xab,0x75,0x67,0x97,0xb9,0xb6,0xaa,0xac,0xab,0xbc,0xb9,0xac]

fileprivate func unknownValueDisk(plain num: UInt8) -> UInt8 {
    let value = Int(num) + 185
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let notiInvitationTitle:String = "L"
fileprivate let show_phaseFloorKey:String = "ENGTH"

/*: "audio" :*/
fileprivate let app_ownVerticalId:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let noti_customerFormat:String = "audioIddecide background"

/*: "audioLength" :*/
fileprivate let userDecreaseFormat:[UInt8] = [0x74,0x60,0x71,0x7c,0x7a,0x59,0x70,0x7b,0x72,0x61,0x7d]

private func ifFeatureReach(pic num: UInt8) -> UInt8 {
    return num ^ 21
}

/*: "audioSandbox" :*/
fileprivate let show_opName:[Character] = ["a","u","d","i","o","S","a","n"]
fileprivate let userLaterMessage:[Character] = ["d","b","o","x"]

/*: "senderId" :*/
fileprivate let noti_arcTitle:[UInt8] = [0x64,0x49,0x72,0x65,0x64,0x6e,0x65,0x73]

/*: "AudioMsg" :*/
fileprivate let notiEditContextUrl:String = "Audisale agree"

/*: "Can't send blank message" :*/
fileprivate let showMonthData:[Character] = ["C","a","n","\'","t"," ","s","e","n","d"," ","b","l","a","n","k"," ","m","e","s","s"]
fileprivate let k_nowMethodPath:[Character] = ["a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let k_extraValue:String = "quotprovider"
fileprivate let k_blackUrl:[Character] = ["M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let appMomentumVisibleValue:[Character] = ["q","u","o","t","e","M","s","g","I","n","f","o"]

/*: "contentType" :*/
fileprivate let main_cropBrownValue:[UInt8] = [0xe8,0xe4,0xe5,0xff,0xee,0xe5,0xff,0xdf,0xf2,0xfb,0xee]

private func atAbout(restore num: UInt8) -> UInt8 {
    return num ^ 139
}

/*: "ImgMsg" :*/
fileprivate let noti_trustName:String = "ImgMsgaffect party"

/*: "img" :*/
fileprivate let kCityName:String = "readingg"

/*: "imageUri" :*/
fileprivate let constTermsOldFormat:[UInt8] = [0x3f,0x43,0x37,0x3d,0x3b,0x2b,0x48,0x3f]

fileprivate func layRounding(intensity num: UInt8) -> UInt8 {
    let value = Int(num) + 42
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "customSend" :*/
fileprivate let kIndeedTitle:[UInt8] = [0x53,0x65,0x63,0x64,0x5f,0x5d,0x43,0x55,0x5e,0x54]

fileprivate func zzWhenMaterial(secular num: UInt8) -> UInt8 {
    let value = Int(num) + 16
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "pic" :*/
fileprivate let app_subjectReadyData:String = "piexclusive"

/*: "VideoMsg" :*/
fileprivate let mainValueWithinFormat:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "video" :*/
fileprivate let user_sequenceValue:String = "VIDEO"

/*: "coverImg" :*/
fileprivate let appTemporaryKey:[UInt8] = [0x67,0x6d,0x49,0x72,0x65,0x76,0x6f,0x63]

/*: "videoUrl" :*/
fileprivate let kCurUrl:[UInt8] = [0xb1,0xa4,0x9f,0xa0,0xaa,0x90,0xad,0xa7]

fileprivate func pmStructure(way num: UInt8) -> UInt8 {
    let value = Int(num) + 197
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "duration" :*/
fileprivate let appVariableId:[UInt8] = [0x17,0x6,0x1,0x12,0x7,0x1a,0x1c,0x1d]

private func collectPremium(to num: UInt8) -> UInt8 {
    return num ^ 115
}

/*: "len" :*/
fileprivate let kBlendStr:[UInt8] = [0x6e,0x65,0x6c]

/*: "Documents/User/voice/" :*/
fileprivate let k_curveMustMsg:String = "Documemark spring detect fatal"
fileprivate let userPersonalData:String = "User/vtun child way"

/*: "Documents/User/record/" :*/
fileprivate let user_yearMsg:[Character] = ["D","o","c","u","m","e","n","t","s","/","U","s","e","r","/","r","e","c","o"]
fileprivate let user_reasonString:[Character] = ["r","d","/"]

/*: "msgIncome" :*/
fileprivate let k_currentModelPath:String = "msgIncoplayer agent phase"
fileprivate let const_pressName:String = "sovereigne"

/*: "messageType" :*/
fileprivate let appJoinSheKey:String = "mcenterssa"

/*: "WantGift" :*/
fileprivate let data_againstString:[Character] = ["W","a","n","t","G","i","f","t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let main_divideStripMsg:[Character] = ["_","P","r","i","v","a","t","e","C","h","a","t"]
fileprivate let data_actionBirthdayRemoveMessage:[Character] = ["T","i","p","s"]
fileprivate let main_toMessage:String = "Title_inmate extent step"

/*: "msgId" :*/
fileprivate let mainWayMomentString:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let user_depthName:[Character] = ["t","o","t","a","l","M","f"]
fileprivate let dataBlackFormat:[Character] = ["C","o","i","n"]

/*: "attachSysMsg" :*/
fileprivate let noti_itemKey:String = "availablettavailablech"
fileprivate let user_guideData:[Character] = ["S","y","s","M","s","g"]

/*: "intimatePhoto" :*/
fileprivate let const_flagPath:[Character] = ["i","n","t","i","m","a","t","e","P","h","o"]
fileprivate let data_tipValue:[Character] = ["t","o"]

/*: "totalIntimate" :*/
fileprivate let appTimingDismissHideString:String = "totalItrue index long empty element"

/*: "uid" :*/
fileprivate let dataReadMovieMsg:[UInt8] = [0xa7,0xbb,0xb6]

private func searchionRemote(dense num: UInt8) -> UInt8 {
    return num ^ 210
}

/*: "intimate" :*/
fileprivate let user_norTitle:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let k_editDialogData:[UInt8] = [0x97,0xb5,0xc2,0x7b,0xc8,0x74,0xc9,0xc7,0xb9,0x74,0xc8,0xbc,0xb9,0x74,0xba,0xc9,0xc2,0xb7,0xc8,0xbd,0xc3,0xc2,0x74,0xb8,0xc9,0xb9,0x74,0xc8,0xc3,0x74,0xcd,0xc3,0xc9,0xc6,0x74,0xc0,0xb5,0xb7,0xbf,0x74,0xc3,0xba,0x74,0xbd,0xc2,0xc8,0xbd,0xc1,0xb5,0xb7,0xcd,0x74,0xc0,0xb9,0xca,0xb9,0xc0,0x82]

fileprivate func undertakeFee(until num: UInt8) -> UInt8 {
    let value = Int(num) - 84
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let notiDrawerTailMessage:String = "resource accountinggiftNum"

/*: "pkgItemsetId" :*/
fileprivate let show_creationValue:[Character] = ["p","k","g","I","t","e","m"]
fileprivate let noti_willPath:String = "setIdstop anonymous"

/*: "from" :*/
fileprivate let kUsId:[Character] = ["f","r","o","m"]

/*: "wantGiftId" :*/
fileprivate let data_publishForeValue:String = "randomnt"
fileprivate let appElementUniversalFormat:String = "Idhead capture talk address save"

/*: "WantGiftMsg" :*/
fileprivate let main_relatedMsg:String = "norm page plus roleWantG"
fileprivate let constCloudFormat:[Character] = ["i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let appCharacterizeData:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let kMightName:String = "View channel verb increase quit"

/*: "Notes" :*/
fileprivate let kHiddenAttentionUrl:[Character] = ["N","o","t","e","s"]

/*: "Report" :*/
fileprivate let showEntityKey:[Character] = ["R","e","p","o","r","t"]

/*: "View read receipts?" :*/
fileprivate let const_forePath:String = "small line publisherView "
fileprivate let userCurriculumIndexId:String = "floor spell warning coordinatorread rec"
fileprivate let show_spellUrl:String = "eipts?leading audience"

/*: "She" :*/
fileprivate let constTaskMessage:String = "one falseShe"

/*: "%@ is using too low a version" :*/
fileprivate let userDisappearExceptUrl:String = "%@ isdeny pin"
fileprivate let const_respectiveMsg:String = "enough dark circle one too"
fileprivate let dataSubtleValue:String = "sure unique pause taa ve"
fileprivate let userIncomePath:String = "pointion"

/*: "Shoot video at least 5 seconds" :*/
fileprivate let showStrokeStrengthMessage:[Character] = ["S","h","o","o","t"," ","v","i"]
fileprivate let appNearValue:[Character] = ["d","e","o"," ","a","t"," ","l","e","a","s","t"," ","5"," ","s","e","c","o","n","d","s"]

/*: "Upload failed" :*/
fileprivate let kOurStr:String = "para register multiple lowUploa"
fileprivate let kTitleWhichString:String = "D"

/*: "播放状态: :*/
fileprivate let userChoiceData:[Character] = ["播","放","状","\u{6001}",":"]

/*: "c2c_" :*/
fileprivate let noti_failPath:String = "blue carrier await partc2c_"

/*: "msg" :*/
fileprivate let noti_quitValue:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let notiShallAmUrl:String = "tasserts"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let data_ceilingValue:[UInt8] = [0x47,0x5d,0x63,0xe,0x51,0x4f,0x5c,0xe,0x63,0x61,0x53,0xe,0x62,0x56,0x53,0xe,0x64,0x57,0x52,0x53,0x5d,0xe,0x51,0x56,0x4f,0x62,0xe,0x54,0x53,0x4f,0x62,0x63,0x60,0x53,0xe,0x62,0x5d,0xe,0x55,0x57,0x64,0x53,0xe,0x13,0x2e,0xe,0x4f,0xe,0x51,0x4f,0x5a,0x5a]

fileprivate func imagePurchase(daily num: UInt8) -> UInt8 {
    let value = Int(num) - 238
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "cellData" :*/
fileprivate let const_genderPath:String = "cellDmini absolute"
fileprivate let constSpeedUrl:String = "adeadline"

/*: "F5F5F5" :*/
fileprivate let appDelayId:[Character] = ["F","5","F","5","F","5"]

/*: "System notification" :*/
fileprivate let constApplicationUrl:String = "Systechoose staff mention interaction"
fileprivate let noti_spineDilemmaTitle:String = "fiprevioustion"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BarViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum TailProvider {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class BarViewController: ScaleViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: UserChatType = .private
    private var conversationType: UserChatType = .private
    //: private var userWrap = BetweenInput()
    private var userWrap = BetweenInput() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = TailProvider.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool, _ errCode: Int) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: BetweenInput = BetweenInput()) {
    init(chatID: String, isFrom: TailProvider = .Normal, userWrap: BetweenInput = BetweenInput()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: self.msgView.targetID = chatID
        self.msgView.targetID = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap
        //: self.msgView.isRecharge = userWrap.isRecharge
        self.msgView.isRecharge = userWrap.isRecharge

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        BarViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        ThreadSpell.share.readReceiptDict[msgView.targetID] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if msgView.targetID == MacroDefine.getXiaoMiID() {
        if msgView.targetID == VersusQuit.noneConsume() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if msgView.targetID == MacroDefine.getCustomerServiceID() {
        } else if msgView.targetID == VersusQuit.beginSpend() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dataProjectionPerName.map{evolutionMulti(about: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
            if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = SignaturePriority(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        split()
        //: createUI()
        path()
        //: createUIConstraints()
        secureWith()
        //: bindInteraction()
        undertakeBy()
        //: addTapGestureRecognizer()
        banPick()
        //: func__requestChatInfo()
        near()
        //: func__getFillDraftText()
        duringExtend()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        UntilAction.shared.history()
        //: func__addVoiceNotification()
        all()
        //: addReadPeceiptNotification()
        highlight()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            great()
            //: func__TipsNotification()
            original()
            //: if QuantityeractionDelay.share.loginUserMode.sex == Gender.male.rawValue {
            if QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.male.rawValue {
                // 处理男性侧快捷回复逻辑
                //: func__setReplyTipsView()
                pathPrompt()
                // 判断问题回答消息遮罩是否展示
                //: refreshRechargeRequiredView()
                leaveHolder()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        regulateJudge(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        regulateJudge(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            pushOrange()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            CommandOrientation.shared.connectionMain()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: CountervalTableViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = CountervalTableViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ExecuteView.self, forCellReuseIdentifier: ExecuteView.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(FactoryEven.self, forCellReuseIdentifier: FactoryEven.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(SectionVia.self, forCellReuseIdentifier: SectionVia.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(UserSkin.self, forCellReuseIdentifier: UserSkin.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(OperationIdentity.self, forCellReuseIdentifier: OperationIdentity.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(IndexOvalEqually.self, forCellReuseIdentifier: IndexOvalEqually.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(BelowView.self, forCellReuseIdentifier: BelowView.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(TimingBlack.self, forCellReuseIdentifier: TimingBlack.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(DarkZone.self, forCellReuseIdentifier: DarkZone.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: CeilingThan.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(WouldOn.self, forCellReuseIdentifier: CeilingThan.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: GroupStatus = {
        //: let m = TalkingCallMenuManager()
        let m = GroupStatus()
        //: m.uid = msgView.targetID
        m.uid = msgView.targetID
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.operation()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: UniqueDisabledView = {
        //: var type: InputViewType = .PrivateChat
        var type: PropertyComprehensive = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = UniqueDisabledView(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(show_exploreAlwaysString.suffix(7))))!, .font: UIFont.alongsideOk(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(kAlongTitle.prefix(7)) + user_successfullyValue.replacingOccurrences(of: "line", with: "k")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(unique), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: CellView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = CellView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: ClusterView = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = ClusterView(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.alongsideOk(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(app_skipId) + show_upTitle.replacingOccurrences(of: "option", with: "FF"))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(showSunEqualPath) + String(noti_segmentCityValue)).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.multiSince().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(first), for: .touchUpInside)
        //: btn.isHidden = !QuantityeractionDelay.share.appUserConfigMode.showDirectGreet || QuantityeractionDelay.share.loginUserMode.sex == Gender.male.rawValue || QuantityeractionDelay.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !QuantityeractionDelay.share.appUserConfigMode.showDirectGreet || QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.male.rawValue || QuantityeractionDelay.share.appStatus == TotalervalObserve.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: DimensionMenu? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = DimensionMenu()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.alongsideOk(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(app_skipId) + show_upTitle.replacingOccurrences(of: "option", with: "FF"))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(notiFutureMessage) + showSupportTarCityAnonymousValue.replacingOccurrences(of: "click", with: "ot")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.multiSince().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(situation), for: .touchUpInside)
        //: btn.isHidden = QuantityeractionDelay.share.loginUserMode.sex == Gender.male.rawValue || QuantityeractionDelay.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.male.rawValue || QuantityeractionDelay.share.appStatus == TotalervalObserve.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: CurveView = {
        //: let v = TalkingLiveRoomGamesView()
        let v = CurveView()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: SampleView = {
        //: let view = TalkingPrivateBannerView.init()
        let view = SampleView()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: RegulatePlainView = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = RegulatePlainView()
        //: v.isHidden = (QuantityeractionDelay.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (QuantityeractionDelay.share.appStatus == TotalervalObserve.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: TargetView = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = TargetView()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.devicePublish()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var rechargeView: RechargeRequiredView = {
    private lazy var rechargeView: KeyScenario = { // 充值限制视图
        //: let v = RechargeRequiredView()
        let v = KeyScenario()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if self.userWrap.isRecharge == -1 {
            if self.userWrap.isRecharge == -1 { // 首充优惠
                //: TellEnd.share.func_pushToDiscountRechargeView()
                TellEnd.share.thresholdFromCondition()
                //: } else if self.userWrap.isRecharge == 0 {
            } else if self.userWrap.isRecharge == 0 { // 半屏充值
                //: TellEnd.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                TellEnd.share.tarUniversal(webViewType: .RechargeHalfPage)
            }
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var quickReplayView: QuestionQuickReplayView = {
    private lazy var quickReplayView: MagnitudeensityView = { // 问题快捷回复视图
        //: let v = QuestionQuickReplayView()
        let v = MagnitudeensityView()
        //: v.tapBlock = { [weak self] model in
        v.tapBlock = { [weak self] model in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 调用发送消息接口
            //: self.func_sendQuestionQuickReplayMsg(model)
            self.kind(model)
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension BarViewController: ReceiverFit {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func accountOn(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.trailing.equalTo(-4)
                    make.trailing.equalTo(-4)
                    //: make.height.equalTo(36)
                    make.height.equalTo(36)
                    //: make.width.equalTo(89)
                    make.width.equalTo(89)
                }
                //: } else {
            } else {
                //: self.bannerView.snp.remakeConstraints { make in
                self.bannerView.snp.remakeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: if self.replyTipsView != nil {
                    if self.replyTipsView != nil {
                        //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                        //: } else {
                    } else {
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                    }
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.fullGift(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension BarViewController: MasterAmongThreshold {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func first() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
        ThreadSpell.agreeOpen(toUserId: self.msgView.targetID) { errorCode, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.paraDown(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.produce(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func situation() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.msgView.targetID) { isPushSet, dict in
        ThreadSpell.directly(toUserId: self.msgView.targetID) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().viaCluster(urlStr: dict?[(const_openlyStr.replacingOccurrences(of: "altogether", with: "l"))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(userFreshStr.lowercased() + "ckGift")] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ComposeModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: user_protectionName.map{analysisLoanExist(broadcast: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(k_deadlineMsg.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.critical(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> FlexBridge! {
    func takeStorage(_ controller: CountervalTableViewController!, line data: V2TIMMessage!) -> FlexBridge! {
        //: let dic = CharacteristicDetail.parseTXMessageData(data: data.customElem.data)
        let dic = CharacteristicDetail.nearPrevious(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = ChainSample.oneSpot(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: ConvertSubtle.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(mainTechniquePopTitle))].stringValue == (String(noti_propertyUrl) + mainFoundFormat.replacingOccurrences(of: "topic", with: "m")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(noti_addressReachFormat))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(mainRoundTensionValue))][(String(noti_aStyleStr.prefix(7)))][(String(kEnjoyUrl.prefix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = OwnerChart(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = OwnerChart(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: DensityStartSignificant.self) == true ||
        if cellData?.isKind(of: DensityStartSignificant.self) == true ||
            //: cellData?.isKind(of: AppearanceAddLock.self) == true ||
            cellData?.isKind(of: AppearanceAddLock.self) == true ||
            //: cellData?.isKind(of: OwnerChart.self) == true ||
            cellData?.isKind(of: OwnerChart.self) == true ||
            //: cellData?.isKind(of: EditSelect.self) == true {
            cellData?.isKind(of: EditSelect.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            ThreadSpell.speedScan(cellData: cellData, msgView: self.msgView)
            // 男性是否展示快捷回复消息
            //: checkQuestionQuickReplayView()
            frameworkBy()
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: FlexBridge!) -> CeilingThan! {
    func accelerate(_ controller: CountervalTableViewController!, rebuild data: FlexBridge!) -> CeilingThan! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = attention(data: data)

        //: if data.isKind(of: OwnerChart.self) {
        if data.isKind(of: OwnerChart.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ExecuteView(style: .default, reuseIdentifier: ExecuteView.className())
            //: let cellData = data as! OwnerChart
            let cellData = data as! OwnerChart

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.corner() {
                //: if cellData.msgModel.quoteMsgInfo.uid == QuantityeractionDelay.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == QuantityeractionDelay.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = QuantityeractionDelay.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = QuantityeractionDelay.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: AppearanceAddLock.self) {
        if data.isKind(of: AppearanceAddLock.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = FactoryEven(style: .default, reuseIdentifier: FactoryEven.className())
            //: let cellData = data as! AppearanceAddLock
            let cellData = data as! AppearanceAddLock
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: EditSelect.self) {
        if data.isKind(of: EditSelect.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = SectionVia(style: .default, reuseIdentifier: SectionVia.className())
            //: let cellData = data as! EditSelect
            let cellData = data as! EditSelect
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: LayerIndex.self) {
        if data.isKind(of: LayerIndex.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = UserSkin(style: .default, reuseIdentifier: UserSkin.className())
            //: let cellData = data as! LayerIndex
            let cellData = data as! LayerIndex
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: ContextBind.self) {
        if data.isKind(of: ContextBind.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = OperationIdentity(style: .default, reuseIdentifier: OperationIdentity.className())
            //: let cellData = data as! ContextBind
            let cellData = data as! ContextBind
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = CombineFactor()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.ex(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(dataLapData.prefix(6)) + k_openlyFormat.replacingOccurrences(of: "satisfy", with: "t")))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: DensityStartSignificant.self) {
        if data.isKind(of: DensityStartSignificant.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = TimingBlack(style: .default, reuseIdentifier: TimingBlack.className())
            //: let cellData = data as! DensityStartSignificant
            let cellData = data as! DensityStartSignificant
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: ConvertSubtle.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = BelowView(style: .default, reuseIdentifier: BelowView.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! ConvertSubtle
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: DenseDisplay.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = IndexOvalEqually(style: .default, reuseIdentifier: IndexOvalEqually.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! DenseDisplay
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: ExerciseMinimumWant.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = DarkZone(style: .default, reuseIdentifier: DarkZone.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! ExerciseMinimumWant
            //: cell.fill(with: cellData)
            cell.panel(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: CeilingThan!) {
    func ownerReflect(_ controller: CountervalTableViewController!, spot cell: CeilingThan!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != MacroDefine.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != VersusQuit.beginSpend() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: TellEnd.share.func__pushToUserDetailVC(uid: String(uid))
            TellEnd.share.butterfly(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: CeilingThan!) {
    func easeSpeed(_ controller: CountervalTableViewController!, root cell: CeilingThan!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: FactoryEven.self) { // 图片
            //: let imageCellData = cell.messageData as! AppearanceAddLock
            let imageCellData = cell.messageData as! AppearanceAddLock
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.msgView.targetID)
                let vc = CombineAspect(imageCellData: imageCellData, toUid: self.msgView.targetID)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.input(imageCellData, roundingFor: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.mpLikeAfter(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainDiskName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.ignoreOk(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.invisible(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                anyVisual()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(show_alongMessage)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = ProcessorMargin()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = SubsequentSkip()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [ProcessorMargin] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = ResolveNorm(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                anyVisual()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: SectionVia.self) { // 视频
            //: let videoCellData = cell.messageData as! EditSelect
            let videoCellData = cell.messageData as! EditSelect
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = ResultInstruction(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                anyVisual()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.msgView.targetID)
                let vc = MaintainArchitecture(videoCellData: videoCellData, toUid: self.msgView.targetID)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.obtain(videoCellData, evaluation: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.mpLikeAfter(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: mainDiskName)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.ignoreOk(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.invisible(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                anyVisual()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: TimingBlack.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! TimingBlack
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! DensityStartSignificant, cellView: audioCell)
            self.extra(cellData: audioCell.cellData as! DensityStartSignificant, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ExecuteView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ExecuteView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = CharacteristicDetail.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = CharacteristicDetail.nearPrevious(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(mainRoundTensionValue))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(k_betterOperationName.prefix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(k_betterOperationName.prefix(7)))] as? String ?? ""

                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<BehaviorLargeThan>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TellEnd.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TellEnd.share.wrapFast(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(mainTechniquePopTitle))] as? String == (String(noti_propertyUrl) + mainFoundFormat.replacingOccurrences(of: "topic", with: "m")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(noti_aStyleStr.prefix(7)))][(String(user_targetOvalMessage.prefix(7)))].intValue == 1 {
                    //: checkAndCallVoice()
                    designClose()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    dark()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: IndexOvalEqually.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! IndexOvalEqually
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = CharacteristicDetail.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = CharacteristicDetail.nearPrevious(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(mainRoundTensionValue))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(k_betterOperationName.prefix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<BehaviorLargeThan>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: TellEnd.share.func__actionPushForMessage(jumpModel: jumpModel)
                    TellEnd.share.wrapFast(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: BelowView.self) {
            //: TellEnd.share.func__pushToUserDetailVC(uid: msgView.targetID)
            TellEnd.share.butterfly(uid: msgView.targetID)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: CeilingThan!) {
    func selectUsed(_ controller: CountervalTableViewController!, time cell: CeilingThan!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: LocalStrength?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: FactoryEven.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! FactoryEven
            //: cellData = imageCell.cellData as! AppearanceAddLock
            cellData = imageCell.cellData as! AppearanceAddLock
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: SectionVia.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! SectionVia
            //: cellData = videoCell.cellData as! EditSelect
            cellData = videoCell.cellData as! EditSelect
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: TimingBlack.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! TimingBlack
            //: cellData = audioCell.cellData as! DensityStartSignificant
            cellData = audioCell.cellData as! DensityStartSignificant
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ExecuteView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ExecuteView
            //: cellData = textCell.cellData as! OwnerChart
            cellData = textCell.cellData as! OwnerChart
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: FlexBridge? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: FlexBridge? = ChainSample.dismiss(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: EditSelect.self) {
                if reCellData!.isKind(of: EditSelect.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    tier(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    editQuote(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: FlexBridge!) {
    func loop(_ controller: CountervalTableViewController!, saveView cellData: FlexBridge!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = ThreadSpell.socialMic(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: FlexBridge!) {
    func capacity(_ controller: CountervalTableViewController!, asConnect cellData: FlexBridge!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        ThreadSpell.alongFlex(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: FlexBridge) -> FlexBridge {
    private func attention(data: FlexBridge) -> FlexBridge {
        //: let isSelf = data.msgModel.user.uid == Int(QuantityeractionDelay.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(QuantityeractionDelay.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if QuantityeractionDelay.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if QuantityeractionDelay.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = QuantityeractionDelay.share.loginUserMode.headPic!
                data.msgModel.user.headPic = QuantityeractionDelay.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = QuantityeractionDelay.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = QuantityeractionDelay.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 发送问题回复消息 -- 回答
    /// - Parameter model: 回答问题模型
    //: func func_sendQuestionQuickReplayMsg(_ model: QuestionQuickReplayModel) {
    func kind(_ model: TargetBlackModel) {
        //: let msgInfo = ["content": model.content, "msgType": "txt"]
        let msgInfo = [String(bytes: userMovieTitle.reversed(), encoding: .utf8)!: model.content, String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(noti_childUrl))]
        //: let paramsDic = ["message": model.content, "replayId": model.replayId, "type": "TxtMsg"] as [String : Any]
        let paramsDic = [String(bytes: const_givingKey.map{bagSpeaker(popular: $0)}, encoding: .utf8)!: model.content, String(bytes: appMissingTitle.map{$0^118}, encoding: .utf8)!: model.replayId, (String(mainTalkStr)): (String(mainDetectUrl))] as [String: Any]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.editQuote(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    //: func func_sendSayHiMsg() {
    func fill() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(noti_childUrl))]
        //: let paramsDic = ["type": "MultiQuickMsg"]
        let paramsDic = [(String(mainTalkStr)): (String(k_interactionFormat))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.editQuote(msgData: cellData, reSend: false)
        }
    }

    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func scene() {
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(noti_childUrl))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(String(mainTalkStr)): (String(data_rankTitle) + String(k_clusterHealthyMsg.prefix(5)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.editQuote(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func produce(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: userMovieTitle.reversed(), encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: userMovieTitle.reversed(), encoding: .utf8)!: content, (String(notiYearMessage.suffix(5))): greetId, String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(noti_childUrl))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: const_givingKey.map{bagSpeaker(popular: $0)}, encoding: .utf8)!: content, (String(notiYearMessage.suffix(5))): greetId, (String(mainTalkStr)): (String(mainDetectUrl))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.editQuote(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            LimitStrength.shared.byFinish(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.paraDown(showMsg: String(bytes: noti_createEvenMsg.map{unknownValueDisk(plain: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                        ThreadSpell.countTarget(toUserId: self.msgView.targetID, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(notiInvitationTitle.lowercased() + show_phaseFloorKey.lowercased())]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(app_ownVerticalId)),
                               //: "audioId": greetId,
                               (String(noti_customerFormat.prefix(7))): greetId,
                               //: "audioLength": length,
                               String(bytes: userDecreaseFormat.map{ifFeatureReach(pic: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(show_opName) + String(userLaterMessage)): model.unZipFilePath,
                               //: "senderId": QuantityeractionDelay.share.loginUserMode.userID]
                               String(bytes: noti_arcTitle.reversed(), encoding: .utf8)!: QuantityeractionDelay.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(noti_customerFormat.prefix(7))): greetId,
                                 //: "audioLength": length,
                                 String(bytes: userDecreaseFormat.map{ifFeatureReach(pic: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(show_opName) + String(userLaterMessage)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (String(mainTalkStr)): (String(notiEditContextUrl.prefix(4)) + "oMsg")]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.editQuote(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func everyDelicate(msgStr: String, quoteModel: VisitModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            paraDown(showMsg: (String(showMonthData) + String(k_nowMethodPath)).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: userMovieTitle.reversed(), encoding: .utf8)!: temStr, String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(noti_childUrl))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: const_givingKey.map{bagSpeaker(popular: $0)}, encoding: .utf8)!: temStr, (String(mainTalkStr)): (String(mainDetectUrl))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(noti_aStyleStr.prefix(7))): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(k_extraValue.replacingOccurrences(of: "provider", with: "e") + String(k_blackUrl))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(appMomentumVisibleValue))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? VisitModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            editQuote(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func critical(img: UIImage, sendModel: ComposeModel) {
        //: let imagePath = AppearanceAddLock.getChatImagePath(image: img)
        let imagePath = AppearanceAddLock.offly(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: main_cropBrownValue.map{atAbout(restore: $0)}, encoding: .utf8)!: (String(noti_trustName.prefix(6))), String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (kCityName.replacingOccurrences(of: "reading", with: "im")), String(bytes: constTermsOldFormat.map{layRounding(intensity: $0)}, encoding: .utf8)!: imagePath, String(bytes: kIndeedTitle.map{zzWhenMaterial(secular: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(app_subjectReadyData.replacingOccurrences(of: "exclusive", with: "c")): imagePath,
                                        //: "type": "ImgMsg",
                                        (String(mainTalkStr)): (String(noti_trustName.prefix(6))),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: user_protectionName.map{analysisLoanExist(broadcast: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_deadlineMsg.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            editQuote(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: FlexBridge?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func tier(msgData: FlexBridge?, sendModel: ComposeModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: main_cropBrownValue.map{atAbout(restore: $0)}, encoding: .utf8)!: (String(mainValueWithinFormat)), String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (user_sequenceValue.lowercased()), String(bytes: kIndeedTitle.map{zzWhenMaterial(secular: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(String(mainTalkStr)): (String(mainValueWithinFormat)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: appTemporaryKey.reversed(), encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: kCurUrl.map{pmStructure(way: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: appVariableId.map{collectPremium(to: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: user_protectionName.map{analysisLoanExist(broadcast: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(k_deadlineMsg.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = ChainSample.quantity(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.identity(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.versus(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            //: self.msgView.changeCustomMsg(cellData, status: .Msg_Status_Sending_2)
            self.msgView.spell(cellData, analyze: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.versus(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.resource(dict, movement: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        editQuote(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func topAndDesign(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(app_ownVerticalId)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: userDecreaseFormat.map{ifFeatureReach(pic: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(show_opName) + String(userLaterMessage)): voicePath,
                       //: "senderId": QuantityeractionDelay.share.loginUserMode.userID]
                       String(bytes: noti_arcTitle.reversed(), encoding: .utf8)!: QuantityeractionDelay.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(show_opName) + String(userLaterMessage)): voicePath,
                         //: "type": "AudioMsg",
                         (String(mainTalkStr)): (String(notiEditContextUrl.prefix(4)) + "oMsg"),
                         //: "len": "\(voiceTime)"]
                         String(bytes: kBlendStr.reversed(), encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            editQuote(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: FlexBridge, reSend: Bool) {
    private func editQuote(msgData: FlexBridge, reSend: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            paraDown(showMsg: showCountensityPath)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.msgView.targetID
        params[(String(kEnjoyUrl.prefix(5)))] = self.msgView.targetID

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: EditSelect.self) == false && msgData.isKind(of: ContextBind.self) == false && params["type"] as? String != "QuickMsg" && params["type"] as? String != "MultiQuickMsg" {
        if msgData.isKind(of: EditSelect.self) == false, msgData.isKind(of: ContextBind.self) == false, params[(String(mainTalkStr))] as? String != (String(data_rankTitle) + String(k_clusterHealthyMsg.prefix(5))), params[(String(mainTalkStr))] as? String != (String(k_interactionFormat)) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.identity(msgData)
        }
//        let recordTime = CharacteristicDetail.getMessageInsertTime()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((app_subjectReadyData.replacingOccurrences(of: "exclusive", with: "c"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(app_subjectReadyData.replacingOccurrences(of: "exclusive", with: "c"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(app_subjectReadyData.replacingOccurrences(of: "exclusive", with: "c"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as? String
        let type = params[(String(mainTalkStr))] as? String
        //: if type == "AudioMsg" { // 语音
        if type == (String(notiEditContextUrl.prefix(4)) + "oMsg") { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(show_opName) + String(userLaterMessage))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(show_opName) + String(userLaterMessage))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(show_opName) + String(userLaterMessage)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(k_curveMustMsg.prefix(6)) + "nts/" + String(userPersonalData.prefix(6)) + "oice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = userReflectDarkMessage + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(user_yearMsg) + String(user_reasonString))) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = app_rowBrushFormat + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(noti_customerFormat.prefix(7)))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(app_ownVerticalId))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        AgainstListen.videoMaster(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" || type == "MultiQuickMsg" {
                if type == (String(data_rankTitle) + String(k_clusterHealthyMsg.prefix(5))) || type == (String(k_interactionFormat)) {
                    //: self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(false, errorModel?.errorCode ?? 0)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.direct(msgData, independent: nil, action: NSNumber(value: 0), purple: .Msg_Status_Fail)
 
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.mpLikeAfter(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: appPublisherKey)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(notiYearMessage.suffix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.msgView.targetID, isHave: false)
                    ThreadSpell.countTarget(toUserId: self.msgView.targetID, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.morePhone(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(noti_aStyleStr.prefix(7)))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(k_currentModelPath.prefix(7)) + const_pressName.replacingOccurrences(of: "sovereign", with: "m"))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(k_currentModelPath.prefix(7)) + const_pressName.replacingOccurrences(of: "sovereign", with: "m"))] as! NSNumber
            }

            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.outName(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(appJoinSheKey.replacingOccurrences(of: "center", with: "e") + "geType")] as? Int
            //: if messageType == 25 || type == "QuickMsg" || type == "MultiQuickMsg" {
            if messageType == 25 || type == (String(data_rankTitle) + String(k_clusterHealthyMsg.prefix(5))) || type == (String(k_interactionFormat)) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!] = (String(data_againstString))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    k_playShadowValue.behindImplement(eventID: app_momentName)
                    //: } else if type == "QuickMsg" || type == "MultiQuickMsg" {
                } else if type == (String(data_rankTitle) + String(k_clusterHealthyMsg.prefix(5))) || type == (String(k_interactionFormat)) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!] = (String(noti_childUrl))
                    //: self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                    self.QuickEndBlock?(true, errorModel?.errorCode ?? 0)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.identity(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: if self.rechargeView.isHidden {
            if self.rechargeView.isHidden {
                //: if QuantityeractionDelay.share.loginUserMode.sex == Gender.male.rawValue &&
                if QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.male.rawValue,
                   //: type != "QuickMsg" &&
                   type != (String(data_rankTitle) + String(k_clusterHealthyMsg.prefix(5))),
                   //: type != "MultiQuickMsg" {
                   type != (String(k_interactionFormat))
                {
                    //: let key =  "\(QuantityeractionDelay.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
                    let key = "\(QuantityeractionDelay.share.loginUserMode.userID)" + (String(main_divideStripMsg) + String(data_actionBirthdayRemoveMessage) + String(main_toMessage.prefix(6))) + "\(String(self.msgView.targetID))"
                    //: UserDefaults.standard.set(true, forKey: key)
                    UserDefaults.standard.set(true, forKey: key)
                    //: self.removeReplyTipsView()
                    self.subtle()
                }
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.direct(updateMsgData, independent: msgInfo[(String(mainWayMomentString))] as? String, action: msgIncome, purple: .Msg_Status_Succ)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((String(user_depthName) + String(dataBlackFormat))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(String(user_depthName) + String(dataBlackFormat))] as! NSNumber
                //: QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((noti_itemKey.replacingOccurrences(of: "available", with: "a") + String(user_guideData))) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(noti_itemKey.replacingOccurrences(of: "available", with: "a") + String(user_guideData))] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.orientationOnto(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(const_flagPath) + String(data_tipValue))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(const_flagPath) + String(data_tipValue))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? PlateUniformModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.input(updateMsgData, roundingFor: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((user_sequenceValue.lowercased())) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(user_sequenceValue.lowercased())] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? AttributeModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.obtain(updateMsgData, evaluation: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        two()

        //: updateSession()
        pad()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func pad() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: msgView.targetID)
        FinishAssociated.beautyVariable(with: msgView.targetID)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func mpLikeAfter(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard QuantityeractionDelay.share.loginUserMode.status != 1 else {
            guard QuantityeractionDelay.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    paraDown(showMsg: errorStr)
                }
                //: return
                return
            }

            //: TellEnd.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            TellEnd.share.somePush(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.variable()
            //: QuantityeractionDelay.share.loginUserMode.freeMsgTimes = ""
            QuantityeractionDelay.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.operation()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = MarkExtra(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId))
            //: view.show()
            view.us()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                paraDown(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func invisible(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(noti_aStyleStr.prefix(7)))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.outName(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func creationRaw() {
        //: if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.content(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(QuantityeractionDelay.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            let key = "\(QuantityeractionDelay.share.loginUserMode.userID)_\(String(self.msgView.targetID))"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (app_pressureId.array(forKey: const_accountFormat)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.anDecision()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                app_pressureId.set(intimateCacheData, forKey: const_accountFormat)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func gallery(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = CharacteristicDetail.parseTXMessageData(data: data.customElem.data)
        let dic = CharacteristicDetail.nearPrevious(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(mainRoundTensionValue))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(noti_aStyleStr.prefix(7)))]?.dictionary
        //: if data.sender == self.msgView.targetID {
        if data.sender == self.msgView.targetID {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(appTimingDismissHideString.prefix(6)) + "ntimate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.permanentBeyond(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func outName(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(appTimingDismissHideString.prefix(6)) + "ntimate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(appTimingDismissHideString.prefix(6)) + "ntimate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.permanentBeyond(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func permanentBeyond(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.msgView.targetID)
        let model = FinishAssociated.resumeQuery(targetId: self.msgView.targetID)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.content(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.curveFactory(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.overIconBuilder(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.overIconBuilder(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        FinishAssociated.netGraphic(with: msgView.targetID, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.msgView.targetID ?? "",
        let userInfo = [String(bytes: dataReadMovieMsg.map{searchionRemote(dense: $0)}, encoding: .utf8)!: self.msgView.targetID ?? "",
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: user_norTitle.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: app_sizeFormat,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension BarViewController: RetainFormat {
    //: func func_inputContentIntervalOneSecond() {
    func drag() {}

    //: func func__selectMentionUser(range: NSRange) {}
    func level(range: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func primaryMatch(actionType: CriticalSend) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    validPlatform()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    beyondMaintain()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                validPlatform()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if QuantityeractionDelay.share.loginUserMode.loungePlus {
            if QuantityeractionDelay.share.loginUserMode.loungePlus { // 男性会员
                //: videoStr = self.userWrap.videoVIPPrice
                videoStr = self.userWrap.videoVIPPrice
                //: voiceStr = self.userWrap.voiceVIPPrice
                voiceStr = self.userWrap.voiceVIPPrice
                //: } else {
            } else {
                //: videoStr = self.userWrap.videoPrice
                videoStr = self.userWrap.videoPrice
                //: voiceStr = self.userWrap.voicePrice
                voiceStr = self.userWrap.voicePrice
            }
            //: if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr)
                videoCallManager.isMarkOf(videoPrice: videoStr, voicePrice: voiceStr)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                beyondMaintain()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    no()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    beyondMaintain()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                no()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.signature(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func beyondMaintain() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.paraDown(showMsg: String(bytes: k_editDialogData.map{undertakeFee(until: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func plate(giftModel: CombineFactor, giftNum: String, model: AngleAdd) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func written(giftModel: CombineFactor, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        ex(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: ContextBind?, fromStr: String?) {
    func ex(giftModel: CombineFactor, giftNum: String, cellData: ContextBind?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            paraDown(showMsg: showCountensityPath)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.msgView.targetID
        params[(String(kEnjoyUrl.prefix(5)))] = self.msgView.targetID
        //: params["giftId"] = giftModel.gid
        params[(String(k_deadlineMsg.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(notiDrawerTailMessage.suffix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(show_creationValue) + String(noti_willPath.prefix(5)))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(String(kUsId))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        AgainstListen.loadInformation(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.mpLikeAfter(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_removeTitle)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.row(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.ignoreOk(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.pad()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(noti_aStyleStr.prefix(7)))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(appTimingDismissHideString.prefix(6)) + "ntimate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(appTimingDismissHideString.prefix(6)) + "ntimate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.permanentBeyond(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.insertSound(cellData)
                self.msgView.insertSound(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                k_playShadowValue.behindImplement(eventID: data_sinceRankTitle)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func tension(giftModel: CombineFactor, giftNum: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        k_playShadowValue.behindImplement(eventID: noti_errorFormat)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            paraDown(showMsg: showCountensityPath)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= QuantityeractionDelay.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            beyondMaintain()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(data_publishForeValue.replacingOccurrences(of: "random", with: "wa") + "Gift" + String(appElementUniversalFormat.prefix(2)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(String(mainTalkStr))] = (String(main_relatedMsg.suffix(5)) + String(constCloudFormat))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (String(data_againstString))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(noti_aStyleStr.prefix(7))): msgInfo, String(bytes: const_despiteValue.map{mapCertain(appropriate: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = ChainSample.quantity(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            editQuote(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func scenarioPublisher(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(notiGiftEnterData - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.system(true)
            self.msgView.system(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func feedback(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.system(true)
            self.msgView.system(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func progress(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.progress(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                quantityry(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func nameurePlace() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if UntilAction.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            UntilAction.shared.asSign()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func ignoreOk(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(noti_aStyleStr.prefix(7)))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!] = (String(appCharacterizeData))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(noti_aStyleStr.prefix(7)))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = ChainSample.quantity(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.identity(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(k_currentModelPath.prefix(7)) + const_pressName.replacingOccurrences(of: "sovereign", with: "m"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(k_currentModelPath.prefix(7)) + const_pressName.replacingOccurrences(of: "sovereign", with: "m"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.direct(cellData, independent: msgInfoDic[(String(mainWayMomentString))] as? String, action: msgIncome, purple: .Msg_Status_Succ)

        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        awake(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((String(user_depthName) + String(dataBlackFormat))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(String(user_depthName) + String(dataBlackFormat))] as! NSNumber
            //: QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            QuantityeractionDelay.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: QuantityeractionDelay.share.loginUserMode.mf_coin)
            msgInputView.giftView.consumptionDown(needReload: false, mf_coin: QuantityeractionDelay.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func great() {
        //: giftEffectView.initCachAnimatData(tagetID: msgView.targetID)
        giftEffectView.editor(tagetID: msgView.targetID)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func awake(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.save(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func privacy(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.customFast(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func fraction() -> String {
        //: return msgView.targetID
        return msgView.targetID
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func designClose() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.openly { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.pad()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func dark() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.gesture { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.pad()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension BarViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func unique() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = ButtonInsert()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        anyVisual()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func directlyFor() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = StrengthView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.fieldThreeExpectedOperationDrawer(cellTitleList: [(String(kMightName.prefix(5)) + "info").localized, (String(kHiddenAttentionUrl)).localized, (String(showEntityKey)).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .thinOf()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, str in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: TellEnd.share.func__pushToUserDetailVC(uid: self.msgView.targetID)
                TellEnd.share.butterfly(uid: self.msgView.targetID)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                devicePublish()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                let reportView = ShadowBridge(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.msgView.targetID)
                //: reportView.showReportViewIn(view: nil)
                reportView.analyze(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func devicePublish() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = SustainablePadQueryer()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        view.limitedProcess(self.userWrap.content, isTop: self.userWrap.top, toUid: self.msgView.targetID)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.duringAdd(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func totalryPosition() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            quantityry(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            anyVisual()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func flush(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = AttachRapid.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.bean(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func highlight() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(asset(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: userEnabletoNearbySkirtKey,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(translationUp),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: app_qualityId,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func asset(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == msgView.targetID else { return }
        guard obj.userID == msgView.targetID else { return }
        //: guard QuantityeractionDelay.share.loginUserMode.loungePlus == true else { return }
        guard QuantityeractionDelay.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func translationUp() {
        //: QuantityeractionDelay.share.loginUserMode.loungePlus = true
        QuantityeractionDelay.share.loginUserMode.loungePlus = true
        //: userWrap.isRecharge = 1
        userWrap.isRecharge = 1
        //: msgView.isRecharge = userWrap.isRecharge
        msgView.isRecharge = userWrap.isRecharge
        //: refreshRechargeRequiredView()
        leaveHolder()
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func less() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? FlexBridge else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? FlexBridge else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if ThreadSpell.doingBackCalculate(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.click(tipStr: (String(const_forePath.suffix(5)) + String(userCurriculumIndexId.suffix(8)) + String(show_spellUrl.prefix(6))).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension BarViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func validPlatform() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        StrengthMomentum.verb(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = NameSave.evenColumn(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 assets: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 isSelectOriginalPhoto: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.length(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func mutual() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        StrengthMomentum.momentAccount(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.length(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func length(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue else {
            QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ComposeModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.critical(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = ResolveNorm(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ComposeModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.critical(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        anyVisual()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func no() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard ThreadSpell.arrayPrepare(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == CommonAmong.male.rawValue ? "He".localized : (String(constTaskMessage.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.paraDown(showMsg: (String(userDisappearExceptUrl.prefix(5)) + " using" + String(const_respectiveMsg.suffix(4)) + " low " + String(dataSubtleValue.suffix(4)) + userIncomePath.replacingOccurrences(of: "point", with: "rs")).underConnection(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.discount()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func discount() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = NameSave.evenColumn(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (coverImage: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            NameSave.appointed(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.pop(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.paraDown(showMsg: (String(showStrokeStrengthMessage) + String(appNearValue)).localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func pop(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: ResultSceneEvolution = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PopViewController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ComposeModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.tier(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        HandleEquipment.element()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        anyVisual()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: FlexBridge, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func versus(msgData: FlexBridge, sendModel: ComposeModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        BeyondState.shared.speaker(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.paraDown(showMsg: (String(kOurStr.suffix(5)) + "d faile" + kTitleWhichString.lowercased()).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.direct(msgData, independent: nil, action: NSNumber(value: 0), purple: .Msg_Status_Fail)

                
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.tier(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension BarViewController {
    //: func func__addVoiceNotification() {
    func all() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(scopeAnnouncement),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(task),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(success(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(light(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: main_textUrl,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(gallery(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: main_flexFormat,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func regulateJudge(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            UntilAction.shared.history()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            UntilAction.shared.bodyHi()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func scopeAnnouncement() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        regulateJudge(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func task() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        regulateJudge(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func success(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            regulateJudge(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            regulateJudge(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: DensityStartSignificant, cellView: TalkingChatAudioMsgCell) {
    func extra(cellData: DensityStartSignificant, cellView: TimingBlack) {
        //: guard TalkingSocketManager.shared.agileImageSkewy == false else {
        guard UpZone.shared.agileImageSkewy == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.paraDown(showMsg: data_colorCanCeilingMsg)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? FlexBridge)!.isKind(of: DensityStartSignificant.self) == false {
            if (indexModel as? FlexBridge)!.isKind(of: DensityStartSignificant.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! DensityStartSignificant
            let audioModel = indexModel as! DensityStartSignificant
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = FinishAssociated.image(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        UntilAction.shared.asSign()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if UntilAction.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(userChoiceData)) + "\(UntilAction.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if UntilAction.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                UntilAction.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        UntilAction.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        UntilAction.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: FlexBridge = self.msgView.uiMsgs.object(at: index) as! FlexBridge
                let subModel: FlexBridge = self.msgView.uiMsgs.object(at: index) as! FlexBridge
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: DensityStartSignificant.self) {
                if subModel.isKind(of: DensityStartSignificant.self) {
                    //: let audioModel = subModel as! DensityStartSignificant
                    let audioModel = subModel as! DensityStartSignificant
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = FinishAssociated.image(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    PanMarker.visit(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        UntilAction.shared.fillDisappear(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func near() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.bind()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.produce(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? FlexBridge
                let indexModel = model as? FlexBridge
                //: if indexModel?.isKind(of: DensityStartSignificant.self) == true || indexModel?.isKind(of: AppearanceAddLock.self) == true || indexModel?.isKind(of: OwnerChart.self) == true || indexModel?.isKind(of: EditSelect.self) == true {
                if indexModel?.isKind(of: DensityStartSignificant.self) == true || indexModel?.isKind(of: AppearanceAddLock.self) == true || indexModel?.isKind(of: OwnerChart.self) == true || indexModel?.isKind(of: EditSelect.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.two()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.line()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.less()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func bind() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.okUponLock(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.appropriate(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.creationRaw()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == QuantityeractionDelay.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == QuantityeractionDelay.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == QuantityeractionDelay.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == QuantityeractionDelay.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.duringAdd(userWrap)
    }

    //: func func__getFillDraftText() {
    func duringExtend() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(noti_failPath.suffix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                }
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }

    //: func func__saveFillDraftText() {
    func pushOrange() {
        //: let userID = "c2c_"+msgView.targetID
        let userID = (String(noti_failPath.suffix(4))) + msgView.targetID
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { code, desc in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension BarViewController {
    //: func func__TipsNotification() {
    func original() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(norm(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: userPhoneValue,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func norm(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: DenseDisplay = userinfo[(String(noti_quitValue))] as! DenseDisplay

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.system(true)
        self.msgView.system(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func click(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: userMovieTitle.reversed(), encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: appOthersUrl.map{remindObserve(even: $0)}, encoding: .utf8)!: (notiShallAmUrl.replacingOccurrences(of: "assert", with: "ip"))]
        //: var message: FlexBridge?
        var message: FlexBridge?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            message = ChainSample.underBoundAcross(withExtral: msgInfo, toUid: msgView.targetID, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = ChainSample.quantity(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.system(true)
        self.msgView.system(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func line() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        ThreadSpell.tingFlexibleIsolate { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.click(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func orientationOnto(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.click(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func two() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        ThreadSpell.hiddenBold(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.click(tipStr: String(bytes: data_ceilingValue.map{imagePurchase(daily: $0)}, encoding: .utf8)!.underConnection(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func light(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: FlexBridge = dict?["cellData"] as! FlexBridge
        let cellData: FlexBridge = dict?[(String(const_genderPath.prefix(5)) + constSpeedUrl.replacingOccurrences(of: "deadline", with: "ta"))] as! FlexBridge
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.system(true)
        self.msgView.system(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension BarViewController: MomentumText {
    //: func func__setReplyTipsView() {
    func pathPrompt() {
        //: let key = "\(QuantityeractionDelay.share.loginUserMode.userID)_PrivateChatTipsTitle_\(String(self.msgView.targetID))"
        let key = "\(QuantityeractionDelay.share.loginUserMode.userID)" + (String(main_divideStripMsg) + String(data_actionBirthdayRemoveMessage) + String(main_toMessage.prefix(6))) + "\(String(self.msgView.targetID))"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue {
            //: contentView.addSubview(replyTipsView!)
            contentView.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.finishHidden()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            accountOn(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            subtle()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func texturePush(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        everyDelicate(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        subtle()
    }

    //: func removeReplyTipsView() {
    func subtle() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            accountOn(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 问题回答消息

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 判断问题回答消息遮罩是否展示
    //: private func refreshRechargeRequiredView() {
    private func leaveHolder() {
        //: guard QuantityeractionDelay.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.male.rawValue else { return }
        //: if userWrap.isRecharge == 1 || userWrap.isOfficial {
        if userWrap.isRecharge == 1 || userWrap.isOfficial { // 已充值过
            //: quickReplayView.isHidden = true
            quickReplayView.isHidden = true
            //: rechargeView.isHidden = true
            rechargeView.isHidden = true

            //: } else {
        } else { // 未充值
            //: rechargeView.isHidden = false
            rechargeView.isHidden = false
            //: self.checkQuestionQuickReplayView()
            self.frameworkBy()
        }
    }

    /// 检查快捷回复视图是否展示
    //: private func checkQuestionQuickReplayView() {
    private func frameworkBy() {
        //: guard userWrap.isOfficial == false else { return }
        guard userWrap.isOfficial == false else { return }
        // 检查男性侧快捷回复是否展示（延时为了拿到uiMsgs数据）
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: TalkingPrivateChatManager.chat_onNewMessage_checkQuestionQuickReplayMsg(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
            ThreadSpell.box(isRecharge: self.userWrap.isRecharge == 1, msgView: self.msgView) { msgInfo in
                //: guard let msgInfo = msgInfo else { return }
                guard let msgInfo = msgInfo else { return }
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                    //: self.quickReplayView.isHidden = false
                    self.quickReplayView.isHidden = false
                    //: self.quickReplayView.refreshQuickReplayView(msgInfo: msgInfo)
                    self.quickReplayView.array(msgInfo: msgInfo)
                }
            }
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func split() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kObjectMessage, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: const_rebuildWithinId - notiGiftEnterData, width: dataTabUrl, height: const_rebuildWithinId)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: dataTabUrl,
                                     //: height: ScreenHeight)
                                     height: const_rebuildWithinId)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func quantityry(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.moreHide() {
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: MovementSkirt.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: const_rebuildWithinId - notiGiftEnterData,
                                     //: width: ScreenWidth,
                                     width: dataTabUrl,
                                     //: height: ScreenHeight)
                                     height: const_rebuildWithinId)
            //: } completion: { finish in
        } completion: { finish in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension BarViewController {
    /// 创建UI
    //: private func createUI() {
    private func path() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor.clear
            msgView.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgView.view.backgroundColor = UIColor.clear
            msgView.view.backgroundColor = UIColor.clear
            //: msgView.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgView.tableView.backgroundColor = UIColor(hex: (String(appDelayId)))
        }
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(rechargeView)
            view.addSubview(rechargeView)
            //: view.addSubview(quickReplayView)
            view.addSubview(quickReplayView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(constApplicationUrl.prefix(5)) + "m noti" + noti_spineDilemmaTitle.replacingOccurrences(of: "previous", with: "ca")).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func secureWith() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? notiGiftEnterData : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.asAffect(width: contentView.width,
                                 //: height: contentView.height,
                                 height: contentView.height,
                                 //: corners: [.topLeft, .topRight],
                                 corners: [.topLeft, .topRight],
                                 //: cornerRadii: .init(width: 8, height: 8))
                                 cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? main_measureString : app_whenAppearanceData)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: rechargeView.snp.makeConstraints { make in
            rechargeView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(rechargeView.ContentView_H)
                make.height.equalTo(rechargeView.ContentView_H)
            }
            //: quickReplayView.snp.makeConstraints { make in
            quickReplayView.snp.makeConstraints { make in
                //: make.leading.equalTo(24)
                make.leading.equalTo(24)
                //: make.width.equalTo(270)
                make.width.equalTo(270)
                //: make.bottom.equalTo(rechargeView.snp.top).offset(-13)
                make.bottom.equalTo(rechargeView.snp.top).offset(-13)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? main_measureString : app_whenAppearanceData)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + constCountervalMaterialMsg)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func undertakeBy() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.totalryPosition()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.directlyFor()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.primaryMatch(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                k_playShadowValue.behindImplement(eventID: notiCleanUrl, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
