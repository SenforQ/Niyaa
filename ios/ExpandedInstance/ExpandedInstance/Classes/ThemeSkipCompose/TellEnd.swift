
//: Declare String Begin

/*: "fee" :*/
fileprivate let mainOurString:[Character] = ["f","e","e"]

/*: "VIPFee" :*/
fileprivate let noti_whichWithinStr:[Character] = ["V","I","P","F","e"]
fileprivate let kMultiValue:String = "additional"

/*: "chattingNum" :*/
fileprivate let k_uponName:String = "CHATTI"
fileprivate let mainComponentPath:String = "challenge alongngNum"

/*: "You're already in her live room" :*/
fileprivate let user_coordinateEmptyPath:[UInt8] = [0x12,0x24,0x3e,0x6c,0x39,0x2e,0x6b,0x2a,0x27,0x39,0x2e,0x2a,0x2f,0x32,0x6b,0x22,0x25,0x6b,0x23,0x2e,0x39,0x6b,0x27,0x22,0x3d,0x2e,0x6b,0x39,0x24,0x24,0x26]

private func scaleAd(core num: UInt8) -> UInt8 {
    return num ^ 75
}

/*: "toUid" :*/
fileprivate let dataRoleName:[UInt8] = [0x64,0x69,0x55,0x6f,0x74]

/*: "momentId" :*/
fileprivate let show_socialTermsUrl:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "extra" :*/
fileprivate let noti_qualityMessage:[UInt8] = [0x8f,0x92,0x9e,0x98,0x8b]

private func withinTranslation(post num: UInt8) -> UInt8 {
    return num ^ 234
}

/*: "uid" :*/
fileprivate let const_coordinateTitle:[UInt8] = [0x59,0x45,0x48]

/*: "onlineStatus" :*/
fileprivate let const_clearlyPerformFormat:[UInt8] = [0x89,0x88,0x8a,0x8f,0x88,0x83,0xb5,0x92,0x87,0x92,0x93,0x95]

private func meAccelerate(lite num: UInt8) -> UInt8 {
    return num ^ 230
}

/*: "isNewUser" :*/
fileprivate let dataOvalPadMsg:[UInt8] = [0xcc,0xd6,0xeb,0xc0,0xd2,0xf0,0xd6,0xc0,0xd7]

private func mergePremium(there num: UInt8) -> UInt8 {
    return num ^ 165
}

/*: "userStatus" :*/
fileprivate let userEducatorScriptKey:[UInt8] = [0xef,0xe9,0xff,0xe8,0xc9,0xee,0xfb,0xee,0xef,0xe9]

/*: "Account is restricted！" :*/
fileprivate let notiHeartPath:String = "Accowoman bold location consume oval"
fileprivate let notiCropValue:String = "task restriction back blanks re"
fileprivate let mainUniformId:String = "striabs"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let showEquivalentKey:[UInt8] = [0x72,0x65,0x74,0x61,0x6c,0x20,0x6e,0x69,0x61,0x67,0x61,0x20,0x79,0x72,0x74,0x20,0x65,0x73,0x61,0x65,0x6c,0x50,0x20,0x2e,0x6d,0x6f,0x6f,0x72,0x20,0x74,0x61,0x68,0x63,0x20,0x65,0x68,0x74,0x20,0x72,0x65,0x74,0x6e,0x65,0x20,0x6f,0x74,0x20,0x64,0x65,0x6c,0x69,0x61,0x46]

/*: "url" :*/
fileprivate let mainPendingMechanismCompletePath:[Character] = ["u","r","l"]

/*: "userDetail" :*/
fileprivate let appThumbLikeValue:String = "useview"

/*: "&type=6" :*/
fileprivate let user_uniformString:String = "color department integrity select drown&type=6"

/*: "&type=7" :*/
fileprivate let kThresholdQuitNeverKey:String = "&type=7what operate length please eh"

/*: "amount" :*/
fileprivate let app_aliveName:[UInt8] = [0x30,0x3c,0x3e,0x24,0x3f,0x25]

/*: "https://" :*/
fileprivate let mainOnString:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "http://" :*/
fileprivate let showWantKey:[Character] = ["h","t","t","p",":","/","/"]

/*: "transparency" :*/
fileprivate let userReasonMessage:[Character] = ["t","r","a","n","s","p","a","r","e","n","c"]
fileprivate let app_becomeSoundId:String = "associate"

/*: "version= :*/
fileprivate let showReceiverMsg:String = "indicatorersi"
fileprivate let show_smartString:[Character] = ["o","n","="]

/*: &packageId= :*/
fileprivate let noti_backWhiteAbsoluteTitle:String = "fragment up stack&packag"
fileprivate let mainPressAccessId:String = "department capacity telescope hiddeneId="

/*: &bundleId= :*/
fileprivate let main_nextTingMsg:String = "&bundconnect background draft infect eff"
fileprivate let showJumpName:String = "="

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TellEnd.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/31.
//

//: import UIKit
import UIKit

//: public class TellEnd: NSObject {
public class TellEnd: NSObject {
    //: @objc static public let share = TellEnd()
    @objc public static let share = TellEnd()
    //: private override init() {}
    override private init() {}
}

//: extension TellEnd {
extension TellEnd {
    // MARK: - 随机视频入口

    //: func func__pushToRandomVideoVC(isBeginRand: Bool) {
    func directionDownBasic(isBeginRand: Bool) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickInRandomvideo)
        k_playShadowValue.behindImplement(eventID: kConstructGateFormat)
        //: guard TalkingSocketManager.shared.agileImageSkewy == false else {
        guard UpZone.shared.agileImageSkewy == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.paraDown(showMsg: data_colorCanCeilingMsg)
            //: return
            return
        }
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        StrengthMomentum.pauseAbout { isOpen in
            //: guard isOpen == true else { return }
            guard isOpen == true else { return }
            //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
            ChartMoveBehavior.norNumberegration { succeed, result, errorModel in
                //: guard succeed else { return }
                guard succeed else { return }
                //: let json = JSON(result ?? [String: Any]())
                let json = JSON(result ?? [String: Any]())
                //: let vc = TalkingRandomVideoViewController()
                let vc = JobViewController()
                //: vc.fee = json["fee"].intValue
                vc.fee = json[(String(mainOurString))].intValue
                //: vc.vipFee = json["VIPFee"].intValue
                vc.vipFee = json[(String(noti_whichWithinStr) + kMultiValue.replacingOccurrences(of: "additional", with: "e"))].intValue
                //: vc.chattingNum = json["chattingNum"].intValue
                vc.chattingNum = json[(k_uponName.lowercased() + String(mainComponentPath.suffix(5)))].intValue
                //: vc.isBeginRand = isBeginRand
                vc.isBeginRand = isBeginRand
                //: vc.isFirstRandow = isBeginRand
                vc.isFirstRandow = isBeginRand
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.moreHide()?.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }

    // MARK: - 跳转个人资料

    //: func func__pushToUserDetailVC(uid: String?) {
    func butterfly(uid: String?) {
        //: guard let currentVC = self.currentViewController() else { return }
        guard let currentVC = self.moreHide() else { return }
        //: if let profileVC = currentVC as? TalkingUserDetailViewController {
        if let profileVC = currentVC as? ProcessViewController {
            // 同一用户的资料页已经在顶部，只刷新
            //: if profileVC.uid == uid {
            if profileVC.uid == uid {
                //: profileVC.requestAllData()
                profileVC.beforeRun()
                //: return
                return
            }
        }

        //: let vc = TalkingUserDetailViewController(tempUid: uid)
        let vc = ProcessViewController(tempUid: uid)
        //: getNavigationController()?.pushViewController(vc, animated: true)
        anyVisual()?.pushViewController(vc, animated: true)
    }

    // MARK: - 观众侧进入直播间

    /// 观众侧跳转到直播间
    /// - Parameters:
    ///   - uid: 主播id
    ///   - enterType: 入口
    //: func func_audiencePushToLiveRoomVC(uid: String, enterType: LiveEnterType) {
    func activityAcrossInclude(uid: String, enterType: DirectChain) {
        // 同一直播间只能进一次
        //: guard String(TalkingLiveManager.shared().liveRoomModel.streamerInfo.uid) != uid else {
        guard String(RedWith.atPerform().liveRoomModel.streamerInfo.uid) != uid else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You're already in her live room".localized)
            self.paraDown(showMsg: String(bytes: user_coordinateEmptyPath.map{scaleAd(core: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        // 校验接口，通过后进入直播间
        //: TalkingAudienceManager().req_enterLiveRoom(streamerUid: uid) { succeed, result, errorModel in
        SkipOriginal().project(streamerUid: uid) { succeed, result, errorModel in
            //: guard succeed else { return }
            guard succeed else { return }
            //: guard let livemodel = TalkingLiveRoomModel.deserialize(from: result as? Dictionary) else { return }
            guard let livemodel = FloorMaterial.deserialize(from: result as? Dictionary) else { return }
            // 退出之前的直播间
            //: let currentVC = self.func__getCurrentActivityVC()
            let currentVC = self.tillFill()
            //: if let vcArr = currentVC?.navigationController?.viewControllers {
            if let vcArr = currentVC?.navigationController?.viewControllers {
                //: for index in 0..<(vcArr.count) {
                for index in 0 ..< (vcArr.count) {
                    //: let vc = vcArr[index]
                    let vc = vcArr[index]
                    //: if vc.isKind(of: TalkingLivePullStreamsViewController.self) == true {
                    if vc.isKind(of: DuringInformation.self) == true {
                        //: (vc as! TalkingLivePullStreamsViewController).popCurrentViewController()
                        (vc as! DuringInformation).afterEnable()
                    }
                }
            }

            // 进入新直播间
            //: TalkingLiveManager.shared().liveRoomModel = livemodel
            RedWith.atPerform().liveRoomModel = livemodel
            //: let liveVc = TalkingLivePullStreamsViewController()
            let liveVc = DuringInformation()
            //: liveVc.type = enterType
            liveVc.type = enterType
            //: currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            currentVC?.navigationController?.pushViewController(liveVc, animated: true)
            // 埋点
            //: switch enterType {
            switch enterType {
            //: case .match:
            case .match:
                //: uploadRecord.uploadRecordEvent(eventID: ClickMatchLiveWindowNoP, toUid: uid)
                k_playShadowValue.behindImplement(eventID: dataCustomInvisibleMessage, toUid: uid)
            //: case .userDetail:
            case .userDetail:
                //: uploadRecord.uploadRecordEvent(eventID: ClickProfileLiveWindowNoP, toUid: uid)
                k_playShadowValue.behindImplement(eventID: dataGateBoxId, toUid: uid)
            //: case .unknown: break
            case .unknown: break
            }
        }
    }

    // MARK: - 跳转聊天

    /// 跳转到消息列表页
    /// - Parameter isHalf: 是否半屏展示
    //: func func__pushToChatListVC(isHalfView: Bool = false) {
    func doingicialProperty(isHalfView: Bool = false) {
        //: let vc = TalkingChatListViewController(isHalfView: isHalfView)
        let vc = MovementSkirt(isHalfView: isHalfView)
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = tillFill()
        //: if isHalfView == false {
        if isHalfView == false {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
            //: } else {
        } else {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
        }
    }

    /// 跳转到消息聊天页
    /// - Parameters:
    ///   - chatID: 对方Id
    ///   - isFrom: 私聊页入口
    /// - Returns: 消息聊天页

    //: func func__pushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, completion: ((_ vc: TalkingPrivateChatController) -> Void)? = nil ) {
    func broadcastResign(chatID: String, isFrom: TailProvider = .Normal, completion: ((_ vc: BarViewController) -> Void)? = nil) {
        //: guard chatID.count > 0 else { return }
        guard chatID.count > 0 else { return }

        //: if chatID != MacroDefine.getXiaoMiID() {
        if chatID != VersusQuit.noneConsume() { // 系统消息
            //: ProgressHUD.show()
            HandleEquipment.faceSpot()
            //: let param = ["toUid": chatID, "momentId": "0", "extra": "1"]
            let param = [String(bytes: dataRoleName.reversed(), encoding: .utf8)!: chatID, String(bytes: show_socialTermsUrl.reversed(), encoding: .utf8)!: "0", String(bytes: noti_qualityMessage.map{withinTranslation(post: $0)}, encoding: .utf8)!: "1"]
            //: TalkingChatRequestTool.req_refreshUserChatInfo(param: param) { [weak self] succeed, result, errorModel in
            AgainstListen.presentation(param: param) { [weak self] succeed, result, errorModel in
                //: ProgressHUD.dismiss()
                HandleEquipment.element()
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: guard succeed else { return }
                guard succeed else { return }

                //: let userWrap = BetweenInput.func__transformedChatinfo(userDic: result as! Dictionary<String, Any>)
                let userWrap = BetweenInput.character(userDic: result as! [String: Any])
                //: DBUserInfoManager.cache_updateMsgUserInfoAndPostNotif(with: userWrap)
                FinishAssociated.radioImmediate(with: userWrap)
                // 更新消息列表用户在线状态
                //: let userInfo = ["uid": chatID,
                let userInfo = [String(bytes: const_coordinateTitle.map{$0^44}, encoding: .utf8)!: chatID,
                                //: "onlineStatus": userWrap.onlineStatus,
                                String(bytes: const_clearlyPerformFormat.map{meAccelerate(lite: $0)}, encoding: .utf8)!: userWrap.onlineStatus,
                                //: "isNewUser":userWrap.isNewUser,
                                String(bytes: dataOvalPadMsg.map{mergePremium(there: $0)}, encoding: .utf8)!: userWrap.isNewUser,
                                //: "userStatus": userWrap.userStatus]
                                String(bytes: userEducatorScriptKey.map{$0^154}, encoding: .utf8)!: userWrap.userStatus]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: constSampleTakeMsg,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
                //: if userWrap.userStatus == 3 {
                if userWrap.userStatus == 3 { /// 被封号
                    //: self.func__showStatusBarErrorMsg(showMsg: "Account is restricted！".localized)
                    self.paraDown(showMsg: (String(notiHeartPath.prefix(4)) + "unt i" + String(notiCropValue.suffix(4)) + mainUniformId.replacingOccurrences(of: "abs", with: "c") + "ted！").localized)
                    //: return
                    return
                }
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    let chatVC = self.pastGo(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
                    //: completion?(chatVC)
                    completion?(chatVC)
                }
            }
            //: }else{
        } else {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let chatVC = self.func__canPushToPriveteChatVC(chatID: chatID, isFrom: isFrom, userWrap: BetweenInput())
                let chatVC = self.pastGo(chatID: chatID, isFrom: isFrom, userWrap: BetweenInput())
                //: completion?(chatVC)
                completion?(chatVC)
            }
        }
    }

    //: func func__canPushToPriveteChatVC(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: BetweenInput ) -> TalkingPrivateChatController {
    func pastGo(chatID: String, isFrom: TailProvider = .Normal, userWrap: BetweenInput) -> BarViewController {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = tillFill()
        //: if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
        if isFrom != .LiveRoom, let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index] .isKind(of: TalkingPrivateChatController.self) == true {
                if vcArr[index].isKind(of: BarViewController.self) == true {
                    //: let vc = vcArr[index] as! TalkingPrivateChatController
                    let vc = vcArr[index] as! BarViewController
                    //: if vc.msgView.targetID == chatID {
                    if vc.msgView.targetID == chatID {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return vc
                        return vc
                    }
                }
            }
        }

        //: let vc = TalkingPrivateChatController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        let vc = BarViewController(chatID: chatID, isFrom: isFrom, userWrap: userWrap)
        //: if isFrom == .LiveRoom {
        if isFrom == .LiveRoom {
            //: currentVC?.addChild(vc)
            currentVC?.addChild(vc)
            //: currentVC?.view.addSubview(vc.view)
            currentVC?.view.addSubview(vc.view)
            //: } else {
        } else {
            //: currentVC?.navigationController?.pushViewController(vc, animated: true)
            currentVC?.navigationController?.pushViewController(vc, animated: true)
        }
        //: return vc
        return vc
    }

    // MARK: - 跳转群聊

    //: func func__pushToGroupChat(groupId: String) {
    func generateerSingle(groupId: String) {
        //: guard groupId.count > 0 else { return }
        guard groupId.count > 0 else { return }
        //: ProgressHUD.show()
        HandleEquipment.faceSpot()
        //: TalkingChatRequestTool.req_checkChatRoom(roomId: groupId) { succeed, result, errorModel in
        AgainstListen.tagPlain(roomId: groupId) { succeed, result, errorModel in
            //: ProgressHUD.dismiss()
            HandleEquipment.element()
            //: guard succeed == true else {
            guard succeed == true else {
                //: if errorModel?.errorCode == 106 {
                if errorModel?.errorCode == 106 {
                    //: } else {
                } else {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                    self.paraDown(showMsg: errorModel?.errorMsg ?? "")
                    //: if errorModel?.errorCode == 103 {
                    if errorModel?.errorCode == 103 { // 不在家族里
                    }
                }

                //: return
                return
            }

            //: guard let data = result as? [String: Any] else {
            guard let data = result as? [String: Any] else {
                //: return
                return
            }

            //: if MacroDefine.isGroupChat(groupId) {
            if VersusQuit.tillComplete(groupId) {
                //: } else {
            } else { // 公共聊天室
                //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
                    //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                    guard self.may(roomId: groupId) == false else { return }
                    //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                    let vc = ReceiverBecome(groupType: GroupType_Meeting, infoData: data)
                    //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                    self.tillFill()?.navigationController?.pushViewController(vc, animated: true)

                    //: } fail: { code, desc in
                } fail: { code, desc in
                    //: if code == 10013 {
                    if code == 10013 { // 程序异常退出，未退出聊天室
                        //: guard self.func__checkIsGroupChatVC(roomId: groupId) == false else { return }
                        guard self.may(roomId: groupId) == false else { return }
                        //: let vc = TalkingGroupChatController(groupType: GroupType_Meeting, infoData: data)
                        let vc = ReceiverBecome(groupType: GroupType_Meeting, infoData: data)
                        //: self.func__getCurrentActivityVC()?.navigationController?.pushViewController(vc, animated: true)
                        self.tillFill()?.navigationController?.pushViewController(vc, animated: true)
                        //: } else {
                    } else {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                        self.paraDown(showMsg: String(bytes: showEquivalentKey.reversed(), encoding: .utf8)!.localized)
                    }
                }
            }
        }
    }

    /// 检测堆栈中是否有群聊控制器，有则跳转到群聊页
    /// - Parameter roomId: 房间Id
    /// - Returns: 检测结果（true: 有; false: 无）
    //: private func func__checkIsGroupChatVC(roomId: String) -> Bool {
    private func may(roomId: String) -> Bool {
        //: let currentVC = self.func__getCurrentActivityVC()
        let currentVC = self.tillFill()
        //: if let vcArr = currentVC?.navigationController?.viewControllers {
        if let vcArr = currentVC?.navigationController?.viewControllers {
            //: for index in 0..<(vcArr.count) {
            for index in 0 ..< (vcArr.count) {
                //: if vcArr[index].isKind(of: TalkingGroupChatController.self) == true {
                if vcArr[index].isKind(of: ReceiverBecome.self) == true {
                    //: let vc = vcArr[index] as! TalkingGroupChatController
                    let vc = vcArr[index] as! ReceiverBecome
                    //: if vc.infoModel.roomId == roomId {
                    if vc.infoModel.roomId == roomId {
                        //: currentVC?.navigationController?.popToViewController(vc, animated: true)
                        currentVC?.navigationController?.popToViewController(vc, animated: true)
                        //: return true
                        return true
                    }
                }
            }
        }

        //: return false
        return false
    }

    // MARK: - 任意用户的动态列表

    //: func func__pushUserPostListWithUid(uid: String) {
    func sceneSensor(uid: String) {}

    // MARK: - 反馈feebook

    //: func func__pushFeedbackVC() {
    func onceMicSpecify() {
        //: let currentVC = func__getCurrentActivityVC()
        let currentVC = tillFill()
        //: let vc = TalkingFeedbackVC.init()
        let vc = ButtonInsert()
        //: currentVC?.navigationController?.pushViewController(vc, animated: true)
        currentVC?.navigationController?.pushViewController(vc, animated: true)
    }

    // MARK: - 消息跳转

    //: func func__actionPushForMessage(jumpModel: TalkingMessageJumpModel) {
    func wrapFast(jumpModel: BehaviorLargeThan) {
        //: if jumpModel.jumpKey == "url" {
        if jumpModel.jumpKey == (String(mainPendingMechanismCompletePath)) {
            //: func__pushToWebVC(urlStr: jumpModel.url)
            atRein(urlStr: jumpModel.url)
            //: } else if jumpModel.jumpKey == "userDetail"{
        } else if jumpModel.jumpKey == (appThumbLikeValue.replacingOccurrences(of: "view", with: "r") + "Detail") {}
    }

    // MARK: - 会员订阅网页

    //: @objc public func func__pushToSubscribePageWebVC() {
    @objc public func midOf() {
        //: TellEnd.share.func__pushToWebVC(webViewType: .SubscribePage)
        TellEnd.share.tierProcessing(webViewType: .SubscribePage)
    }
}

// MARK: - 跳转H5

//: extension TellEnd {
extension TellEnd {
    // 跳转全屏H5入口
    //: func func__pushToWebVC(webViewType: WebUrlType) {
    func tierProcessing(webViewType: PlatformVolume) {
        //: func__pushToWebVC(webViewType: webViewType, webConfig: TalkingWebConfig())
        providerFilter(webViewType: webViewType, webConfig: ImageTranslate())
    }

    /// 系统主动拨打弹窗，余额不足跳转充值/订阅页
    //: func func__jumpVideoCallToWebRecharge(popupCallIndex: Int, appendParams: String = "&type=6") {
    func boardConnection(popupCallIndex: Int, appendParams: String = (String(user_uniformString.suffix(7)))) {
        //: let view = TalkingInsufficientBalanceView.init()
        let view = AccessibleView()
        //: view.show()
        view.identityForm()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if popupCallIndex == 1 {
            if popupCallIndex == 1 { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
                self.tarUniversal(webViewType: .RechargeHalfPage)
                //: } else if popupCallIndex == 2 {
            } else if popupCallIndex == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                doRange(appendParams: appendParams)
            }
        }
    }

    /// 余额不足跳转充值/订阅页
    /// - Parameters:
    ///   - clickEvent: 埋点事件
    ///   - sufficient: 余额是否充足（默认true）
    //: func func__jumpToWebRecharge(clickEvent: String = "", sufficient: Bool = true, appendParams: String = "&type=7") {
    func somePush(clickEvent: String = "", sufficient: Bool = true, appendParams: String = (String(kThresholdQuitNeverKey.prefix(7)))) {
        //: let payWinType = QuantityeractionDelay.share.appUserConfigMode.payWinType
        let payWinType = QuantityeractionDelay.share.appUserConfigMode.payWinType
        // 样式：半屏充值页 || (已订阅 && 余额不足)

        //: let view = TalkingInsufficientBalanceView.init()
        let view = AccessibleView()
        //: view.show()
        view.identityForm()
        //: view.openBtnClock = { [weak self] in
        view.openBtnClock = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if payWinType == 1 || (QuantityeractionDelay.share.loginUserMode.loungePlus && sufficient == false) {
            if payWinType == 1 || (QuantityeractionDelay.share.loginUserMode.loungePlus && sufficient == false) { // 半屏充值页
                //: self.func__pushToHalfWebVC(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                self.tarUniversal(webViewType: .RechargeHalfPage, clickEvent: clickEvent)
                //: } else if payWinType == 2 {
            } else if payWinType == 2 { // 会员订阅弹窗
                //: func__pushToSubscribeAlert(appendParams: appendParams)
                doRange(appendParams: appendParams)
            }
        }
    }

    /// 跳转会员订阅弹窗
    /// - Parameter appendParams: url拼接参数（假视频流程拼接："&type=6；默认流程："&type=7"）
    //: func func__pushToSubscribeAlert(appendParams: String = "&type=7") {
    func doRange(appendParams: String = (String(kThresholdQuitNeverKey.prefix(7)))) {
        //: var urlStr = TalkingWebManager.getFullUrl(urlType: .SubscribeAlert)
        var urlStr = OnCalculate.occurThird(urlType: .SubscribeAlert)
        //: urlStr.append(appendParams)
        urlStr.append(appendParams)
        //: TellEnd.share.func__pushToWebVC(urlStr: urlStr)
        TellEnd.share.atRein(urlStr: urlStr)
        // 曝光用户数埋点
        //: uploadRecord.uploadRecordEvent(eventID: subscribtionRecordButton, parameterStr: ["amount": 0])
        k_playShadowValue.signal(eventID: dataAlwaysPath, parameterStr: [String(bytes: app_aliveName.map{$0^81}, encoding: .utf8)!: 0])
    }

    /// 跳转半屏H5入口
    /// - Parameters:
    ///   - webViewType: 网页URL
    ///   - clickEvent: 埋点事件
    //: func func__pushToHalfWebVC(webViewType: WebUrlType, clickEvent: String = "") {
    func tarUniversal(webViewType: PlatformVolume, clickEvent: String = "") {
        //: var config = TalkingWebConfig.init()
        var config = ImageTranslate()
        //: config.showLoadingView = false
        config.showLoadingView = false
        //: config.isHalfView = true
        config.isHalfView = true
        //: func__pushToWebVC(webViewType: webViewType, webConfig: config)
        providerFilter(webViewType: webViewType, webConfig: config)
        // 充值埋点
        //: if webViewType == .RechargeHalfPage {
        if webViewType == .RechargeHalfPage {
            //: RECHARGE_CLICK_EVENT = clickEvent
            notiBottomId = clickEvent
            //: if clickEvent.count > 0 {
            if clickEvent.count > 0 {
                //: uploadRecord.uploadRecordEvent(eventID: clickEvent, parameterStr: ["amount": 0])
                k_playShadowValue.signal(eventID: clickEvent, parameterStr: [String(bytes: app_aliveName.map{$0^81}, encoding: .utf8)!: 0])
            }
        }
    }

    /// 跳转到充值优惠弹窗
    //: func func_pushToDiscountRechargeView() {
    func thresholdFromCondition() {
        //: guard !QuantityeractionDelay.share.appUserConfigMode.discountUrl.isEmpty else { return }
        guard !QuantityeractionDelay.share.appUserConfigMode.discountUrl.isEmpty else { return }
        //: let httpsUrl = QuantityeractionDelay.share.appConfigMode.urlH5Domain + QuantityeractionDelay.share.appUserConfigMode.discountUrl
        let httpsUrl = QuantityeractionDelay.share.appConfigMode.urlH5Domain + QuantityeractionDelay.share.appUserConfigMode.discountUrl
        //: let httpUrl = httpsUrl.replacingOccurrences(of: "https://", with: "http://")
        let httpUrl = httpsUrl.replacingOccurrences(of: (String(mainOnString)), with: (String(showWantKey)))
        //: TellEnd.share.func__pushToWebVC(urlStr: httpUrl)
        TellEnd.share.atRein(urlStr: httpUrl)
    }

    //: func func__pushToWebVC(webViewType: WebUrlType, webConfig: TalkingWebConfig) {
    func providerFilter(webViewType: PlatformVolume, webConfig: ImageTranslate) {
        //: let urlStr = TalkingWebManager.getFullUrl(urlType: webViewType)
        let urlStr = OnCalculate.occurThird(urlType: webViewType)
        //: func__pushToWebView(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
        relationship(urlStr: urlStr, webViewType: webViewType, webConfig: webConfig)
    }

    //: func func__pushToWebVC(urlStr: String?, webConfig: TalkingWebConfig? = nil) {
    func atRein(urlStr: String?, webConfig: ImageTranslate? = nil) {
        //: let config = (webConfig == nil) ? TalkingWebConfig():webConfig
        let config = (webConfig == nil) ? ImageTranslate() : webConfig
        //: func__pushToWebView(urlStr: urlStr, webViewType: nil, webConfig: config!)
        relationship(urlStr: urlStr, webViewType: nil, webConfig: config!)
    }

    //: func func__pushToWebView(urlStr: String?, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    func relationship(urlStr: String?, webViewType: PlatformVolume?, webConfig: ImageTranslate) {
        //: guard urlStr != nil && urlStr != "" else { return }
        guard urlStr != nil, urlStr != "" else { return }
        //: var config = webConfig
        var config = webConfig

        // 有值且大于0，半屏按照比例展示
        //: if config.widthHeight ?? 0 > 0 {
        if config.widthHeight ?? 0 > 0 {
            //: config.isHalfView = true
            config.isHalfView = true
        }
        // 解析url中带的参数
        //: let urlParams = JSON(urlStr!.urlParameters ?? [:])
        let urlParams = JSON(urlStr!.urlParameters ?? [:])
        //: if urlParams["transparency"].stringValue == "1" { // 透明背景
        if urlParams[(String(userReasonMessage) + app_becomeSoundId.replacingOccurrences(of: "associate", with: "y"))].stringValue == "1" { // 透明背景
            //: config.clearBgColor = true
            config.clearBgColor = true
        }

        // url拼接参数
        //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "`%^{}\"[]|\\<>").inverted
        let allowCharSet = CharacterSet(charactersIn: "`%^{}" + "\"" + "[]|\\<>").inverted
        //: var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        var urlStr = urlStr!.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
        //: let otherParams = "version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)"
        let otherParams = (showReceiverMsg.replacingOccurrences(of: "indicator", with: "v") + String(show_smartString)) + "\(userManagerKeyUrl)" + (String(noti_backWhiteAbsoluteTitle.suffix(7)) + String(mainPressAccessId.suffix(4))) + "\(appMarkerResultValue)" + (String(main_nextTingMsg.prefix(5)) + "leId" + showJumpName.capitalized) + "\(appFirstFormat)"
        //: if urlStr.contains("?") {
        if urlStr.contains("?") {
            //: urlStr = "\(urlStr)&\(otherParams)"
            urlStr = "\(urlStr)&\(otherParams)"
            //: } else {
        } else {
            //: urlStr = "\(urlStr)?\(otherParams)"
            urlStr = "\(urlStr)?\(otherParams)"
        }

        //: let vc = TalkingWebViewController.init(urlString: urlStr, webViewType: webViewType, webConfig: config)
        let vc = SubstantialViewController(urlString: urlStr, webViewType: webViewType, webConfig: config)

        // 展示网页
        //: if let currentVC = func__getCurrentActivityVC() {
        if let currentVC = tillFill() {
            // 如果当前视图是模态上来的，跳转到下级时只能使用模态效果跳转
            //: var currentVCIsPresent = false
            var currentVCIsPresent = false
            //: if currentVC.isKind(of: TalkingWebViewController.self) &&
            if currentVC.isKind(of: SubstantialViewController.self) &&
                //: (currentVC as! TalkingWebViewController).isModal == true {
                (currentVC as! SubstantialViewController).isModal == true
            {
                //: currentVCIsPresent = true
                currentVCIsPresent = true
            }

            //: if config.isHalfView {
            if config.isHalfView { // 半屏使用present
                //: if config.widthHeight == nil {
                if config.widthHeight == nil {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight*2/3)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId * 2 / 3)
                    //: vc.tranConfig.configCorner(corners: [.topLeft, .topRight], radius: 18)
                    vc.tranConfig.floorRespective(corners: [.topLeft, .topRight], radius: 18)
                    //: } else {
                } else {
                    //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenWidth/config.widthHeight!)
                    vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataTabUrl, height: dataTabUrl / config.widthHeight!)
                }
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else if config.clearBgColor || currentVCIsPresent {
            } else if config.clearBgColor || currentVCIsPresent { // 透明背景只能用present
                //: vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
                vc.tranConfig.frameOfPresentedView = CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId)
                //: vc.isModal = true
                vc.isModal = true
                //: currentVC.present(vc, animated: true)
                currentVC.present(vc, animated: true)

                //: } else {
            } else {
                //: vc.isModal = false
                vc.isModal = false
                //: currentVC.navigationController?.pushViewController(vc, animated: true)
                currentVC.navigationController?.pushViewController(vc, animated: true)
            }
        }
    }
}

// MARK: - 获取当前视图、导航

//: extension TellEnd {
public extension TellEnd {
    //: @objc public func func__getCurrentActivityVC() -> UIViewController? {
    @objc func tillFill() -> UIViewController? {
        //: return currentViewController()
        return moreHide()
    }
}

//: struct TalkingMessageJumpModel: HandyJSON {
struct BehaviorLargeThan: HandyJSON {
    //: var jumpKey: String?
    var jumpKey: String? // 评论的id
    //: var jumpUid: String?
    var jumpUid: String?
    //: var url: String?
    var url: String?
}
