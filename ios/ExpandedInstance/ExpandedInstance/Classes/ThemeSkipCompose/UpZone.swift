
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let userThreeGraphicRedData:[Character] = ["S","O","C","K","E","T","_","A"]
fileprivate let app_adminKey:String = "absolute pic fresh universalDDR_KEY"

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let constEhExploreValue:[UInt8] = [0xf1,0xed,0xe1,0xe9,0xe7,0xf6,0xfd,0xf4,0xeb,0xe6,0xe7,0xed,0xe1,0xea,0xe3,0xf6,0xfd,0xf0,0xe7,0xf3,0xf7,0xe7,0xf1,0xf6,0xe1,0xe3,0xee,0xee,0xfd,0xec,0xed,0xf6,0xeb,0xe4,0xeb,0xe1,0xe3,0xf6,0xeb,0xed,0xec]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let const_sufficientPath:[UInt8] = [0x5d,0x50,0x4e,0x54,0x61,0x50,0x6a,0x51,0x57,0x5a,0x4c,0x5f,0x5e,0x4e,0x5d,0x50,0x50,0x59,0x6a,0x59,0x5a,0x5f,0x54,0x51,0x54,0x4e,0x4c,0x5f,0x54,0x5a,0x59]

fileprivate func scopeTotal(successful num: UInt8) -> UInt8 {
    let value = Int(num) + 245
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET 连接成功." :*/
fileprivate let const_cheapId:String = "SOCKby"
fileprivate let app_refuseMiniId:String = "T \u{8fde}接成功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let const_consumerPutThreadKey:String = "sock"
fileprivate let dataSophisticatedPath:String = "败, erall persist transform assist"

/*: "SOCKET 失败 :*/
fileprivate let show_occurUrl:[Character] = ["S","O","C","K","E","T"," ","\u{5931}","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let main_symbolKey:String = "suspend, 切换过"
fileprivate let dataBitImpressionPath:String = "et域名\u{91cd}新"
fileprivate let userProjectionId:[Character] = ["\u{5c1d}","试","\u{ff0c}","地","址","："]

/*: "SOCKET 已关闭, code: :*/
fileprivate let mainNumbOkKey:String = "SOCfeature"
fileprivate let kThreeString:String = "闭, code:fore currently obtain"

/*: , reason: :*/
fileprivate let constCorrectMeetingUrl:[Character] = [","," ","r"]
fileprivate let const_largeName:String = "off script pack two closeeason:"

/*: /?sid= :*/
fileprivate let kSupporterGrainStr:String = "mutter calculate limited/?sid="

/*: &uid= :*/
fileprivate let appSymbolPath:String = "&uid=delivery excited transfer"

/*: &platform=iphone&version= :*/
fileprivate let appZzMobileMessage:String = "blind character bad smart&platfor"
fileprivate let main_beginString:String = "like phone tip too requiredm="
fileprivate let show_insteadKey:String = "IPHONE"
fileprivate let showThreeData:String = "condition leadingsion="

/*: &packageId= :*/
fileprivate let show_lonelyKey:String = "dark she previous whole&pack"
fileprivate let appChoiceKey:String = "ageId=word literal file provision"

/*: "1v1server. :*/
fileprivate let k_singleGuideMessage:String = "1v1servinstall paint path model medal"
fileprivate let noti_matchKey:String = "biology vertical could weer."

/*: .com:7766" :*/
fileprivate let data_modifyUrl:[Character] = [".","c","o","m",":","7","7","6","6"]

/*: "43.129.185.194:7767" :*/
fileprivate let mainTriggerTitle:String = "43.129.1"
fileprivate let kUserStandardMsg:String = "85.1"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let userTingString:[UInt8] = [0x2e,0xad,0x97,0xe9,0xb3,0x85,0xe5,0x91,0xbd,0xe7,0xad,0x96,0xe6,0x8c,0xbc,0xef,0xac,0x90,0xe5,0x91,0x9b,0xe7,0x64,0x65,0x67,0x6e,0x61,0x68,0x43,0x79,0x74,0x69,0x6c,0x69,0x62,0x61,0x68,0x63,0x61,0x65,0x72,0x9c,0xbb,0xe7,0x91,0xbd,0xe7,0x20,0x54,0x45,0x4b,0x43,0x4f,0x53]

/*: "cmd" :*/
fileprivate let dataCycleMaleTitle:String = "cmback"

/*: "ping" :*/
fileprivate let main_permissionMsg:[Character] = ["p","i","n","g"]

/*: "callHeartbeat" :*/
fileprivate let userPmMsg:[Character] = ["c","a","l","l","H","e","a","r","t","b","e","a"]
fileprivate let show_mayMessage:[Character] = ["t"]

/*: "recievedDate" :*/
fileprivate let userStateUrl:[Character] = ["r","e","c","i","e","v","e","d","D","a"]
fileprivate let appScenarioStr:String = "TE"

/*: "liveHeartBeat" :*/
fileprivate let main_increaseAgentStr:String = "liveHeasset force delay shall agent"

/*: "🔗 关闭 socket" :*/
fileprivate let notiToStr:[Character] = ["🔗"," ","关","闭"," ","s","o","c","k"]
fileprivate let show_alwaysVisibleMsg:[Character] = ["e","t"]

/*: "The user logs out" :*/
fileprivate let app_fullFormat:String = "The reflect priority distinction alongside particle"
fileprivate let showObtainMsg:String = " logsubstantial cap ceiling against span"

/*: "SOCKET  :*/
fileprivate let app_rankKey:[Character] = ["S","O","C","K","E","T"," "]

/*: 重连. 重试次数： :*/
fileprivate let app_opportunityAlwaysString:[Character] = ["重","连","."," ","重"]
fileprivate let userFeedbackFormat:[Character] = ["试","次","数","："]

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let k_contactTitle:String = "visitorC"
fileprivate let userHousingMsg:[Character] = ["K","E","T"," ","连","\u{63a5}","正","常"," ","不","\u{9700}","要","重","新","\u{8fde}","\u{63a5}","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let kSituationString:[Character] = ["S","O","C","K","E","T"," ","\u{6b63}","在","连","接"," ","不"]
fileprivate let noti_screenPath:[Character] = ["需","要","重","\u{65b0}","连","接","."]

/*: "&resume=1" :*/
fileprivate let userLocateMsg:[Character] = ["&"]
fileprivate let kProgressTitle:String = "speaker option chartresume=1"

/*: "&lang= :*/
fileprivate let kMediaRevealUrl:String = "&lang=via hero calendar camera"

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let const_readyData:[Character] = ["S","O","C","K","E","T"," ","开","始","连","接",","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let const_sovereignStr:String = "momentum provideonNet"
fileprivate let dataRequireRefreshString:[Character] = ["D","i","s","a","b","l","e","d"]

/*: "errno" :*/
fileprivate let const_marginId:String = "errboth"

/*: "data" :*/
fileprivate let app_calledString:[Character] = ["d","a","t","a"]

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let showHalfId:String = "SOCKET eager technique"
fileprivate let data_appearanceName:[Character] = ["连","接","保","\u{6d3b}","c","m","d",":"," ","p","i","n","g","."]

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let main_secretMessage:[UInt8] = [0x44,0x40,0x34,0x3c,0x36,0x45,0x11,0x65,0x63,0x65,0x54,0xd5,0xb0,0x8e,0xd7,0xa5,0xac,0x54,0x5e,0x55,0x2b,0x11,0x54,0x52,0x5d,0x5d,0x39,0x56,0x52,0x63,0x65,0x53,0x56,0x52,0x65,0x1f]

fileprivate func remainRecto(mutter num: UInt8) -> UInt8 {
    let value = Int(num) - 241
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let show_distinctionData:[UInt8] = [0x55,0x49,0x45,0x4d,0x43,0x52,0x26,0x6a,0x6f,0x70,0x63,0xe2,0xb9,0x9b,0xe0,0xb2,0xbd,0x65,0x6b,0x62,0x3c,0x26,0x6a,0x6f,0x70,0x63,0x4e,0x63,0x67,0x74,0x72,0x44,0x63,0x67,0x72,0x28]

private func nobodyVolume(consume num: UInt8) -> UInt8 {
    return num ^ 6
}

/*: "onP" :*/
fileprivate let const_downUrl:String = "onPinstance farmer floor media beyond"

/*: "msg" :*/
fileprivate let app_generalFormat:String = "mtipg"

/*: "🔗 处理消息：cmdName: :*/
fileprivate let main_telescopeId:String = "🔗 处理\u{6d88}"
fileprivate let constTherePath:String = "go char sophisticated contentdNam"
fileprivate let appLatFormat:String = "assert texturee:"

/*: , errNo: :*/
fileprivate let appCommunicatePleaseSourceName:String = ", erad filter"

/*: , msg: :*/
fileprivate let constPlayerMessage:String = ", msg:colonial need special option giving"

/*: , data: :*/
fileprivate let noti_colorFactorStr:String = ", datbar retain"
fileprivate let k_placeMessage:[Character] = ["a",":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let main_architectureDivideId:String = "SOCKET instead bucket"
fileprivate let userRandomTitle:[Character] = ["\u{63a5}","收","异","常","\u{6d88}","息","\u{ff1a}","c","m","d"]
fileprivate let main_missDrawKey:[Character] = ["N","a","m","e",":"]

/*: "onReceivedGift" :*/
fileprivate let appIdentifyEffectFormat:String = "onRecrunning mask"
fileprivate let userInfoValue:[Character] = ["e","i"]
fileprivate let k_placeData:String = "vedGiftangle interaction reserve equally"

/*: "onVideoMatch" :*/
fileprivate let constQueryionString:String = "track"
fileprivate let data_noAPath:String = "nVidop depth instead shared follow"

/*: "onRequestCall" :*/
fileprivate let data_itsName:String = "top stoponRequ"
fileprivate let userHourPath:[Character] = ["a","l","l"]

/*: "onCancelCall" :*/
fileprivate let noti_throughMsg:String = "task operateonCancel"
fileprivate let userCurrencyNormFormat:[Character] = ["C","a","l","l"]

/*: "onRefuseCall" :*/
fileprivate let mainProjectionString:String = "view"
fileprivate let k_yesterdayString:[Character] = ["R","e","f","u","s","e","C","a","l","l"]

/*: "onAcceptCall" :*/
fileprivate let dataDiscloseValue:[Character] = ["o","n","A","c","c","e","p","t","C"]
fileprivate let kPlainMsg:String = "ALL"

/*: "onStopCall" :*/
fileprivate let user_itemPersonKey:[Character] = ["o","n","S","t","o","p","C","a"]
fileprivate let const_wordString:String = "contrast"

/*: "freeCallTimes" :*/
fileprivate let showBecomeCropString:[Character] = ["f","r","e","e","C","a","l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let userMaxAgePath:String = "mfCoinheight adjust line"

/*: "onMissCoin" :*/
fileprivate let show_layerComprehensiveData:String = "command try anonymousonMissCo"
fileprivate let main_creationMsg:String = "IN"

/*: "acceptCall" :*/
fileprivate let main_gravityTitle:String = "acceptCamissing fresh out image"
fileprivate let kIdentifyDateId:String = "saltsalt"

/*: "stopCall" :*/
fileprivate let data_mutualStr:[Character] = ["s","t","o","p","C","a"]
fileprivate let dataIncludeName:String = "mutter"

/*: "requestCall" :*/
fileprivate let show_exclusiveString:String = "writingequ"
fileprivate let show_takeValue:String = "L"

/*: "[Invite you to a video call...]" :*/
fileprivate let app_statAddressMsg:[UInt8] = [0x3,0x11,0x36,0x2e,0x31,0x2c,0x3d,0x78,0x21,0x37,0x2d,0x78,0x2c,0x37,0x78,0x39,0x78,0x2e,0x31,0x3c,0x3d,0x37,0x78,0x3b,0x39,0x34,0x34,0x76,0x76,0x76,0x5]

private func spotClub(lose num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "type" :*/
fileprivate let appAccessAcceptValue:[Character] = ["t","y","p","e"]

/*: "[Invite you to a voice call...]" :*/
fileprivate let kDensePath:[UInt8] = [0xc4,0xd6,0xf1,0xe9,0xf6,0xeb,0xfa,0xbf,0xe6,0xf0,0xea,0xbf,0xeb,0xf0,0xbf,0xfe,0xbf,0xe9,0xf0,0xf6,0xfc,0xfa,0xbf,0xfc,0xfe,0xf3,0xf3,0xb1,0xb1,0xb1,0xc2]

private func pointOp(ceiling num: UInt8) -> UInt8 {
    return num ^ 159
}

/*: "uid" :*/
fileprivate let userGirlSituationId:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let app_explainWeeklyTitle:[Character] = ["n"]
fileprivate let user_femaleValue:[Character] = ["i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let notiGateDepartmentString:[Character] = ["h","e"]
fileprivate let constNearMsg:String = "adPicabs relation i remark method"

/*: "onCallResume" :*/
fileprivate let showTimeFormat:[Character] = ["o","n","C","a","l","l","R","e","s","u","m","e"]

/*: "cancelCall" :*/
fileprivate let constApplicationId:String = "caold"

/*: "refuseCall" :*/
fileprivate let appContactMessage:String = "reassertus"

/*: "onSendProgramGift" :*/
fileprivate let const_sophisticatedPath:String = "onSeplain fun system"
fileprivate let dataThinMsg:String = "special resident skipndPro"
fileprivate let mainDifficultStr:String = "Giftfollowing every plus stream"

/*: "onGameFs" :*/
fileprivate let main_verbBindName:[Character] = ["o","n","G","a","m","e","F"]
fileprivate let const_wordAgreeMessage:[Character] = ["s"]

/*: "broadStatus" :*/
fileprivate let k_furtherValidTeaString:String = "pick hero available welcomebroadS"
fileprivate let showSubsequentData:[Character] = ["t","a","t","u","s"]

/*: "onCallChatMsg" :*/
fileprivate let data_checkFormat:String = "hold pm dismissonCallCh"
fileprivate let noti_writeChildTitle:String = "atMsgpet merge properly permission subject"

/*: "onUserBlock" :*/
fileprivate let k_trustPath:String = "the repeat pressonUs"

/*: "isRecharge" :*/
fileprivate let kDenyItemValue:String = "isRecmagnitude over from estimated substantial"

/*: "source" :*/
fileprivate let dataPersonDatabaseFormat:String = "soucapacityc"
fileprivate let const_ignoreDomainIdentityUrl:[Character] = ["e"]

/*: "invitePop" :*/
fileprivate let showCurrentData:String = "invimeet"
fileprivate let kTempStr:[Character] = ["e","P","o","p"]

/*: "videoPop" :*/
fileprivate let mainMethodUrl:String = "videoPopdrag absolute"

/*: "coin" :*/
fileprivate let noti_modelTitle:[Character] = ["c","o","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  UpZone.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let app_detailPath = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let const_packageStr = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let app_lengthValue = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let appCalculateStr = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let main_showChapterKey = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let show_presentRecordMsg = (String(userThreeGraphicRedData) + String(app_adminKey.suffix(7)))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let userAroundUrl = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let data_gateTakePath = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let constConversationName = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let data_loadOutsideUrl = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let notiAwayName = NSNotification.Name(rawValue: String(bytes: constEhExploreValue.map{$0^162}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let const_darkPurpleName = NSNotification.Name(rawValue: String(bytes: const_sufficientPath.map{scopeTotal(successful: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol StatusKit: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func frequencyComment(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol IndicatorDirection: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func faceBlock(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func beNet(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol MasterRange: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func appropriateProperty(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func minimize(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func plusBy(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func nonePriority(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol SingleExtra: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func shadowBrush(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func closePainter(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class UpZone: NSObject {
    //: enum SocketRetryType: String {
    enum NumberGrain: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var agileImageSkewyVC: AnyObject?
    var agileImageSkewyVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CoordinatorExtra.shared.direction == .rightToLeft ? 0 : dataTabUrl,
        //: y: StatusBarHeight+190
        y: dataDoingRapidMsg + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: CoordinatorExtra.shared.direction == .rightToLeft ? 0 : dataTabUrl,
        //: y: StatusBarHeight+190
        y: dataDoingRapidMsg + 190
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: StatusKit? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: IndicatorDirection? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: MasterRange? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: SingleExtra? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var agileImageSkewy = false
    @objc dynamic var agileImageSkewy = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = UpZone()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(noneParent(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(speakerSumo),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension UpZone: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        MaximumMaster.circleStat(msg: (const_cheapId.replacingOccurrences(of: "by", with: "E") + app_refuseMiniId))
        //: sendPing()
        hiWith()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: noti_gestureTechniqueMsg,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: show_presentRecordMsg)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        examplePermission()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        MaximumMaster.circleStat(msg: (const_consumerPutThreadKey.uppercased() + "ET 连\u{63a5}失" + String(dataSophisticatedPath.prefix(5)) + "ror:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = QuantityeractionDelay.share.reachability?.connection, status != .unavailable else {
        guard let status = QuantityeractionDelay.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == appCalculateStr {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = afterMap(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            MaximumMaster.circleStat(msg: (String(show_occurUrl)) + "\(appCalculateStr)" + (main_symbolKey.replacingOccurrences(of: "suspend", with: "次") + "Sock" + dataBitImpressionPath + String(userProjectionId)) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.thresholdUp(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.overAdvanced(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        MaximumMaster.circleStat(msg: (mainNumbOkKey.replacingOccurrences(of: "feature", with: "K") + "ET \u{5df2}关" + String(kThreeString.prefix(8))) + "\(code)" + (String(constCorrectMeetingUrl) + String(const_largeName.suffix(6))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.startSpeed(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == data_loadOutsideUrl {
            //: return
            return
        }
        //: if let status = QuantityeractionDelay.share.reachability?.connection, status != .unavailable {
        if let status = QuantityeractionDelay.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.thresholdUp(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func afterMap(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = QuantityeractionDelay.share.appConfigMode.ws
        self.linkAddrArr = QuantityeractionDelay.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = roundGraph()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: show_presentRecordMsg)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = QuantityeractionDelay.share.loginUid
        let uid = QuantityeractionDelay.share.loginUid
        //: let sid = QuantityeractionDelay.share.loginSessionId
        let sid = QuantityeractionDelay.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(kSupporterGrainStr.suffix(6))) + "\(sid)" + (String(appSymbolPath.prefix(5))) + "\(uid)" + (String(appZzMobileMessage.suffix(8)) + String(main_beginString.suffix(2)) + show_insteadKey.lowercased() + "&ver" + String(showThreeData.suffix(5))) + "\(userManagerKeyUrl)" + (String(show_lonelyKey.suffix(5)) + String(appChoiceKey.prefix(6))) + "\(appMarkerResultValue)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func roundGraph() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return main_byPhoneValue ? [(String(k_singleGuideMessage.prefix(7)) + String(noti_matchKey.suffix(3))) + "\(show_whiteTitle)" + (String(data_modifyUrl))] : [(mainTriggerTitle.capitalized + kUserStandardMsg.capitalized + "94:7767")]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension UpZone {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func noneParent(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            MaximumMaster.circleStat(msg: String(bytes: userTingString.reversed(), encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.startSpeed(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.thresholdUp(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func speakerSumo() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.thresholdUp(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.accountFor()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension UpZone {
    /// socket保活心跳包
    //: private func sendPing() {
    private func hiWith() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: app_detailPath, block: { timer in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.commonCrop(info: [(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d")): (String(main_permissionMsg))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func rangeRole() {
        //: endTalkingPing()
        resistanceAdd()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: const_packageStr, block: { timer in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.agileImageSkewy || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.agileImageSkewyVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.agileImageSkewyVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.agileImageSkewy || (self.moreHide()?.isKind(of: StatusEven.self) == false && self.moreHide()?.isKind(of: TellPerform.self) == false) && (self.agileImageSkewyVC?.isKind(of: StatusEven.self) == false && self.agileImageSkewyVC?.isKind(of: TellPerform.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.commonCrop(info: [(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d")): (String(userPmMsg) + String(show_mayMessage))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func resistanceAdd() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func accountFor() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(String(userStateUrl) + appScenarioStr.lowercased())] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < data_cellData {
                //: QuantityeractionDelay.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                QuantityeractionDelay.share.resumeArc(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension UpZone {
    /// 开启直播心跳包
    //: func startLivePing() {
    func orangeVisual() {
        //: endLivePing()
        betweenPrivacy()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: app_lengthValue, block: { timer in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.commonCrop(info: [(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d")): (String(main_increaseAgentStr.prefix(6)) + "artBeat")])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func betweenPrivacy() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension UpZone {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func examplePermission() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = TexturePersistVisibleKit()
        //: let keyWindow = MacroDefine.getWindow()
        let keyWindow = VersusQuit.further()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.agileImageSkewy
        self.containerView?.isHidden = !self.agileImageSkewy
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func product() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension UpZone {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func startSpeed(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(notiToStr) + String(show_alwaysVisibleMsg)))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: data_loadOutsideUrl, reason: (String(app_fullFormat.prefix(4)) + "user" + String(showObtainMsg.prefix(4)) + "s out"))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        resistanceAdd()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            product()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func behindProvide() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = UpZone.shared.afterMap(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.thresholdUp(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func thresholdUp(timeInterval: TimeInterval, type: NumberGrain) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            MaximumMaster.circleStat(msg: (String(app_rankKey)) + "\(type.rawValue)" + (String(app_opportunityAlwaysString) + String(userFeedbackFormat)) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.through(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func through(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                MaximumMaster.circleStat(msg: (k_contactTitle.replacingOccurrences(of: "visitor", with: "SO") + String(userHousingMsg)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                MaximumMaster.circleStat(msg: (String(kSituationString) + String(noti_screenPath)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.agileImageSkewy {
        if self.agileImageSkewy {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(userLocateMsg) + String(kProgressTitle.suffix(8))))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(kMediaRevealUrl.prefix(6))) + "\(ImageClear.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        startSpeed(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = main_showChapterKey
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        MaximumMaster.circleStat(msg: (String(const_readyData)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func commonCrop(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if QuantityeractionDelay.share.reachability?.connection == nil || QuantityeractionDelay.share.reachability?.connection == .unavailable {
        if QuantityeractionDelay.share.reachability?.connection == nil || QuantityeractionDelay.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d")): (String(const_sovereignStr.suffix(5)) + String(dataRequireRefreshString)),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (const_marginId.replacingOccurrences(of: "both", with: "no")): "\(constConversationName)",
                                       //: "data": [:]]
                                       (String(app_calledString)): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            overAdvanced(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d"))] as? String == (String(main_permissionMsg)) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                MaximumMaster.circleStat(msg: (String(showHalfId.prefix(7)) + String(data_appearanceName)))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d"))] as? String == (String(userPmMsg) + String(show_mayMessage)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                MaximumMaster.circleStat(msg: String(bytes: main_secretMessage.map{remainRecto(mutter: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d"))] as? String == (String(main_increaseAgentStr.prefix(6)) + "artBeat") {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                MaximumMaster.circleStat(msg: String(bytes: show_distinctionData.map{nobodyVolume(consume: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d"))] as? String == (String(main_permissionMsg)) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d")): (String(const_downUrl.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (const_marginId.replacingOccurrences(of: "both", with: "no")): "\(userAroundUrl)",
                                           //: "data": [:]]
                                           (String(app_calledString)): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                overAdvanced(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.thresholdUp(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func overAdvanced(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(dataCycleMaleTitle.replacingOccurrences(of: "back", with: "d"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(const_marginId.replacingOccurrences(of: "both", with: "no"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(app_generalFormat.replacingOccurrences(of: "tip", with: "s"))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(String(app_calledString))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (main_telescopeId + "息：cm" + String(constTherePath.suffix(4)) + String(appLatFormat.suffix(2))) + "\(cmdName)" + (String(appCommunicatePleaseSourceName.prefix(4)) + "rNo:") + "\(errorNo)" + (String(constPlayerMessage.prefix(6))) + "\(msg)" + (String(noti_colorFactorStr.prefix(5)) + String(k_placeMessage)) + "\(data)")

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            MaximumMaster.circleStat(msg: (String(main_architectureDivideId.prefix(7)) + String(userRandomTitle) + String(main_missDrawKey)) + "\(cmdName)" + (String(appCommunicatePleaseSourceName.prefix(4)) + "rNo:") + "\(errorNo)" + (String(constPlayerMessage.prefix(6))) + "\(msg)" + (String(noti_colorFactorStr.prefix(5)) + String(k_placeMessage)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: QuantityeractionDelay.share.loginUserMode.freeCallTimes = 0
                QuantityeractionDelay.share.loginUserMode.freeCallTimes = 0
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.frequencyComment(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(appIdentifyEffectFormat.prefix(5)) + String(userInfoValue) + String(k_placeData.prefix(7))) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: showLibraryString, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (constQueryionString.replacingOccurrences(of: "track", with: "o") + String(data_noAPath.prefix(4)) + "eoMatch") { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.faceBlock(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(data_itsName.suffix(6)) + "estC" + String(userHourPath)) { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.beNet(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(noti_throughMsg.suffix(8)) + String(userCurrencyNormFormat)) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (mainProjectionString.replacingOccurrences(of: "view", with: "on") + String(k_yesterdayString)) { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(dataDiscloseValue) + kPlainMsg.lowercased()) { // 接收方发送acceptCall回执
            //: self.agileImageSkewy = true
            self.agileImageSkewy = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            rangeRole()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.closePainter(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(user_itemPersonKey) + const_wordString.replacingOccurrences(of: "contrast", with: "ll")) { // 接收方发送stopCall回执
            //: self.agileImageSkewy = false
            self.agileImageSkewy = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            resistanceAdd()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(showBecomeCropString))] as? Int {
                //: QuantityeractionDelay.share.loginUserMode.freeCallTimes = freeCallTimes
                QuantityeractionDelay.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: QuantityeractionDelay.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            QuantityeractionDelay.share.loginUserMode.mf_coin = json[(String(app_calledString))][(String(userMaxAgePath.prefix(6)))].stringValue

            //: inviteCallPopViewEndShow(data: data)
            shBoth(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_listenFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(show_layerComprehensiveData.suffix(8)) + main_creationMsg.lowercased()) { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.nonePriority(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (String(main_gravityTitle.prefix(8)) + kIdentifyDateId.replacingOccurrences(of: "salt", with: "l")) { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.agileImageSkewy = true
            self.agileImageSkewy = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            rangeRole()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.appropriateProperty(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (String(data_mutualStr) + dataIncludeName.replacingOccurrences(of: "mutter", with: "ll")) { // 接收方/拨打方 挂断了通话
            //: self.agileImageSkewy = false
            self.agileImageSkewy = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            resistanceAdd()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.minimize(data: data)
            // 更新余额
            //: QuantityeractionDelay.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            QuantityeractionDelay.share.loginUserMode.mf_coin = json[(String(app_calledString))][(String(userMaxAgePath.prefix(6)))].stringValue
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(showBecomeCropString))] as? Int {
                //: QuantityeractionDelay.share.loginUserMode.freeCallTimes = freeCallTimes
                QuantityeractionDelay.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            //: inviteCallPopViewEndShow(data: data)
            shBoth(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: k_listenFormat, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false

            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (show_exclusiveString.replacingOccurrences(of: "writing", with: "r") + "estCal" + show_takeValue.lowercased()) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: notiAwayName, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: QuantityeractionDelay.share.recieve1v1TalkRequest(info: data)
                QuantityeractionDelay.share.resumeArc(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(String(userStateUrl) + appScenarioStr.lowercased())] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: app_statAddressMsg.map{spotClub(lose: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(String(app_calledString))][(String(appAccessAcceptValue))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: kDensePath.map{pointOp(ceiling: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                AlongAt.crushBy(uid: String(json[(String(app_calledString))][(String(userGirlSituationId))].intValue),
                                          //: title: data["nickname"] as? String,
                                          title: data[(String(app_explainWeeklyTitle) + String(user_femaleValue))] as? String,
                                          //: body: body,
                                          body: body,
                                          //: imageUrl: data["headPic"] as? String)
                                          imageUrl: data[(String(notiGateDepartmentString) + String(constNearMsg.prefix(5)))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                AlongAt.shared.marginSensor()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(showTimeFormat)) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.agileImageSkewy = true
            self.agileImageSkewy = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            rangeRole()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(showTimeFormat))] = "1"
            //: QuantityeractionDelay.share.recieve1v1TalkResumeRequest(info: resumeData)
            QuantityeractionDelay.share.finishDown(info: resumeData)

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (constApplicationId.replacingOccurrences(of: "old", with: "nc") + "elCall") { // 拨打方拒绝了通话，接收方收到该指令
            //: self.agileImageSkewy = false
            self.agileImageSkewy = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            resistanceAdd()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.plusBy(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                AlongAt.iconThreshold(identifier: String(json[(String(app_calledString))][(String(userGirlSituationId))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                AlongAt.shared.beef()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (appContactMessage.replacingOccurrences(of: "assert", with: "f") + "eCall") { // 接收方拒绝了通话，拨打方收到该指令
            //: self.agileImageSkewy = false
            self.agileImageSkewy = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            resistanceAdd()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.shadowBrush(data: data)

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(const_sophisticatedPath.prefix(4)) + String(dataThinMsg.suffix(5)) + "gram" + String(mainDifficultStr.prefix(4))) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.outsideFeature(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(main_verbBindName) + String(const_wordAgreeMessage)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(k_furtherValidTeaString.suffix(6)) + String(showSubsequentData))] as? Int ?? -1
            //: if QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue {
            if QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: const_darkPurpleName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: showLibraryString, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: const_darkPurpleName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(data_checkFormat.suffix(8)) + String(noti_writeChildTitle.prefix(5))) { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            SingleConnect.writeAnnouncement().monthExist(msg: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(k_trustPath.suffix(4)) + "erBlock") { // 用户被封号
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: main_matchTitle, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension UpZone {
    /// 邀请通话通话结束之后，判断弹窗类型
    //: private func inviteCallPopViewEndShow(data: [String: Any]) {
    private func shBoth(data: [String: Any]) {
        // 展示评分弹窗
        //: AppStoreRatingManager.shared.requestQuickRating()
        CommandOrientation.shared.connectionMain()
        //: guard QuantityeractionDelay.share.loginUserMode.sex == Gender.male.rawValue else {
        guard QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.male.rawValue else {
            //: return
            return
        }
        // 弹出优惠充值弹窗
        //: if (data["isRecharge"]) as? Bool == false {
        if data[(String(kDenyItemValue.prefix(5)) + "harge")] as? Bool == false {
            //: TellEnd.share.func_pushToDiscountRechargeView()
            TellEnd.share.thresholdFromCondition()
        }
        // 半屏充值弹窗场景
        //: let source = data["source"] as? String
        let source = data[(dataPersonDatabaseFormat.replacingOccurrences(of: "capacity", with: "r") + String(const_ignoreDomainIdentityUrl))] as? String
        //: guard source == "invitePop" || source == "videoPop" else {
        guard source == (showCurrentData.replacingOccurrences(of: "meet", with: "t") + String(kTempStr)) || source == (String(mainMethodUrl.prefix(8))) else {
            //: return
            return
        }
        //: if (data["coin"] as? Int) ?? 0 < 60 {
        if (data[(String(noti_modelTitle))] as? Int) ?? 0 < 60 {
            //: TellEnd.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            TellEnd.share.tarUniversal(webViewType: .RechargeHalfPage)
        }
    }
}
