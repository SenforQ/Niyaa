
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_dilemmaStr:[UInt8] = [0xfa,0xfd,0xfa,0xe7,0xbb,0xf0,0xfc,0xf7,0xf6,0xe1,0xa9,0xba,0xb3,0xfb,0xf2,0xe0,0xb3,0xfd,0xfc,0xe7,0xb3,0xf1,0xf6,0xf6,0xfd,0xb3,0xfa,0xfe,0xe3,0xff,0xf6,0xfe,0xf6,0xfd,0xe7,0xf6,0xf7]

private func elementFeature(speaker num: UInt8) -> UInt8 {
    return num ^ 147
}

/*: "Licensing" :*/
fileprivate let constMinimumReplaceValue:String = "among grandfather produceLicensin"
fileprivate let data_delayTailId:String = "indeed"

/*: "Granting licences to receive better video calls and earn more money" :*/
fileprivate let k_cookieTriggerKey:[UInt8] = [0x75,0x40,0x53,0x5c,0x46,0x5b,0x5c,0x55,0x12,0x5e,0x5b,0x51,0x57,0x5c,0x51,0x57,0x41,0x12,0x46,0x5d,0x12,0x40,0x57,0x51,0x57,0x5b,0x44,0x57,0x12,0x50,0x57,0x46,0x46,0x57,0x40,0x12,0x44,0x5b,0x56,0x57,0x5d,0x12,0x51,0x53,0x5e,0x5e,0x41,0x12,0x53,0x5c,0x56,0x12,0x57,0x53,0x40,0x5c,0x12,0x5f,0x5d,0x40,0x57,0x12,0x5f,0x5d,0x5c,0x57,0x4b]

private func butterflyExtent(yellow num: UInt8) -> UInt8 {
    return num ^ 50
}

/*: "Turn on Camera and recording permissions in settings to use the feature properly" :*/
fileprivate let main_discloseValue:[UInt8] = [0xcf,0xee,0xe9,0xf5,0xbb,0xf4,0xf5,0xbb,0xd8,0xfa,0xf6,0xfe,0xe9,0xfa,0xbb,0xfa,0xf5,0xff,0xbb,0xe9,0xfe,0xf8,0xf4,0xe9,0xff,0xf2,0xf5,0xfc,0xbb,0xeb,0xfe,0xe9,0xf6,0xf2,0xe8,0xe8,0xf2,0xf4,0xf5,0xe8,0xbb,0xf2,0xf5,0xbb,0xe8,0xfe,0xef,0xef,0xf2,0xf5,0xfc,0xe8,0xbb,0xef,0xf4,0xbb,0xee,0xe8,0xfe,0xbb,0xef,0xf3,0xfe,0xbb,0xfd,0xfe,0xfa,0xef,0xee,0xe9,0xfe,0xbb,0xeb,0xe9,0xf4,0xeb,0xfe,0xe9,0xf7,0xe2]

private func blendPartner(floor num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "Cancel" :*/
fileprivate let k_warnFormat:[Character] = ["C","a","n","c","e","l"]

/*: "Open" :*/
fileprivate let mainStripValue:String = "Openour remark foundation"

/*: "icon_peimisionlic_camera" :*/
fileprivate let constTimeCrystalName:[Character] = ["i","c","o","n","_"]
fileprivate let notiAvailablePetValue:String = "departmentim"
fileprivate let mainThroughUrl:String = "some ad fadelic_ca"

/*: "icon_peimissionlic_mic" :*/
fileprivate let mainServiceMsg:String = "mirror tell beat rawicon_p"
fileprivate let showExceptStr:[Character] = ["s","s","i","o"]
fileprivate let noti_contextChangeTitle:[Character] = ["n","l","i","c","_","m","i","c"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  YellowView.swift
//  ExpandedInstance
//
//  Created by Charlotte on 2024/3/22.
//

//: import UIKit
import UIKit

//: class TalkingPermissionView: UIView {
class YellowView: UIView {
    //: var popView: TalkingPopView?
    var popView: AmongBind?

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: self.setupSubviews()
        self.micEnable()
        //: self.setupSubViewsConstraint()
        self.notBridge()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_dilemmaStr.map{elementFeature(speaker: $0)}, encoding: .utf8)!)
    }

    //: lazy var backView: UIView = {
    lazy var backView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.layer.cornerRadius = 15
        view.layer.cornerRadius = 15
        //: view.layer.masksToBounds = true
        view.layer.masksToBounds = true
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var titleLB: UILabel = {
    lazy var titleLB: UILabel = {
        //: let lb = UILabel.init()
        let lb = UILabel()
        //: lb.textColor = UIColor.appTitleColor()
        lb.textColor = UIColor.thinOf()
        //: lb.font = UIFont.pingfangMediumFont(fontSize: 20)
        lb.font = UIFont.wantStream(fontSize: 20)
        //: lb.text = "Licensing".localized
        lb.text = (String(constMinimumReplaceValue.suffix(8)) + data_delayTailId.replacingOccurrences(of: "indeed", with: "g")).localized
        //: return lb
        return lb
        //: }()
    }()

    //: lazy var messageLB: UILabel = {
    lazy var messageLB: UILabel = {
        //: let lb = UILabel.init()
        let lb = UILabel()
        //: lb.textColor = UIColor.appTitleColor()
        lb.textColor = UIColor.thinOf()
        //: lb.font = UIFont.pingfangRugularFont(fontSize: 16)
        lb.font = UIFont.botLocal(fontSize: 16)
        //: if QuantityeractionDelay.share.loginUserMode.sex == Gender.female.rawValue && QuantityeractionDelay.share.appStatus != AppSkinStatus.special.rawValue {
        if QuantityeractionDelay.share.loginUserMode.sex == CommonAmong.female.rawValue && QuantityeractionDelay.share.appStatus != TotalervalObserve.special.rawValue {
            //: lb.text = "Granting licences to receive better video calls and earn more money".localized
            lb.text = String(bytes: k_cookieTriggerKey.map{butterflyExtent(yellow: $0)}, encoding: .utf8)!.localized
            //: } else {
        } else {
            //: lb.text = "Turn on Camera and recording permissions in settings to use the feature properly".localized
            lb.text = String(bytes: main_discloseValue.map{blendPartner(floor: $0)}, encoding: .utf8)!.localized
        }
        //: lb.numberOfLines = 0
        lb.numberOfLines = 0
        //: lb.textAlignment = .center
        lb.textAlignment = .center
        //: return lb
        return lb
        //: }()
    }()

    //: lazy var cancleBtn: UIButton = {
    lazy var cancleBtn: UIButton = {
        //: let btn = UIButton.init(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.setTitle("Cancel".localized, for: .normal)
        btn.setTitle((String(k_warnFormat)).localized, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 16)
        btn.titleLabel?.font = UIFont.alongsideOk(type: .Medium, fontSize: 16)
        //: btn.setTitleColor(.appValueDetailColor(), for: .normal)
        btn.setTitleColor(.underCountensityLanguage(), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.multiSince().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.addTarget(self, action: #selector(cancleBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(beforeFactory), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var openBtn: UIButton = {
    lazy var openBtn: UIButton = {
        //: let btn = UIButton.init(type: .custom)
        let btn = UIButton(type: .custom)
        //: btn.setTitle("Open".localized, for: .normal)
        btn.setTitle((String(mainStripValue.prefix(4))).localized, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 16)
        btn.titleLabel?.font = UIFont.alongsideOk(type: .Medium, fontSize: 16)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 130, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.doEmbrace(colors: UIColor.monthForMomentum(), size: CGSize(width: 130, height: 50)), for: .normal)
        //: btn.addTarget(self, action: #selector(openBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(signFormat), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

//: extension TalkingPermissionView {
extension YellowView {
    //: func show() {
    func blackVideo() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = AmongBind(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId)
        //: popView?.initWithView(view: self)
        popView?.searched(view: self)
        //: popView?.showInView(view: MacroDefine.getWindow())
        popView?.petDrawer(view: VersusQuit.further())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func cancleBtnClick() {
    @objc func beforeFactory() {
        //: popView?.dismissView()
        popView?.recognize()
        //: popView = nil
        popView = nil
    }

    //: @objc func openBtnClick() {
    @objc func signFormat() {
        //: let url = URL(string: UIApplication.openSettingsURLString)
        let url = URL(string: UIApplication.openSettingsURLString)
        //: if  UIApplication.shared.canOpenURL(url!) {
        if UIApplication.shared.canOpenURL(url!) {
            //: if #available(iOS 10, *) {
            if #available(iOS 10, *) {
                //: UIApplication.shared.open(url!, options: [:], completionHandler: {(success) in})
                UIApplication.shared.open(url!, options: [:], completionHandler: { success in })
                //: } else {
            } else {
                //: UIApplication.shared.openURL(url!)
                UIApplication.shared.openURL(url!)
            }
        }
        //: cancleBtnClick()
        beforeFactory()
    }

    //: func setPermissionIconView(type: TalkingPermissionsType) {
    func promiseBy(type: FrameGraph) {
        //: let count = type == .cameraAndMicrophone ? 2: 1
        let count = type == .cameraAndMicrophone ? 2 : 1
        //: for index in 0..<count {
        for index in 0 ..< count {
            //: let icon = UIImageView()
            let icon = UIImageView()
            //: icon.contentMode = .scaleAspectFill
            icon.contentMode = .scaleAspectFill
            //: backView.addSubview(icon)
            backView.addSubview(icon)
            //: if count == 1 {
            if count == 1 {
                //: icon.snp.makeConstraints { make in
                icon.snp.makeConstraints { make in
                    //: make.centerX.equalToSuperview()
                    make.centerX.equalToSuperview()
                    //: make.size.equalTo(62)
                    make.size.equalTo(62)
                    //: make.top.equalTo(messageLB.snp.bottom).offset(20)
                    make.top.equalTo(messageLB.snp.bottom).offset(20)
                }
                //: } else {
            } else {
                //: if index == 0 {
                if index == 0 {
                    //: icon.snp.makeConstraints { make in
                    icon.snp.makeConstraints { make in
                        //: make.leading.equalTo(61)
                        make.leading.equalTo(61)
                        //: make.size.equalTo(62)
                        make.size.equalTo(62)
                        //: make.top.equalTo(messageLB.snp.bottom).offset(20)
                        make.top.equalTo(messageLB.snp.bottom).offset(20)
                    }
                    //: } else {
                } else {
                    //: icon.snp.makeConstraints { make in
                    icon.snp.makeConstraints { make in
                        //: make.trailing.equalTo(-61)
                        make.trailing.equalTo(-61)
                        //: make.size.equalTo(62)
                        make.size.equalTo(62)
                        //: make.top.equalTo(messageLB.snp.bottom).offset(20)
                        make.top.equalTo(messageLB.snp.bottom).offset(20)
                    }
                }
            }
            //: switch type {
            switch type {
            //: case .camera:
            case .camera:
                //: icon.image = UIImage.BundleImageNamed(name: "icon_peimisionlic_camera")
                icon.image = UIImage.transferAndJoinSmall(name: (String(constTimeCrystalName) + notiAvailablePetValue.replacingOccurrences(of: "department", with: "pe") + "ision" + String(mainThroughUrl.suffix(6)) + "mera"))
            //: case .microphone:
            case .microphone:
                //: icon.image = UIImage.BundleImageNamed(name: "icon_peimissionlic_mic")
                icon.image = UIImage.transferAndJoinSmall(name: (String(mainServiceMsg.suffix(6)) + "eimi" + String(showExceptStr) + String(noti_contextChangeTitle)))
            //: case .cameraAndMicrophone:
            case .cameraAndMicrophone:
                //: if index == 0 {
                if index == 0 {
                    //: icon.image = UIImage.BundleImageNamed(name: "icon_peimisionlic_camera")
                    icon.image = UIImage.transferAndJoinSmall(name: (String(constTimeCrystalName) + notiAvailablePetValue.replacingOccurrences(of: "department", with: "pe") + "ision" + String(mainThroughUrl.suffix(6)) + "mera"))
                    //: } else {
                } else {
                    //: icon.image = UIImage.BundleImageNamed(name: "icon_peimissionlic_mic")
                    icon.image = UIImage.transferAndJoinSmall(name: (String(mainServiceMsg.suffix(6)) + "eimi" + String(showExceptStr) + String(noti_contextChangeTitle)))
                }
            //: default:
            default:
                //: break
                break
            }
        }
    }
}

//: extension TalkingPermissionView {
extension YellowView {
    // 添加视图
    //: private func setupSubviews() {
    private func micEnable() {
        //: self.addSubview(backView)
        self.addSubview(backView)
        //: backView.addSubview(titleLB)
        backView.addSubview(titleLB)
        //: backView.addSubview(messageLB)
        backView.addSubview(messageLB)
        //: backView.addSubview(cancleBtn)
        backView.addSubview(cancleBtn)
        //: backView.addSubview(openBtn)
        backView.addSubview(openBtn)
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func notBridge() {
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.width.equalTo(295)
            make.width.equalTo(295)
            //: make.height.equalTo(321)
            make.height.equalTo(321)
        }
        //: titleLB.snp.makeConstraints { make in
        titleLB.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(32)
            make.top.equalTo(32)
            //: make.height.equalTo(22)
            make.height.equalTo(22)
        }
        //: messageLB.snp.makeConstraints { make in
        messageLB.snp.makeConstraints { make in
            //: make.top.equalTo(titleLB.snp.bottom).offset(21)
            make.top.equalTo(titleLB.snp.bottom).offset(21)
            //: make.leading.equalTo(20)
            make.leading.equalTo(20)
            //: make.trailing.equalTo(-20)
            make.trailing.equalTo(-20)
        }
        //: cancleBtn.snp.makeConstraints { make in
        cancleBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(12)
            make.leading.equalTo(12)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
            //: make.width.equalTo(130)
            make.width.equalTo(130)
        }
        //: openBtn.snp.makeConstraints { make in
        openBtn.snp.makeConstraints { make in
            //: make.trailing.equalTo(-12)
            make.trailing.equalTo(-12)
            //: make.bottom.size.equalTo(cancleBtn)
            make.bottom.size.equalTo(cancleBtn)
        }
    }
}
