
//: Declare String Begin

/*: "http://" :*/
fileprivate let app_awakeId:[Character] = ["h","t"]
fileprivate let user_allowString:String = "he impression labeltp://"

/*: "https://" :*/
fileprivate let noti_brushKey:[Character] = ["h","t","t","p","s",":","/"]
fileprivate let showBeyondMessage:String = "/"

/*: "http:// :*/
fileprivate let notiNetLaunchString:[Character] = ["h","t","t","p",":","/"]
fileprivate let const_feeTitle:String = "tab"

/*: "serverSelctIndex =  :*/
fileprivate let userCharacteristicCanvasMessage:String = "himerve"
fileprivate let showSignificantBotPath:String = "dex = host valid feature"

/*: "serverUrlStr =  :*/
fileprivate let k_feeDateGreatKey:String = "SERV"
fileprivate let app_subName:String = "r = above boy"

/*: "app. :*/
fileprivate let data_coverString:String = "app.failure not occur"

/*: .com" :*/
fileprivate let appCoreValue:String = ".comgive distinction"

/*: "mf/chatRoom/sendGift" :*/
fileprivate let const_illegalKey:String = "mf/ctype media within actor"
fileprivate let userDrawerKey:String = "om/senbasic lat"

/*: "recharge/applePayNotify" :*/
fileprivate let data_successData:String = "ad"
fileprivate let main_numbId:[Character] = ["e","c","h","a","r","g","e","/","a","p","p","l","e","P","a","y","N","o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let main_fileMsg:String = "uassociateer"
fileprivate let dataConditionPath:[Character] = ["e"]
fileprivate let constDownUrl:String = "following disappearLogin"

/*: "IM/getMyBlackList" :*/
fileprivate let notiMiniScenarioId:String = "IM/gethousing fresh gold"
fileprivate let notiAutomaticallyMsg:[Character] = ["k","L","i","s","t"]

/*: "mf/business/sendMsg" :*/
fileprivate let user_successMeetId:String = "wait welcomemf/bus"
fileprivate let data_centerKey:String = "s/sendMsathletic orientation limited environment refuse"
fileprivate let k_lawmakerUsName:[Character] = ["g"]

/*: "mf/business/sendGift" :*/
fileprivate let show_areaOccurValue:String = "mf/buseducator framework failure explain"
fileprivate let const_loanMessage:String = "interrupt origin typical trade/sendGi"
fileprivate let app_volumeKey:String = "middlet"

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let user_likeRandomProvideMessage:String = "mf/chdetailed hide"
fileprivate let constCurrentlyStr:String = "m/senask shall voice obtain float"

/*: "GJ21FB" :*/
fileprivate let main_besideString:String = "GJ21runB"

/*: "完成了检测,请求状态正常" :*/
fileprivate let notiRoundingFormat:String = "choose"
fileprivate let notiEditId:String = "shot检测,"

/*: "接口请求失败" :*/
fileprivate let mainTipId:[Character] = ["接","口","请","求","\u{5931}","败"]

/*: "完成了检测,需要更换server url" :*/
fileprivate let mainInmateData:[Character] = ["\u{5b8c}","\u{6210}","了","\u{68c0}","测",",","需","\u{8981}","更","换","s","e","r","v","e"]
fileprivate let notiVariableTitle:String = "r urlcontact fit sort crucial"

/*: "HTTPProxy" :*/
fileprivate let kGalleryProjectPath:String = "h"
fileprivate let app_ridTitle:[Character] = ["T","T","P","P","r","o","x","y"]

/*: "__SCOPED__" :*/
fileprivate let mainDrawingTopMessage:[Character] = ["_","_","S","C"]
fileprivate let data_ovalMessage:[Character] = ["O","P","E","D","_","_"]

/*: "tap" :*/
fileprivate let main_interestStr:String = "ttriggerp"

/*: "tun" :*/
fileprivate let constLoseMsg:String = "tufun"

/*: "ipsec" :*/
fileprivate let const_oddUrl:String = "ipmaintainec"

/*: "ppp" :*/
fileprivate let show_increaseId:[Character] = ["p","p","p"]

/*: "func__updateTalkingCookies =  :*/
fileprivate let dataAutomaticallyTitle:String = "func__illegal final dilemma"
fileprivate let appComponentValue:String = "ALKING"
fileprivate let userBetweenSmallFormat:String = "big picture standard ups = "

/*: "https:// :*/
fileprivate let user_equivalentSpaceName:[Character] = ["h","t","t","p","s",":"]
fileprivate let userObtainOfferValue:[Character] = ["/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let mainWayEffectData:[UInt8] = [0x20,0x3d,0x20,0x6c,0x72,0x55,0x68,0x74,0x69,0x57,0x73,0x65,0x69,0x6b,0x6f,0x6f,0x43,0x65,0x6d,0x61,0x53,0x65,0x74,0x65,0x6c,0x65,0x64,0x5f,0x5f,0x63,0x6e,0x75,0x66]

/*: "UID" :*/
fileprivate let notiRoundCanData:[Character] = ["U","I","D"]

/*: "PHPSESSID" :*/
fileprivate let showEventString:[Character] = ["P","H","P","S","E","S","S"]
fileprivate let main_skipLockFormat:[Character] = ["I","D"]

/*: "func__saveAllCookies =  :*/
fileprivate let mainShPath:String = "fuorientation"
fileprivate let app_laterString:String = "AllCaccept require interested"
fileprivate let appLoadPath:String = " "

/*: "func__saveAllCookies  error" :*/
fileprivate let app_coreValue:[Character] = ["f","u","n","c","_","_","s","a","v","e","A","l","l","C","o","o","k","i","e","s"," "," ","e","r","r","o"]
fileprivate let appDoingStr:[Character] = ["r"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ImageClear.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum ColorSophisticated: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class ImageClear: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = ImageClear()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        largeDown()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(app_awakeId) + String(user_allowString.suffix(5)))) || newValue.hasPrefix((String(noti_brushKey) + showBeyondMessage.capitalized)) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if main_byPhoneValue && !mainAwayQuantityensityFormat {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(notiNetLaunchString) + const_feeTitle.replacingOccurrences(of: "tab", with: "/")) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(notiNetLaunchString) + const_feeTitle.replacingOccurrences(of: "tab", with: "/")) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    hideBag(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func largeDown() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = app_pressureId.integer(forKey: appGraphSubtleString)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (userCharacteristicCanvasMessage.replacingOccurrences(of: "him", with: "s") + "rSelctIn" + String(showSignificantBotPath.prefix(6))) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = nearPair()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(app_awakeId) + String(user_allowString.suffix(5)))
        //: if Environment_Formal && !Encryption_Request {
        if main_byPhoneValue && !mainAwayQuantityensityFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(app_awakeId) + String(user_allowString.suffix(5)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (k_feeDateGreatKey.lowercased() + "erUrlSt" + String(app_subName.prefix(4))) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func nearPair() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func memberField(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(data_coverString.prefix(4))) + "\(show_whiteTitle)" + (String(appCoreValue.prefix(4))), (String(data_coverString.prefix(4))) + "\(show_whiteTitle)" + (String(appCoreValue.prefix(4)))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(const_illegalKey.prefix(4)) + "hatRo" + String(userDrawerKey.prefix(6)) + "dGift"),
         //: "recharge/applePayNotify",
         (data_successData.replacingOccurrences(of: "ad", with: "r") + String(main_numbId)),
         //: "user/appleLogin"] + slowPathArr1
         (main_fileMsg.replacingOccurrences(of: "associate", with: "s") + "/appl" + String(dataConditionPath) + String(constDownUrl.suffix(5)))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(notiMiniScenarioId.prefix(6)) + "MyBlac" + String(notiAutomaticallyMsg)),
         //: "mf/business/sendMsg",
         (String(user_successMeetId.suffix(6)) + "ines" + String(data_centerKey.prefix(8)) + String(k_lawmakerUsName)),
         //: "mf/business/sendGift",
         (String(show_areaOccurValue.prefix(6)) + "iness" + String(const_loanMessage.suffix(7)) + app_volumeKey.replacingOccurrences(of: "middle", with: "f")),
         //: "mf/chatRoom/sendMsg"]
         (String(user_likeRandomProvideMessage.prefix(5)) + "atRoo" + String(constCurrentlyStr.prefix(5)) + "dMsg")]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = MacroDefine.getSystemLangCode()
        let lang = VersusQuit.darkFactor()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .instanceAcross(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (main_besideString.replacingOccurrences(of: "run", with: "F")) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension ImageClear {
    //: func checkServerUrl() {
    func alongsideRemove() {
        //: if QuantityeractionDelay.share.networkStatus != .Unavailable {
        if QuantityeractionDelay.share.networkStatus != .Unavailable {
            //: func_requestHost()
            pauseCan()
            //: } else {
        } else {
            //: func_starCodeTime()
            fixed()
        }
    }

    //: func func_starCodeTime() {
    func fixed() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] num in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.pauseCan()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func libraryBeside() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func pauseCan() {
        //: if QuantityeractionDelay.share.networkStatus == .Unavailable {
        if QuantityeractionDelay.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        libraryBeside()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        ChannelRange.theTo { [weak self] succeed, result, errorModel in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (notiRoundingFormat.replacingOccurrences(of: "choose", with: "完") + notiEditId.replacingOccurrences(of: "shot", with: "成了") + "请\u{6c42}状态正常"))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (String(mainTipId)))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.fixed()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(mainInmateData) + String(notiVariableTitle.prefix(5))))
                    //: self.func__changeHostUrl()
                    self.splitDestroy()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func splitDestroy() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        libraryBeside()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        app_pressureId.set(serverSelctIndex, forKey: appGraphSubtleString)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(app_awakeId) + String(user_allowString.suffix(5)))
        //: if Environment_Formal && !Encryption_Request {
        if main_byPhoneValue, !mainAwayQuantityensityFormat {
            //: serverPrefix = "http://"
            serverPrefix = (String(app_awakeId) + String(user_allowString.suffix(5)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension ImageClear {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func productImage() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(kGalleryProjectPath.uppercased() + String(app_ridTitle))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func originToDistinctive() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(mainDrawingTopMessage) + String(data_ovalMessage))] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((main_interestStr.replacingOccurrences(of: "trigger", with: "a"))) || keyStr.contains((constLoseMsg.replacingOccurrences(of: "fun", with: "n"))) || keyStr.contains((const_oddUrl.replacingOccurrences(of: "maintain", with: "s"))) || keyStr.contains((String(show_increaseId))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension ImageClear {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func hideBag(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(dataAutomaticallyTitle.prefix(6)) + "updateT" + appComponentValue.lowercased() + "Cookie" + String(userBetweenSmallFormat.suffix(4))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = needOriginal()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
        var urlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(app_awakeId) + String(user_allowString.suffix(5))), with: (String(noti_brushKey) + showBeyondMessage.capitalized))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((String(noti_brushKey) + showBeyondMessage.capitalized)) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(user_equivalentSpaceName) + String(userObtainOfferValue)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            educator(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        notRating()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func educator(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: mainWayEffectData.reversed(), encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func needOriginal() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(app_awakeId) + String(user_allowString.suffix(5)))
        //: if Environment_Formal && !Encryption_Request {
        if main_byPhoneValue, !mainAwayQuantityensityFormat {
            //: serverPrefix = "https://"
            serverPrefix = (String(noti_brushKey) + showBeyondMessage.capitalized)
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(data_coverString.prefix(4))) + "\(show_whiteTitle)" + (String(appCoreValue.prefix(4))))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func stepHome(note: Notification) {
        //: if QuantityeractionDelay.share.loginUid.isEmptyString {
        if QuantityeractionDelay.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            tapConversation()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            despiteAnalysis()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func tapConversation() {
        //: var htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
        var htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(notiRoundCanData)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(notiRoundCanData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(showEventString) + String(main_skipLockFormat)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(showEventString) + String(main_skipLockFormat))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(notiRoundCanData)) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(String(notiRoundCanData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(showEventString) + String(main_skipLockFormat)) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(String(showEventString) + String(main_skipLockFormat))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((String(notiRoundCanData))), h5Cookie.keys.contains((String(notiRoundCanData))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(String(notiRoundCanData))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(String(notiRoundCanData))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((String(showEventString) + String(main_skipLockFormat))), h5Cookie.keys.contains((String(showEventString) + String(main_skipLockFormat))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(String(showEventString) + String(main_skipLockFormat))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(String(showEventString) + String(main_skipLockFormat))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func despiteAnalysis() {
        //: let htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
        let htmlDomain: String = QuantityeractionDelay.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == QuantityeractionDelay.share.loginUserMode.userID {
            if cookie.name == (String(notiRoundCanData)), cookie.value == QuantityeractionDelay.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (String(showEventString) + String(main_skipLockFormat)) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (String(notiRoundCanData)) {
                        //: appCookie["UID"] = cookie
                        appCookie[(String(notiRoundCanData))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (String(showEventString) + String(main_skipLockFormat)) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(String(showEventString) + String(main_skipLockFormat))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((String(notiRoundCanData))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(notiRoundCanData))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((String(showEventString) + String(main_skipLockFormat))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(String(showEventString) + String(main_skipLockFormat))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func notRating() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (mainShPath.replacingOccurrences(of: "orientation", with: "nc") + "__save" + String(app_laterString.prefix(4)) + "ookies =" + appLoadPath.capitalized) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            app_pressureId.set(cookieData, forKey: const_mendTextureStr)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(app_coreValue) + String(appDoingStr)))
        }
    }

    //: func func__setAppCookies() {
    func rankMerge() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = app_pressureId.object(forKey: const_mendTextureStr)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func pictureConstraint() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        app_pressureId.removeObject(forKey: const_mendTextureStr)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
