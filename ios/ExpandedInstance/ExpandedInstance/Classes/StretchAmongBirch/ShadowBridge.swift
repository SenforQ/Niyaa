
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let appHalfClearlyMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "Cancel" :*/
fileprivate let user_pinionValue:String = "Cancelinterrupt large"

/*: "Submit" :*/
fileprivate let constHeroString:String = "Submivisual opportunity offer away"
fileprivate let noti_businessTitle:String = "option"

/*: "Please tell us your problems                   " :*/
fileprivate let notiAdditionId:[UInt8] = [0x10,0x2c,0x25,0x21,0x33,0x25,0x60,0x34,0x25,0x2c,0x2c,0x60,0x35,0x33,0x60,0x39,0x2f,0x35,0x32,0x60,0x30,0x32,0x2f,0x22,0x2c,0x25,0x2d,0x33,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60,0x60]

private func loopAnswer(size num: UInt8) -> UInt8 {
    return num ^ 64
}

/*: "You need to choose at least one." :*/
fileprivate let data_noticeKey:[UInt8] = [0xb5,0x83,0x99,0xcc,0x82,0x89,0x89,0x88,0xcc,0x98,0x83,0xcc,0x8f,0x84,0x83,0x83,0x9f,0x89,0xcc,0x8d,0x98,0xcc,0x80,0x89,0x8d,0x9f,0x98,0xcc,0x83,0x82,0x89,0xc2]

private func readingAdjustment(equivalent num: UInt8) -> UInt8 {
    return num ^ 236
}

/*: "Please tell us your problems." :*/
fileprivate let const_publishMessage:String = "extensive tar residentPleas"
fileprivate let app_removeName:String = "retain lose near mend comply us yo"
fileprivate let kStrokeUrl:String = "domain radioroblems."

/*: "app/userReport" :*/
fileprivate let show_angleMsg:String = "quantity capture tab logapp/"
fileprivate let main_feedbackString:[Character] = ["o","r","t"]

/*: "reason" :*/
fileprivate let userExerciseVisibleUrl:String = "recalledon"

/*: "desc" :*/
fileprivate let data_zoneId:[UInt8] = [0xa7,0xa6,0xb0,0xa0]

private func manFatal(launch num: UInt8) -> UInt8 {
    return num ^ 195
}

/*: "mf/moment/report" :*/
fileprivate let mainAffectPhaseUrl:String = "entry access pressuremf/mo"
fileprivate let userBecomeParentName:String = "rlabelpor"
fileprivate let constStrokeLoopUrl:String = "time"

/*: "momentId" :*/
fileprivate let k_exclusiveSucceedMessage:[UInt8] = [0xe,0x10,0xe,0x6,0xf,0x15,0xea,0x5]

fileprivate func styleUs(athletic num: UInt8) -> UInt8 {
    let value = Int(num) - 161
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/momentReply/report" :*/
fileprivate let notiDirectlyPriorityName:String = "mf/moassist global appeal remove"
fileprivate let dataGrandfatherJuniorData:String = "Replywhite category related new"

/*: "app/videoReport" :*/
fileprivate let kCharacterizePath:String = "app/every job category biology recommend"
fileprivate let main_cycleStr:String = "epous"

/*: "logId" :*/
fileprivate let noti_findFinishPath:[UInt8] = [0x96,0x99,0x91,0x73,0x8e]

fileprivate func bounceScreen(official num: UInt8) -> UInt8 {
    let value = Int(num) + 214
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Submitted successfully" :*/
fileprivate let constSineTitle:String = "Submittexture great info expression"
fileprivate let mainBucketString:String = "diskessf"
fileprivate let notiThenConnectUrl:[Character] = ["u","l","l","y"]

/*: "Nudity" :*/
fileprivate let mainUnknownMsg:String = "staff"
fileprivate let dataHighlightFlagMessage:String = "udiadvancedy"

/*: "Violence" :*/
fileprivate let const_volumeNetMsg:[Character] = ["V","i","o","l","e","n","c"]
fileprivate let noti_clickImageKey:[Character] = ["e"]

/*: "Harassment" :*/
fileprivate let k_genderTurnUrl:[Character] = ["H","a","r","a","s","s","m"]
fileprivate let constStrategyString:[Character] = ["e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let mainSubtleId:[Character] = ["S","u","i","c","i","d","e"," ","o","r"," ","S","e","l","f","-","i","n","j","u","r","y"]

/*: "False Information" :*/
fileprivate let noti_criticalStr:[Character] = ["F","a","l","s","e"," ","I","n","f"]
fileprivate let kReportString:String = "ORMATION"

/*: "Spam" :*/
fileprivate let show_listUrl:String = "identity toSpam"

/*: "Hate Speech" :*/
fileprivate let kDropTailFormat:String = "capacity include effHate S"

/*: "Terrorism" :*/
fileprivate let mainObserverAheadValue:[Character] = ["T","e","r","r","o","r"]
fileprivate let show_voiceKey:[Character] = ["i","s","m"]

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let data_musicStr:[UInt8] = [0x8a,0xb8,0xb5,0xbc,0xf9,0xb6,0xbf,0xf9,0x90,0xb5,0xb5,0xbc,0xbe,0xb8,0xb5,0xf9,0xb6,0xab,0xf9,0x8b,0xbc,0xbe,0xac,0xb5,0xb8,0xad,0xbc,0xbd,0xf9,0x9e,0xb6,0xb6,0xbd,0xaa]

private func paraOldSpan(cell num: UInt8) -> UInt8 {
    return num ^ 217
}

/*: "Sending disgusting messages" :*/
fileprivate let data_permissionSlowData:String = "auto dismiss previous disappearSend"
fileprivate let user_againstPath:String = "SGUST"
fileprivate let appEvaluationPath:String = "meaccountaccounta"
fileprivate let appPurpleRelatedValue:[Character] = ["g","e","s"]

/*: "Using photos of nudity or drugs" :*/
fileprivate let mainSayValue:[UInt8] = [0xfb,0x19,0xf,0x14,0xd,0xc6,0x16,0xe,0x15,0x1a,0x15,0x19,0xc6,0x15,0xc,0xc6,0x14,0x1b,0xa,0xf,0x1a,0x1f,0xc6,0x15,0x18,0xc6,0xa,0x18,0x1b,0xd,0x19]

fileprivate func whiteTarget(channel num: UInt8) -> UInt8 {
    let value = Int(num) + 90
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Harassing or sending ads" :*/
fileprivate let const_domainMainString:String = "Harassmove projection"
fileprivate let notiWhichScheduleMessage:String = "r sespell early exactly"
fileprivate let k_totalName:String = "aappearance"

/*: "Defraud or using fake info" :*/
fileprivate let const_roundMessage:[Character] = ["D","e"]
fileprivate let mainEfficiencyStr:String = "fraud reduction reduce"
fileprivate let user_mutterPath:String = "ing fafurther nobody fatal"

/*: "Others" :*/
fileprivate let constNoteMessage:String = "family private music saltOther"
fileprivate let const_perName:String = "span"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ShadowBridge.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum ObtainPhone: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class ShadowBridge: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: ObtainPhone?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [EditDivide] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = EditDivide()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: ObtainPhone?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        amItem()
        //: setupSubViewsConstraint()
        dimension()
        //: setupReasons()
        averageDiscover()
        //: bindInteraction()
        instructionFor()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: appHalfClearlyMsg.reversed(), encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((String(user_pinionValue.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.multiSince(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.alongsideOk(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.multiSince().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(current), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(constHeroString.prefix(5)) + noti_businessTitle.replacingOccurrences(of: "option", with: "t")).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.alongsideOk(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.doEmbrace(colors: UIColor.monthForMomentum(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(calculate), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: dataTabUrl - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.robotBy(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = OfFlush(placeholder: String(bytes: notiAdditionId.map{loopAnswer(size: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.underCountensityLanguage(), frame: CGRect(x: 10, y: 10, width: dataTabUrl - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .alongsideOk(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.thinOf()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .alongsideOk(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.robotBy(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(DuringLoop.self, forCellReuseIdentifier: DuringLoop.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension ShadowBridge {
    //: @objc func func__TapGestureRecognizer() {
    @objc func despiteInstance() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func analyze(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = MacroDefine.getWindow()
            let window = VersusQuit.further()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func choice(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func current() {
        //: self.dismissReportView(succeed: false)
        self.choice(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func calculate() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.paraDown(showMsg: String(bytes: data_noticeKey.map{readingAdjustment(equivalent: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.paraDown(showMsg: (String(const_publishMessage.suffix(5)) + "e tell" + String(app_removeName.suffix(6)) + "ur p" + String(kStrokeUrl.suffix(8))).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(show_angleMsg.suffix(4)) + "userRep" + String(main_feedbackString))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (userExerciseVisibleUrl.replacingOccurrences(of: "called", with: "as")): selectedModel.type, String(bytes: data_zoneId.map{manFatal(launch: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(mainAffectPhaseUrl.suffix(5)) + "ment/" + userBecomeParentName.replacingOccurrences(of: "label", with: "e") + constStrokeLoopUrl.replacingOccurrences(of: "time", with: "t"))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: k_exclusiveSucceedMessage.map{styleUs(athletic: $0)}, encoding: .utf8)!: tempId, (userExerciseVisibleUrl.replacingOccurrences(of: "called", with: "as")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(notiDirectlyPriorityName.prefix(5)) + "ment" + String(dataGrandfatherJuniorData.prefix(5)) + "/report")
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (userExerciseVisibleUrl.replacingOccurrences(of: "called", with: "as")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(kCharacterizePath.prefix(4)) + "videoR" + main_cycleStr.replacingOccurrences(of: "us", with: "rt"))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: noti_findFinishPath.map{bounceScreen(official: $0)}, encoding: .utf8)!: tempId, (userExerciseVisibleUrl.replacingOccurrences(of: "called", with: "as")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        ShouldBridge.connectTwo(reqPath: path, reqParams: param) { succeed, result, errorModel in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.outsideFeature(showMsg: (String(constSineTitle.prefix(6)) + "ted su" + mainBucketString.replacingOccurrences(of: "disk", with: "cc") + String(notiThenConnectUrl)).localized)
                //: self.dismissReportView(succeed: true)
                self.choice(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension ShadowBridge: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in tableView: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: DuringLoop.className()) as! DuringLoop
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.lightForSkipCur(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension ShadowBridge {
    /// 添加视图
    //: private func setupSubviews() {
    private func amItem() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func dimension() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func averageDiscover() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(mainUnknownMsg.replacingOccurrences(of: "staff", with: "N") + dataHighlightFlagMessage.replacingOccurrences(of: "advanced", with: "t")).localized,
                           //: "Violence".localized,
                           (String(const_volumeNetMsg) + String(noti_clickImageKey)).localized,
                           //: "Harassment".localized,
                           (String(k_genderTurnUrl) + String(constStrategyString)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(mainSubtleId)).localized,
                           //: "False Information".localized,
                           (String(noti_criticalStr) + kReportString.lowercased()).localized,
                           //: "Spam".localized,
                           (String(show_listUrl.suffix(4))).localized,
                           //: "Hate Speech".localized,
                           (String(kDropTailFormat.suffix(6)) + "peech").localized,
                           //: "Terrorism".localized,
                           (String(mainObserverAheadValue) + String(show_voiceKey)).localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: data_musicStr.map{paraOldSpan(cell: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(data_permissionSlowData.suffix(4)) + "ing di" + user_againstPath.lowercased() + "ing " + appEvaluationPath.replacingOccurrences(of: "account", with: "s") + String(appPurpleRelatedValue)).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: mainSayValue.map{whiteTarget(channel: $0)}, encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(const_domainMainString.prefix(6)) + "ing o" + String(notiWhichScheduleMessage.prefix(4)) + "nding " + k_totalName.replacingOccurrences(of: "appearance", with: "ds")).localized,
                           //: "Defraud or using fake info".localized,
                           (String(const_roundMessage) + String(mainEfficiencyStr.prefix(6)) + "or us" + String(user_mutterPath.prefix(6)) + "ke info").localized,
                           //: "Others".localized]
                           (String(constNoteMessage.suffix(5)) + const_perName.replacingOccurrences(of: "span", with: "s")).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = QuantityeractionDelay.share.appConfigMode.videoReport["reason"] {
            if let reason = QuantityeractionDelay.share.appConfigMode.videoReport[(userExerciseVisibleUrl.replacingOccurrences(of: "called", with: "as"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = EditDivide()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func instructionFor() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] recognizer in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
