
//: Declare String Begin

/*: "msgType" :*/
fileprivate let noti_dayEnoughMsg:String = "msgTypeschedule due"

/*: "tips" :*/
fileprivate let constOndPath:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let dataMapProcessorValue:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let appTipMsg:[Character] = ["m","s","g","I","n"]
fileprivate let main_insideData:String = "mm"

/*: "user" :*/
fileprivate let show_alongsideData:String = "usprotection"

/*: "uid" :*/
fileprivate let main_domainHungTitle:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let show_carrierName:[UInt8] = [0x98,0x93,0x8d,0x95,0x98,0x8b,0x97,0x8f]

fileprivate func errorBeauty(nous num: UInt8) -> UInt8 {
    let value = Int(num) - 42
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "headPic" :*/
fileprivate let main_builderClearMessage:[UInt8] = [0x63,0x69,0x50,0x64,0x61,0x65,0x68]

/*: "age" :*/
fileprivate let const_architectureId:[UInt8] = [0x29,0x2f,0x2d]

fileprivate func intervalJob(like num: UInt8) -> UInt8 {
    let value = Int(num) + 56
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift" :*/
fileprivate let show_securityFormat:[Character] = ["g","i","f","t"]

/*: "isGroup" :*/
fileprivate let noti_quitPath:[Character] = ["i","s","G","r","o","u","p"]

/*: "WantGift" :*/
fileprivate let appEraseMutualUrl:String = "primary auto sun tailWant"

/*: "txt" :*/
fileprivate let constNormString:String = "pathxpath"

/*: "img" :*/
fileprivate let notiReductionMsg:String = "IMG"

/*: "video" :*/
fileprivate let appDayUrl:String = "caredeo"

/*: "audio" :*/
fileprivate let data_particleData:String = "auadditionio"

/*: "messageType" :*/
fileprivate let kJustHeapUrl:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "contentType" :*/
fileprivate let user_speedPath:String = "conavoidenavoid"
fileprivate let k_accelerateMsg:[Character] = ["T","y","p","e"]

/*: "ImgMsg" :*/
fileprivate let show_fillShadeData:String = "ImgMsgundertake oval"

/*: "imageUri" :*/
fileprivate let main_toInjuryTitle:String = "businessmage"
fileprivate let constEqualWithData:String = "Uridemonstrate automatically future nature spend"

/*: "type" :*/
fileprivate let userFoundId:[Character] = ["t","y","p","e"]

/*: "pic" :*/
fileprivate let userTrainTitle:[Character] = ["p","i","c"]

/*: "isBurn" :*/
fileprivate let mainRectoData:[Character] = ["i","s","B","u","r","n"]

/*: "giftId" :*/
fileprivate let notiViewData:String = "invitation sustainablegiftI"
fileprivate let userSubjectKey:String = "ago"

/*: "params" :*/
fileprivate let show_tipMessage:String = "parammodify"

/*: "audioLength" :*/
fileprivate let userFaceTitle:String = "ago context mount situation wordaudioL"

/*: "len" :*/
fileprivate let main_provideAccountString:String = "LEN"

/*: "audioSandbox" :*/
fileprivate let dataSpringUrl:String = "audigallery"
fileprivate let main_farmerName:String = "before"

/*: "senderId" :*/
fileprivate let notiDealDetectString:[Character] = ["s","e","n","d","e","r","I","d"]

/*: "AudioMsg" :*/
fileprivate let noti_warningString:[Character] = ["A","u","d","i","o","M","s","g"]

/*: "content" :*/
fileprivate let noti_selectString:String = "conranke"
fileprivate let mainStatId:String = "nvid"

/*: "TxtMsg" :*/
fileprivate let notiPathStr:String = "sub immediate hung medal categoryTxtMsg"

/*: "message" :*/
fileprivate let userSaveUrl:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let app_randomToolString:String = "quobutterflye"
fileprivate let constJudgeTitle:[Character] = ["M","s","g","I","d"]

/*: "quoteMsgInfo" :*/
fileprivate let mainThatKey:String = "quoteMcountry valid ale to database"

/*: "replayId" :*/
fileprivate let notiLeyStr:String = "hepla"
fileprivate let showRankString:[Character] = ["y","I","d"]

/*: "rYMsgType" :*/
fileprivate let k_fieldStr:[Character] = ["r","Y","M","s","g","T","y","p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let userLostString:String = "enemy cookieGJ:Sess"
fileprivate let kRecommendStr:String = ":"
fileprivate let noti_hungUrl:String = "AutoGcommit warn similar"
fileprivate let userAgainRespectiveId:String = "others ale brick listen core:TxtCu"

/*: "changeDirection" :*/
fileprivate let main_fatalBagMsg:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t","i"]
fileprivate let show_invitationData:String = "oaway"

/*: "system" :*/
fileprivate let kConvertStr:String = "SYSTEM"

/*: "title" :*/
fileprivate let notiDilemmaId:String = "titlreply"

/*: "icon" :*/
fileprivate let show_exFormat:[Character] = ["i","c","o","n"]

/*: "all" :*/
fileprivate let main_randomId:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let constWritingFormat:String = "ICON"
fileprivate let k_projectionKey:String = "terms absolute one boxs_pcr"

/*: "status" :*/
fileprivate let dataCompleteSaveValue:String = "stfunctionaltus"

/*: "msgIncome" :*/
fileprivate let show_memoryFormat:String = "msgIncomemethod carrier maximum full condition"

/*: "msgId" :*/
fileprivate let app_evolutionTitle:String = "msgIdcigar retain complaint present"

/*: "SinkTap" :*/
fileprivate let userHeavyStr:String = "pass quit field maintainTMsgTran"
fileprivate let notiVisualName:[Character] = ["s","l","a","t","e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let data_lostUrl:String = "TMsgTprivacy split connect down scope"
fileprivate let notiAvailableMessage:[Character] = ["r","a","n","s","l","a","t","e","d","C","o","n","t","e","n","t"]

/*: "formatType" :*/
fileprivate let const_maleTitle:[Character] = ["f","o","r","m","a"]
fileprivate let user_destinationFlagSecularUrl:[Character] = ["t","T","y","p","e"]

/*: "plain" :*/
fileprivate let data_restoreValue:String = "PLAIN"

/*: "imageData" :*/
fileprivate let constPmThatUrl:String = "removeag"

/*: "VideoMsg" :*/
fileprivate let userBeforeKey:[Character] = ["V","i","d","e","o","M","s","g"]

/*: "match" :*/
fileprivate let user_aId:String = "MATCH"

/*: "userIntro" :*/
fileprivate let noti_honeyWholeMsg:String = "uminimum"
fileprivate let showTransformKey:String = "erIntrodivide quality"

/*: "targetId" :*/
fileprivate let notiListString:[Character] = ["t","a","r","g","e","t","I","d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let app_primaryData:[Character] = ["y","y","y","y","-","M","M","-","d","d"," ","H"]
fileprivate let user_teamMessage:String = "H:mm:ssthrough pattern"

/*: "sendTime" :*/
fileprivate let k_toTitle:[Character] = ["s","e"]
fileprivate let dataSparkTitle:[Character] = ["n","d","T","i","m","e"]

/*: "isRead" :*/
fileprivate let user_protectionSecretString:String = "isReadprovision successfully"

/*: "audioData" :*/
fileprivate let user_entryKey:String = "audioDatload conversion"
fileprivate let notiFragmentTotalName:String = "rapid"

/*: "audioUri" :*/
fileprivate let main_perString:[Character] = ["a","u"]
fileprivate let constRocketString:String = "become pure protocol emptydioUri"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ChainSample.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum StreamAssociate: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - ChainSample

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class ChainSample: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func bringTolerance() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> FlexBridge? {
    public class func underBoundAcross(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> FlexBridge? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = quantity(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: WithinMargin.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            WithinMargin.shared.duringAll(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> FlexBridge? {
    public class func quantity(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> FlexBridge? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(noti_dayEnoughMsg.prefix(7)))] = (String(constOndPath))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(String(dataMapProcessorValue)): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = squareToSpreadForm(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DenseDisplay(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = SceneModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? NeedModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(show_alongsideData.replacingOccurrences(of: "protection", with: "er"))] = [
            //: "uid": QuantityeractionDelay.share.loginUserMode.userID,
            (String(main_domainHungTitle)): QuantityeractionDelay.share.loginUserMode.userID,
            //: "nickname": QuantityeractionDelay.share.loginUserMode.nickname,
            String(bytes: show_carrierName.map{errorBeauty(nous: $0)}, encoding: .utf8)!: QuantityeractionDelay.share.loginUserMode.nickname,
            //: "headPic": QuantityeractionDelay.share.loginUserMode.headPic,
            String(bytes: main_builderClearMessage.reversed(), encoding: .utf8)!: QuantityeractionDelay.share.loginUserMode.headPic,
            //: "age": QuantityeractionDelay.share.loginUserMode.age,
            String(bytes: const_architectureId.map{intervalJob(like: $0)}, encoding: .utf8)!: QuantityeractionDelay.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(String(dataMapProcessorValue)): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = squareToSpreadForm(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (String(show_securityFormat)) {
            //: var data: FlexBridge
            var data: FlexBridge
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_quitPath))] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = DespiteEnhance(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = LayerIndex(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = LayerIndex(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SceneModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (String(appEraseMutualUrl.suffix(4)) + "Gift") {
            //: let data = ContextBind(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = ContextBind(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SceneModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (constNormString.replacingOccurrences(of: "path", with: "t")) {
            //: var data: FlexBridge
            var data: FlexBridge
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(noti_quitPath))] as? String == "1" { // 群聊
                //: data = AlongMount(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = AlongMount(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = OwnerChart(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = OwnerChart(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SceneModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (notiReductionMsg.lowercased()) {
            //: let data = AppearanceAddLock(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = AppearanceAddLock(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SceneModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (appDayUrl.replacingOccurrences(of: "care", with: "vi")) {
            //: let data = EditSelect(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = EditSelect(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SceneModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (data_particleData.replacingOccurrences(of: "addition", with: "d")) {
            //: let data = DensityStartSignificant(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DensityStartSignificant(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SceneModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(String(show_securityFormat))] != nil && (msgInfo[(String(kJustHeapUrl))] as? Int) == 4 {
            //: let data = OwnerChart(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = OwnerChart(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? SceneModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: FlexBridge) -> FlexBridge? {
    public class func dismiss(withData cellData: FlexBridge) -> FlexBridge? {
        // 图片
        //: if let reCellData = cellData as? AppearanceAddLock {
        if let reCellData = cellData as? AppearanceAddLock {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(MacroDefine.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(VersusQuit.heroPrior())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(user_speedPath.replacingOccurrences(of: "avoid", with: "t") + String(k_accelerateMsg))] = (String(show_fillShadeData.prefix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] = (notiReductionMsg.lowercased())
            //: msgInfo["imageUri"] = filePath
            msgInfo[(main_toInjuryTitle.replacingOccurrences(of: "business", with: "i") + String(constEqualWithData.prefix(3)))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(String(userFoundId))] = (String(show_fillShadeData.prefix(6)))
            //: params["pic"] = filePath
            params[(String(userTrainTitle))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(String(mainRectoData))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(String(mainRectoData))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(String(notiViewData.suffix(5)) + userSubjectKey.replacingOccurrences(of: "ago", with: "d"))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(String(notiViewData.suffix(5)) + userSubjectKey.replacingOccurrences(of: "ago", with: "d"))] = "0"
            }

            //: dic["params"] = params
            dic[(show_tipMessage.replacingOccurrences(of: "modify", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = quantity(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? EditSelect {
        if let reCellData = cellData as? EditSelect {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? DensityStartSignificant {
        if let reCellData = cellData as? DensityStartSignificant {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = FinishAssociated.image(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(userFaceTitle.suffix(6)) + "ength")] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(main_provideAccountString.lowercased())] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(dataSpringUrl.replacingOccurrences(of: "gallery", with: "o") + "Sandbo" + main_farmerName.replacingOccurrences(of: "before", with: "x"))] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(dataSpringUrl.replacingOccurrences(of: "gallery", with: "o") + "Sandbo" + main_farmerName.replacingOccurrences(of: "before", with: "x"))] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] = (data_particleData.replacingOccurrences(of: "addition", with: "d"))
            //: msgInfo["senderId"] = QuantityeractionDelay.share.loginUserMode.userID
            msgInfo[(String(notiDealDetectString))] = QuantityeractionDelay.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(String(userFoundId))] = (String(noti_warningString))
            //: dic["params"] = params
            dic[(show_tipMessage.replacingOccurrences(of: "modify", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = quantity(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? OwnerChart {
        if let reCellData = cellData as? OwnerChart {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(noti_selectString.replacingOccurrences(of: "rank", with: "t") + mainStatId.replacingOccurrences(of: "vid", with: "t"))] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(noti_dayEnoughMsg.prefix(7)))] = (constNormString.replacingOccurrences(of: "path", with: "t"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(String(userFoundId))] = (String(notiPathStr.suffix(6)))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(userSaveUrl))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.corner() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(app_randomToolString.replacingOccurrences(of: "butterfly", with: "t") + String(constJudgeTitle))] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(mainThatKey.prefix(6)) + "sgInfo")] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: if let replayId = reCellData.msgModel.params["replayId"] { // 问题回答消息重发
            if let replayId = reCellData.msgModel.params[(notiLeyStr.replacingOccurrences(of: "he", with: "re") + String(showRankString))] { // 问题回答消息重发
                //: params["replayId"] = replayId
                params[(notiLeyStr.replacingOccurrences(of: "he", with: "re") + String(showRankString))] = replayId
            }

            //: dic["params"] = params
            dic[(show_tipMessage.replacingOccurrences(of: "modify", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = quantity(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> FlexBridge? {
    public class func oneSpot(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> FlexBridge? {
        //: var cellData: FlexBridge?
        var cellData: FlexBridge?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(String(dataMapProcessorValue))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(show_alongsideData.replacingOccurrences(of: "protection", with: "er"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? SceneModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == QuantityeractionDelay.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == QuantityeractionDelay.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == QuantityeractionDelay.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == QuantityeractionDelay.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(k_fieldStr))] as? String == (String(userLostString.suffix(7)) + kRecommendStr.capitalized + String(noti_hungUrl.prefix(5)) + "reet" + String(userAgainRespectiveId.suffix(6)) + "stom") {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(String(main_fatalBagMsg) + show_invitationData.replacingOccurrences(of: "away", with: "n"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = styleBy(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (kConvertStr.lowercased()) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = SceneModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? NeedModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DenseDisplay(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(noti_selectString.replacingOccurrences(of: "rank", with: "t") + mainStatId.replacingOccurrences(of: "vid", with: "t"))] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = AppearanceAddLock(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = AppearanceAddLock(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = EditSelect(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EditSelect(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = DespiteEnhance(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = LayerIndex(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = LayerIndex(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = giftTo(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(String(dataMapProcessorValue))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? WithoutModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = ContextBind(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ContextBind(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = DensityStartSignificant(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DensityStartSignificant(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? DensityStartSignificant {
            if let audioCellData = cellData as? DensityStartSignificant {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = discoverUniform(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = PanMarker.clearWrite(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = PanMarker.deployBottom(tweenGreet(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = ConvertSubtle(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(notiDilemmaId.replacingOccurrences(of: "reply", with: "e"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(noti_selectString.replacingOccurrences(of: "rank", with: "t") + mainStatId.replacingOccurrences(of: "vid", with: "t"))] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(notiReductionMsg.lowercased())] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ExerciseMinimumWant(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = AlongMount(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = AlongMount(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = OwnerChart(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = OwnerChart(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? EndModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(String(show_exFormat))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: QuantityeractionDelay.share.loginUserMode.headPic ?? ""
                        QuantityeractionDelay.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = WithoutModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(noti_selectString.replacingOccurrences(of: "rank", with: "t") + mainStatId.replacingOccurrences(of: "vid", with: "t"))] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(String(dataMapProcessorValue))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = AgainstModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(main_randomId))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (constWritingFormat.lowercased() + "_chat" + String(k_projectionKey.suffix(5)))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.dividePending(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(dataCompleteSaveValue.replacingOccurrences(of: "functional", with: "a"))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(show_memoryFormat.prefix(9)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(kJustHeapUrl))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(app_evolutionTitle.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(userHeavyStr.suffix(8)) + String(notiVisualName))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = SinkTap(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(data_lostUrl.prefix(5)) + String(notiAvailableMessage))] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = PlateUniformModel.beginInTap()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = PlateUniformModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = AttributeModel.enter()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = AttributeModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = VariableModel.denseTransition()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func styleBy(_ extraDic: [String: Any]) -> StreamAssociate {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(String(dataMapProcessorValue))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? SceneModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(String(const_maleTitle) + String(user_destinationFlagSecularUrl))] as? String == (data_restoreValue.lowercased()) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(main_toInjuryTitle.replacingOccurrences(of: "business", with: "i") + String(constEqualWithData.prefix(3)))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(constPmThatUrl.replacingOccurrences(of: "remove", with: "im") + "eData")] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(userBeforeKey)) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = shape(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(user_aId.lowercased())] as? Int) == 1 || (msgInfo?[(user_aId.lowercased())] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (String(constOndPath)) || (dic?[(String(kJustHeapUrl))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(String(const_maleTitle) + String(user_destinationFlagSecularUrl))] as? String == "mf") && ((dic?[(String(kJustHeapUrl))] as? Int) == 15 || (dic?[(String(kJustHeapUrl))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (noti_honeyWholeMsg.replacingOccurrences(of: "minimum", with: "s") + String(showTransformKey.prefix(7))) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(noti_dayEnoughMsg.prefix(7)))] as? String == (String(appEraseMutualUrl.suffix(4)) + "Gift") || (msgInfo?[(String(kJustHeapUrl))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func tweenGreet(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.dividePending(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(String(dataMapProcessorValue))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(appTipMsg) + main_insideData.replacingOccurrences(of: "mm", with: "fo"))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(show_alongsideData.replacingOccurrences(of: "protection", with: "er"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(String(notiDealDetectString))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == QuantityeractionDelay.share.loginUserMode.userID {
        if senderId == QuantityeractionDelay.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(String(notiDealDetectString))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(notiListString))] = message.userID ?? (user?[(String(notiListString))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(notiDealDetectString))] = "\(msgInfo?[(String(main_domainHungTitle))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(notiListString))] = "\(msgInfo?[(String(main_domainHungTitle))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(app_evolutionTitle.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(app_evolutionTitle.prefix(5)))] = "\(msgInfo?[(String(app_evolutionTitle.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.dividePending(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(app_evolutionTitle.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(app_evolutionTitle.prefix(5)))] = "\(customDic[(String(app_evolutionTitle.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(app_evolutionTitle.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((dataSpringUrl.replacingOccurrences(of: "gallery", with: "o") + "Sandbo" + main_farmerName.replacingOccurrences(of: "before", with: "x"))) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(dataSpringUrl.replacingOccurrences(of: "gallery", with: "o") + "Sandbo" + main_farmerName.replacingOccurrences(of: "before", with: "x"))] = "\(msgInfo?[(dataSpringUrl.replacingOccurrences(of: "gallery", with: "o") + "Sandbo" + main_farmerName.replacingOccurrences(of: "before", with: "x"))] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(userFaceTitle.suffix(6)) + "ength")) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(userFaceTitle.suffix(6)) + "ength")] = "\(msgInfo?[(String(userFaceTitle.suffix(6)) + "ength")] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (String(app_primaryData) + String(user_teamMessage.prefix(7)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(k_toTitle) + String(dataSparkTitle))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == QuantityeractionDelay.share.loginUserMode.userID ? "1" : "0"
        info?[(String(user_protectionSecretString.prefix(6)))] = senderId == QuantityeractionDelay.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func discoverUniform(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = tweenGreet(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(app_evolutionTitle.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func giftTo(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func squareToSpreadForm(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func shape(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(noti_dayEnoughMsg.prefix(7)))] as? String, msgType == (data_particleData.replacingOccurrences(of: "addition", with: "d")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(user_speedPath.replacingOccurrences(of: "avoid", with: "t") + String(k_accelerateMsg))] as? String,
           //: contentType == "AudioMsg"
           contentType == (String(noti_warningString))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(user_entryKey.prefix(8)) + notiFragmentTotalName.replacingOccurrences(of: "rapid", with: "a"))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(main_perString) + String(constRocketString.suffix(6)))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
