
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let show_pictureUnableMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let dataErrorMessage:[UInt8] = [0xf1,0xed,0xe0]

/*: "headPic" :*/
fileprivate let user_directionPath:[Character] = ["h","e","a","d","P","i","c"]

/*: "nickname" :*/
fileprivate let showQualityFormat:String = "nicknaselection"

/*: "birthday" :*/
fileprivate let notiDrawId:String = "biflex"
fileprivate let appAlbumMessage:[Character] = ["t","h","d","a","y"]

/*: "videoUrl" :*/
fileprivate let user_belowFutureName:[Character] = ["v","i","d","e","o","U","r","l"]

/*: "location" :*/
fileprivate let notiAppearDetailedName:String = "extend"
fileprivate let dataPlusAccessMsg:String = "osaveation"

/*: "cmd" :*/
fileprivate let dataRedId:[UInt8] = [0x20,0x2a,0x21]

fileprivate func markerCompareCapacity(target num: UInt8) -> UInt8 {
    let value = Int(num) + 67
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let app_downName:[Character] = ["r","e","f","u","s","e","C"]
fileprivate let userFloorDistributionPath:String = "atiptip"

/*: "data" :*/
fileprivate let kAgentSubjectFormat:[UInt8] = [0xf,0xa,0x1f,0xa]

/*: "Call interruption" :*/
fileprivate let userIncreaseMediaString:String = "Call private alongside fluid"
fileprivate let const_easeUrl:String = "inagainst"
fileprivate let constAppointedFeedbackUrl:String = "insteaduption"

/*: "stopCall" :*/
fileprivate let k_restoreUrl:String = "reveal"
fileprivate let constStopString:String = "topCallpo integrity purchase reward yes"

/*: "logId" :*/
fileprivate let k_tablePublishStr:[UInt8] = [0x1c,0x1f,0x17,0x39,0x14]

/*: "cancelCall" :*/
fileprivate let show_sustainableRawKey:String = "cancelCatoday then match description"
fileprivate let data_bounceStr:String = "fadefade"

/*: "No answer" :*/
fileprivate let kAcceptFormat:String = "Noevent eff"
fileprivate let show_businessGapValue:[Character] = [" ","a","n","s","w","e","r"]

/*: "duration" :*/
fileprivate let user_exampleTitle:[Character] = ["d","u","r","a","t","i","o","n"]

/*: "She has hung up" :*/
fileprivate let showGroupFormat:[Character] = ["S","h","e"]
fileprivate let mainNoticeValidOnId:String = "later flex eager has "

/*: "cancelSex" :*/
fileprivate let data_okFeatureTitle:String = "caplate"

/*: "He has hung up" :*/
fileprivate let noti_boyPath:[Character] = ["H","e"," ","h","a","s"," "]
fileprivate let appReserveSmartUrl:String = "hung upfemale double active"

/*: "endType" :*/
fileprivate let app_nearbyValue:String = "valid pause kit agreeendType"

/*: "stopSex" :*/
fileprivate let mainDuringPath:String = "stopSexting successfully"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let main_feeConsequenceUrl:[UInt8] = [0x44,0x63,0x7e,0x78,0x6b,0x6b,0x64,0x6e,0x64,0x68,0x63,0x79,0x2d,0x6a,0x62,0x61,0x69,0x2d,0x6e,0x62,0x64,0x63,0x7e,0x21,0x2d,0x6e,0x6c,0x61,0x61,0x2d,0x65,0x6c,0x7e,0x2d,0x68,0x63,0x69,0x68,0x69]

private func marginFault(heart num: UInt8) -> UInt8 {
    return num ^ 13
}

/*: "The other hung up the phone" :*/
fileprivate let main_anonymousPath:String = "The ointerest player"
fileprivate let main_nousMsg:String = " hung game conversation"
fileprivate let mainFunMustPath:String = "change salt warn before turnhe p"
fileprivate let kDistanceMessage:String = "holaunch"

/*: "type" :*/
fileprivate let data_onceName:String = "tyheroe"

/*: "jumpUrl" :*/
fileprivate let dataPendingData:String = "jumpUrlbirthday convert that this"

/*: "Cancel" :*/
fileprivate let app_invitationData:String = "program the recto policyCancel"

/*: "Get gold coins" :*/
fileprivate let notiFogName:[Character] = ["G"]
fileprivate let app_endString:String = "integration actor classet go"

/*: "refuseSex" :*/
fileprivate let constAdministrativeGlobalName:[Character] = ["r","e","f","u","s","e","S","e","x"]

/*: "onAcceptCall" :*/
fileprivate let userPastStarFormat:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let data_reportData:[Character] = ["a","c","c","e","p","t","C","a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let k_speakerKeyUrl:String = "Calls successful pic number"
fileprivate let main_cigarEaseMessage:[Character] = ["a","r"]
fileprivate let userDisplayTimingPath:String = "e canctop compute"

/*: "The call has ended" :*/
fileprivate let constWritingValue:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"," "]
fileprivate let const_departmentTimeTitle:String = "enlistenerelistener"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  TellPerform.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let showDiscoverData = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class TellPerform: ScaleViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: DismissModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: SpotSkin? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: PaintView? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        UpZone.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        UpZone.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        UpZone.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: show_pictureUnableMsg.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! WithoutImplement
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.storage()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            warning()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.fromRefuse()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        enableAccess()
        //: setupSubViewsConstraint()
        futureWithinSize()
        //: initNotificationObserver()
        must()
        //: req_callGetUserInfo()
        remarkDoing()
        //: timeoutRules()
        delicate()
        //: resumeTalk()
        pairReload()
        //: setUpBGMPlayer()
        painter()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
            if QuantityeractionDelay.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = SignaturePriority(frame: CGRect(x: 0, y: 0, width: dataTabUrl, height: const_rebuildWithinId))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.agileImageSkewy = false
        UpZone.shared.agileImageSkewy = false
        //: TalkingSocketManager.shared.isCalling = false
        UpZone.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.commonUs()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: SubstantialRow = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = SubstantialRow(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: OrangeSpot = {
        //: let m = TalkingVoiceChatManager()
        let m = OrangeSpot()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension TellPerform {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func remarkDoing() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        AroundSubtle.levelEx(uid: pairUid) { [weak self] succeed, result, errorModel in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: dataErrorMessage.map{$0^132}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(user_directionPath))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(showQualityFormat.replacingOccurrences(of: "selection", with: "me"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(notiDrawId.replacingOccurrences(of: "flex", with: "r") + String(appAlbumMessage))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(user_belowFutureName))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(notiAppearDetailedName.replacingOccurrences(of: "extend", with: "l") + dataPlusAccessMsg.replacingOccurrences(of: "save", with: "c"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.pan()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension TellPerform {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func warning() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        StrengthMomentum.lawmakerStorage(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.paraDown(showMsg: k_implementMsg)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(app_downName) + userFloorDistributionPath.replacingOccurrences(of: "tip", with: "l")), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: dataErrorMessage.map{$0^132}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.disable()
                //: return
                return
            }

            //: self.stopBGM()
            self.even()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = SpotSkin.paint(type: .Response)
            //: self.startBGM()
            self.storage()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func commonUs() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.even()
        //: TalkingSocketManager.shared.callerDelegate = nil
        UpZone.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        UpZone.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        UpZone.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func disable(animated: Bool = true) {
        //: self.stopBGM()
        self.even()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func bullet() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.provideSecure(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func pairReload() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            bullet()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension TellPerform {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func must() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(pic(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(own(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: noti_gestureTechniqueMsg,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        showDiscoverData.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func pic(notif: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            disable()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func own(note: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard UpZone.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        UpZone.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        UpZone.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        UpZone.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.attributeOrUnless(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension TellPerform: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.paraDown(showMsg: (String(userIncreaseMediaString.prefix(5)) + const_easeUrl.replacingOccurrences(of: "against", with: "te") + constAppointedFeedbackUrl.replacingOccurrences(of: "instead", with: "rr")).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (k_restoreUrl.replacingOccurrences(of: "reveal", with: "s") + String(constStopString.prefix(7))), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(show_sustainableRawKey.prefix(8)) + data_bounceStr.replacingOccurrences(of: "fade", with: "l")), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(app_downName) + userFloorDistributionPath.replacingOccurrences(of: "tip", with: "l")), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: dataErrorMessage.map{$0^132}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.publisher()
                //: TalkingSocketManager.shared.agileImageSkewyVC = nil
                UpZone.shared.agileImageSkewyVC = nil
            }
            //: popCurrentViewController()
            disable()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.aboveReasonMake(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension TellPerform {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func painter() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? HandleDivide.Call : HandleDivide.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = SpotSkin.paint(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func storage() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.sustainable()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func even() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.prepare()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension TellPerform {
    /// 超时定时器
    //: private func timeoutRules() {
    private func delicate() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? notiNaturalTitle : self.chatModel?.outAdvanced()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            displayBig()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.displayBig()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func displayBig() {
        //: guard !TalkingSocketManager.shared.agileImageSkewy else { return }
        guard !UpZone.shared.agileImageSkewy else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.paraDown(showMsg: (String(kAcceptFormat.prefix(2)) + String(show_businessGapValue)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(show_sustainableRawKey.prefix(8)) + data_bounceStr.replacingOccurrences(of: "fade", with: "l")), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(app_downName) + userFloorDistributionPath.replacingOccurrences(of: "tip", with: "l")), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: dataErrorMessage.map{$0^132}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        UpZone.shared.commonCrop(info: socketDict)
        //: popCurrentViewController()
        disable()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension TellPerform {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func tillItem(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(user_exampleTitle))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.impression(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.anAgainst()
            //: createTalkRoom()
            bullet()
            //: self.stopBGM()
            self.even()
        }
    }
}

// MARK: - MasterRange【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension TellPerform: MasterRange {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func plusBy(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showGroupFormat) + String(mainNoticeValidOnId.suffix(5)) + "hung up").localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(data_okFeatureTitle.replacingOccurrences(of: "plate", with: "nc") + "elSex")].stringValue == CommonAmong.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_boyPath) + String(appReserveSmartUrl.prefix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.paraDown(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.publisher()
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }

        //: popCurrentViewController()
        disable()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func appropriateProperty(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.even()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.anAgainst()
        //: self.createTalkRoom()
        self.bullet()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func minimize(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(app_nearbyValue.suffix(7)))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(showGroupFormat) + String(mainNoticeValidOnId.suffix(5)) + "hung up").localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(mainDuringPath.prefix(7)))].stringValue == CommonAmong.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(noti_boyPath) + String(appReserveSmartUrl.prefix(7))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue {
            if QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: main_feeConsequenceUrl.map{marginFault(heart: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(main_anonymousPath.prefix(5)) + "ther" + String(main_nousMsg.prefix(6)) + "up t" + String(mainFunMustPath.suffix(4)) + kDistanceMessage.replacingOccurrences(of: "launch", with: "ne")).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.paraDown(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.publisher()
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.attachPresent()
        //: self.popCurrentViewController()
        self.disable()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: k_listenFormat, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func nonePriority(msg: String, data: [String: Any]) {
        //: guard QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(data_onceName.replacingOccurrences(of: "hero", with: "p"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(dataPendingData.prefix(7)))] as? String else { return }
            //: let httpsUrl = QuantityeractionDelay.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = QuantityeractionDelay.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = ImageTranslate()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: TellEnd.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            TellEnd.share.atRein(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            TimingVersus.noEntityGuide(title: nil,
                                           //: message: msg,
                                           message: msg,
                                           //: leftBtnTitle: "Cancel".localized,
                                           leftBtnTitle: (String(app_invitationData.suffix(6))).localized,
                                           //: rightBtnTitle: "Get gold coins".localized) {
                                           rightBtnTitle: (String(notiFogName) + String(app_endString.suffix(5)) + "ld coins").localized)
            {
                //: TalkingAlertShow.hideAlert()
                TimingVersus.stripNow()
                //: } rightBlock: {
            } rightBlock: {
                //: TellEnd.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                TellEnd.share.somePush(clickEvent: const_thirdOldValue, sufficient: false)
            }
        }
    }
}

// MARK: - SingleExtra【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension TellPerform: SingleExtra {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func shadowBrush(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(showGroupFormat) + String(mainNoticeValidOnId.suffix(5)) + "hung up").localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(String(constAdministrativeGlobalName))].stringValue == CommonAmong.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(noti_boyPath) + String(appReserveSmartUrl.prefix(7))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.paraDown(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.publisher()
            //: TalkingSocketManager.shared.agileImageSkewyVC = nil
            UpZone.shared.agileImageSkewyVC = nil
        }
        //: popCurrentViewController()
        disable()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func closePainter(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.even()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.anAgainst()
        //: self.createTalkRoom()
        self.bullet()
    }
}

// MARK: - StatusKit【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension TellPerform: StatusKit {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func frequencyComment(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(app_downName) + userFloorDistributionPath.replacingOccurrences(of: "tip", with: "l")) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.paraDown(showMsg: (String(kAcceptFormat.prefix(2)) + String(show_businessGapValue)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.publisher()
                //: TalkingSocketManager.shared.agileImageSkewyVC = nil
                UpZone.shared.agileImageSkewyVC = nil
            }
            //: self.popCurrentViewController()
            self.disable()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(userPastStarFormat)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard QuantityeractionDelay.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard QuantityeractionDelay.share.appStatus == TotalervalObserve.normal.rawValue else { return }
            //: TellEnd.share.func__jumpToWebRecharge(sufficient: false)
            TellEnd.share.somePush(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(userPastStarFormat)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.paraDown(showMsg: msg)
            //: self.popCurrentViewController()
            self.disable()
        }
    }
}

// MARK: - GroupMake【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension TellPerform: GroupMake {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func enableConfirm() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(data_reportData)), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: dataErrorMessage.map{$0^132}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func last() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.paraDown(showMsg: (String(k_speakerKeyUrl.prefix(6)) + String(main_cigarEaseMessage) + String(userDisplayTimingPath.prefix(6)) + "elled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(show_sustainableRawKey.prefix(8)) + data_bounceStr.replacingOccurrences(of: "fade", with: "l")), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.paraDown(showMsg: (String(k_speakerKeyUrl.prefix(6)) + String(main_cigarEaseMessage) + String(userDisplayTimingPath.prefix(6)) + "elled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (String(app_downName) + userFloorDistributionPath.replacingOccurrences(of: "tip", with: "l")), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: dataErrorMessage.map{$0^132}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.paraDown(showMsg: (String(constWritingValue) + const_departmentTimeTitle.replacingOccurrences(of: "listener", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            UpZone.shared.commonCrop(info: [String(bytes: dataRedId.map{markerCompareCapacity(target: $0)}, encoding: .utf8)!: (k_restoreUrl.replacingOccurrences(of: "reveal", with: "s") + String(constStopString.prefix(7))), String(bytes: kAgentSubjectFormat.map{$0^107}, encoding: .utf8)!: [String(bytes: k_tablePublishStr.map{$0^112}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.attachPresent()
        //: self.popCurrentViewController()
        self.disable()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func frequency(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.aboveSum(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func captureIn(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.scopeTiming(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func atPrior() {
        //: TalkingSocketManager.shared.agileImageSkewyVC = self
        UpZone.shared.agileImageSkewyVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = PaintView.customerAcross()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.moreHide()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.beyondSize()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.nearPure(talkingTime)
        //: popCurrentViewController()
        disable()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func tail(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.nearPure(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func borderChallenge(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.towardExit(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension TellPerform {
    /// 添加视图
    //: private func setupSubviews() {
    private func enableAccess() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func futureWithinSize() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
